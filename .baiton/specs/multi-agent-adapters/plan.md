# plan result

```json
{
  "steps": [
    {
      "title": "Re-confirm the resolution/gating surface before editing (read-only)",
      "detail": "What exists today. src/activation/executable.ts exports the per-agent core `resolveExecutable(agent, executableName, lookup, override): Result<ResolvedExecutable, ExecutableError>` (line 94) with the seams `ExecutableLookup` (35) and `OverrideGetter` (43), and the error union `override-missing | not-on-path` (72-74). src/extension.ts hard-wires exactly one agent: the consts `CLAUDE_AGENT`/`CLAUDE_EXECUTABLE` (58-59), one `resolveExecutable(...)` call (137-149) whose failure is shown as a warning, and `const canDispatch = !workspace.restricted && executable !== undefined;` (153), both stashed on `ActivationState` (66-82) and passed to `registerCommands` (169-173). `getActivationState`/`ActivationState` have no other reader anywhere in src/ or test/ (verified by grep), so the shape is free to change. src/activation/commands.ts mirrors the two fields on `CommandActivation` (126-132) and gates through exactly two helpers: `ensureCanDispatch` (752) = restricted check then `ensureExecutable`, and `ensureExecutable` (768) = `activation.canDispatch`. Four call sites: `runStage` (731, reached from `registerStageCommand` at 571 which knows its `Stage`), the action command `replan`/`stop` (588), `registerViewCommand` (652), and `runSubmitPr` (846). The canonical agent-id -> binary map already landed in T01: `AGENT_BINARY` at src/adapter/adapter.ts:16 (`claude`->`claude`, `opencode`->`opencode`, `antigravity`->`agy`, `codex`->`codex`) with `isAgentId` at src/adapter/index.ts:24. `STAGE_ROLE` (src/activation/engineFacade.ts:47) is the stage -> role table commands.ts already imports (line 92). `config.roles[role].agent` is an unvalidated string (src/config/types.ts:20). No source change in this step.",
      "files": [
        "src/activation/executable.ts",
        "src/extension.ts",
        "src/activation/commands.ts",
        "src/adapter/adapter.ts",
        "src/adapter/index.ts",
        "src/activation/engineFacade.ts",
        "src/config/types.ts"
      ]
    },
    {
      "title": "Add a per-agent executable table to executable.ts, keeping resolveExecutable as its core",
      "detail": "Do not change `resolveExecutable`'s signature or behaviour — it stays the single-agent pure core and test/activation.gating.test.ts's six existing cases must pass untouched. Add on top of it:\n\n1. A third `ExecutableError` variant `| { kind: 'unknown-agent'; agent: string; message: string }` with a doc bullet in the existing block (72-74): 'the configured `agent` id is not one this build supports, so no executable name is known for it; the roles using it cannot dispatch'. Message wording, matching the T06 engine refusal so the user sees one vocabulary: `\"<agent>\" is not a supported agent id; use one of claude, opencode, antigravity, codex in \"roles.<role>.agent\" in .baiton/config.json.` Build the id list from `Object.keys(AGENT_BINARY)` rather than a literal so a future id cannot drift.\n\n2. `export interface AgentExecutables` — the resolved table, answering per agent id: `get(agent: string): ResolvedExecutable | undefined`, `errorFor(agent: string): ExecutableError | undefined` (undefined exactly when `get` returns a value), `readonly errors: readonly ExecutableError[]` (every failure, in first-seen agent order, for surfacing once at activation), and `readonly agents: readonly string[]` (the distinct agents that were asked for, in first-seen order). Exactly one of `get`/`errorFor` answers for an agent that was resolved; both return `undefined` for an agent that was never asked for — document that third case explicitly, because commands.ts relies on it not being confused with 'failed'.\n\n3. `export function resolveAgentExecutables(agents: readonly string[], lookup: ExecutableLookup, override: OverrideGetter): AgentExecutables`. It de-duplicates `agents` preserving first-seen order (several roles normally share one agent, and a PATH walk per role would be wasted work and duplicate warnings); for each distinct id: `isAgentId(id)` false -> record the `unknown-agent` error; otherwise call `resolveExecutable(id, AGENT_BINARY[id], lookup, override)` and record the ok value or the error. The override seam is already keyed by agent (`baiton.agents.<agent>.path`), so nothing about override handling changes — each agent gets its own override key for free.\n\nImport `AGENT_BINARY` and `isAgentId` from '../adapter' directly rather than adding a `binaryFor` seam: the spec's architecture note asks for one canonical home for the map that both the registry and the executable resolver read, and both are pure constants/predicates, so testability is unaffected. src/adapter/adapter.ts imports only ../model/role, so there is no cycle.\n\n4. Extend the module doc block (1-22) to say the resolver now resolves one executable per distinct agent id referenced in `config.roles`, that a per-role dispatch gate is derived from that table rather than a single global flag, and that a stale or missing executable disables dispatch only for the roles configured with that agent.",
      "files": [
        "src/activation/executable.ts",
        "src/adapter/adapter.ts",
        "src/adapter/index.ts"
      ]
    },
    {
      "title": "Resolve one executable per configured agent in extension.ts",
      "detail": "In src/extension.ts:\n\n1. Delete the `CLAUDE_AGENT`/`CLAUDE_EXECUTABLE` consts (57-59) and their comment; the agent set now comes from config and the binary names from `AGENT_BINARY`. Keep `SETTINGS_NS`.\n\n2. Change the `./activation` import to bring in `resolveAgentExecutables` and `type AgentExecutables` (drop `resolveExecutable`/`ResolvedExecutable` if they become unused — `ResolvedExecutable` is no longer named on `ActivationState`). Import `ROLES` from './model' (or from './model/role') to enumerate the roles.\n\n3. Replace the single-resolve block (134-149) with, after the config load so `config` is in scope:\n```ts\n// Locate every configured agent's executable on PATH with a per-agent settings\n// override (Req 22.7). Roles may name different agents, so resolution is per\n// distinct agent id and a miss disables dispatch only for the roles that use\n// it (Req 22.8, 14.5) — the workspace still opens for browsing.\nconst executables = resolveAgentExecutables(\n  ROLES.map((role) => config.roles[role].agent),\n  pathLookup,\n  settingsOverride,\n);\nfor (const failure of executables.errors) {\n  void vscode.window.showWarningMessage(failure.message);\n}\n```\nOne warning per distinct failing agent, not per role — that is what the de-duplication in `resolveAgentExecutables` buys.\n\n4. Delete `const canDispatch = ...` (153). Restricted Mode is still a global condition and stays where it is (`workspace.restricted`); what disappears is only the executable half of the flag. Replace `ActivationState`'s `executable`/`canDispatch` fields (66-82) with a single `readonly executables: AgentExecutables;`, and rewrite its doc comment: the table carries one resolution per distinct agent id in `config.roles`; dispatch for a role is permitted when the workspace is not restricted (Req 22.2) and that role's agent resolved (Req 22.8, 14.5).\n\n5. Update both construction sites — `activationState = { workspace, config, executables };` (162) and the `registerCommands(context, { workspace, config, executables }, surface)` call (169-173).\n\n6. Leave `pathLookup`, `settingsOverride`, `executableExtensions`, `isExecutableFile` and the whole crash-recovery half of the file untouched. `settingsOverride` already interpolates the agent into `agents.${agent}.path`, so the new ids work the moment T08 declares them in package.json; until then a non-claude override simply reads as unset and PATH resolution applies. Also update the activation-order doc block (29-52) step 4/5 narration to mention per-agent executable resolution.",
      "files": [
        "src/extension.ts",
        "src/activation/executable.ts",
        "src/model/role.ts"
      ]
    },
    {
      "title": "Turn the two gate helpers in commands.ts into per-role gates",
      "detail": "In src/activation/commands.ts:\n\n1. `CommandActivation` (126-132): replace `executable: ResolvedExecutable | undefined;` and `canDispatch: boolean;` with `executables: AgentExecutables;`, and swap the `import type { ResolvedExecutable } from './executable';` (84) for `import type { AgentExecutables } from './executable';`.\n\n2. Rewrite `ensureExecutable` (768) to take the role it is gating:\n```ts\nfunction ensureExecutable(activation: CommandActivation, surface: Surface, role: Role): boolean {\n  const agent = activation.config.roles[role].agent;\n  const failure = activation.executables.errorFor(agent);\n  if (failure === undefined) {\n    return true;\n  }\n  surface.warn(`Baiton cannot dispatch the \"${role}\" stage: ${failure.message}`);\n  return false;\n}\n```\nThe message now names the role and the agent instead of the hard-coded 'the Claude executable', which is the user-facing point of this todo: with a mixed config the warning has to say *which* role is blocked and *which* binary is missing. Rewrite its doc comment accordingly (it currently says 'the Claude executable').\n\n3. Rewrite `ensureCanDispatch` (752) as `ensureCanDispatch(activation, surface, role)`: keep the Restricted Mode check first and unchanged (it is genuinely global, Req 22.1/22.2), then delegate to the role-gated `ensureExecutable`.\n\n4. Both helpers read `activation.config`, which `CommandActivation` already carries, so no new parameter threading beyond the role.",
      "files": [
        "src/activation/commands.ts",
        "src/activation/executable.ts"
      ]
    },
    {
      "title": "Pass the right role at each of the four gate call sites",
      "detail": "Still in src/activation/commands.ts, decide per call site which role the gate is about — this is where the global flag actually disappears:\n\n1. Stage triggers (`runStage`, 722-742, reached from `registerStageCommand` at 571): `runStage` already receives `stage`, so gate with `STAGE_ROLE[stage]` — the same table the facade uses to pick the role it dispatches with, so the gate and the launch can never disagree. `ensureCanDispatch(activation, surface, STAGE_ROLE[stage])`.\n\n2. Submit PR (`runSubmitPr`, 846): gate with `'pr-writer'` — the literal `submitPr` itself resolves its adapter and model from (src/engine/submitPr.ts, T06).\n\n3. The View command (`registerViewCommand`, 652): View attaches to a *recorded* run, so its role is `STAGE_ROLE[entry.stage]` — known only inside `runView` after the journal entry is read (699), i.e. after the current gate runs. Move the check: drop `ensureExecutable` from the command body and, in `runView`, fold the executable check into the existing unknown-agent branch (698-704) right after the role is derived, gating on the *same* role that `attach` will use. Keep the existing `adapterForRole` refusal as-is and add, beside it, `const failure = activation.executables.errorFor(config.roles[role].agent);` -> `surface.warn(...)` + return. This needs `activation` (or just the table + config) threaded into `runView`; pass the already-available `activation` object rather than two new parameters. Note the consequence in `runView`'s doc comment: a mixed-agent config can now view a session whose agent is installed even while another role's agent is missing — which is exactly the old global flag's failure mode.\n\n4. Control actions `replan`/`stop` (`registerActionCommand`, 588): these launch no sub-agent — src/activation/engineFacade.ts:87-96 dispatches them with a placeholder `role: 'planner'` and the comment 'Control actions launch no sub-agent'. There is therefore no role whose executable is meaningfully required, and gating `stop` on a binary being present is actively wrong (you want to be able to cancel a run and clear the queue when the CLI has gone missing). Replace `ensureCanDispatch(activation, surface)` here with the Restricted Mode check alone — extract that check into a small `ensureNotRestricted(activation, surface): boolean` used by both `ensureCanDispatch` and this call site, so there is one copy of the wording. Record the reason in a one-line comment at the call site. This is a deliberate, user-visible loosening; flag it in the execution summary for the reviewer.\n\n5. Leave the orchestrator/tool-driven dispatch path untouched: it goes through the run queue, which probes the role's adapter before every launch (Req 14.2) and refuses `probe-failed`, so a missing binary is still caught there without a second gate.",
      "files": [
        "src/activation/commands.ts",
        "src/activation/engineFacade.ts"
      ]
    },
    {
      "title": "Extend test/activation.gating.test.ts with per-agent resolution cases",
      "detail": "Add a `describe('resolveAgentExecutables (Req 22.7, 22.8, 14.5)')` beside the existing `resolveExecutable` block, reusing its `lookupFrom`/`overrideOf` helpers (widen `overrideOf` to a per-agent table, e.g. `overrideTable(t: Record<string, string>)` returning `(agent) => t[agent]`, without touching the existing single-value helper the current tests use). Cases:\n- resolves several distinct agents in one call: `['claude', 'antigravity']` with a lookup table containing `claude` and `agy` -> both `get(...)` hit, `errors` empty; asserts the `antigravity` -> `agy` binary mapping is honoured (the one place the T01 map is exercised end-to-end).\n- de-duplicates: `['claude', 'claude', 'codex']` asks the lookup for `claude` once (count calls in the stub) and reports `agents` as `['claude', 'codex']`.\n- partial failure is per agent: `['claude', 'opencode']` where only `claude` is on PATH -> `get('claude')` resolves, `errorFor('opencode')` is `not-on-path`, `errors` has length 1. This is the property the whole todo exists for.\n- per-agent override: an override set only for `codex` resolves `codex` from the override (`override: true`) while `claude` still resolves from PATH.\n- unknown id: `['nope']` -> `errorFor('nope').kind === 'unknown-agent'`, `get('nope')` undefined, and the message names the supported ids.\n- never asked for: `get('codex')`/`errorFor('codex')` both undefined when `codex` was not in the input.\nDo not weaken the six existing `resolveExecutable` cases; they must pass unchanged as the evidence that the single-agent core is untouched.",
      "files": [
        "test/activation.gating.test.ts",
        "src/activation/executable.ts"
      ]
    },
    {
      "title": "Verify: type-check, lint, suite, and unchanged claude-only behaviour",
      "detail": "Run `npm run compile`, `npm run lint` and `npm test`; all three must pass. The compile is the real check that every `canDispatch`/`executable` reader was converted — grep `canDispatch` and `activation.executable` across src/ and test/ afterwards and expect no hits outside the new per-role helpers. Confirm the no-behaviour-change property for the default config: with all six roles on `claude`, `resolveAgentExecutables` makes exactly one PATH lookup for `claude`, every role's gate consults that one resolution, and a missing claude binary still blocks plan/execute/review/submit-PR exactly as before (only the message text changes, now naming the role). No test currently constructs `CommandActivation` or `ActivationState` (grep confirms), so the command-layer change breaks no test at compile time. Explicitly do not touch package.json (T08) and do not feed `ResolvedExecutable.path` into any adapter's `shellPath` (out of scope; adapters keep emitting the bare binary name).",
      "files": [
        "package.json",
        "src/activation/commands.ts",
        "src/extension.ts",
        "test/activation.gating.test.ts"
      ]
    }
  ],
  "risks": [
    "Two deliberate behaviour changes fall out of deleting the global flag, and a reviewer will read them as scope creep unless they are called out. (a) `ensureExecutable` today reads `activation.canDispatch`, which already includes `!workspace.restricted` — so the View command is in fact blocked under Restricted Mode despite its doc comment claiming it is not. Splitting the gate makes View behave as documented (it writes nothing). (b) `replan`/`stop` stop being executable-gated because they launch no sub-agent. Both are defensible and should be stated in the execution summary; if the reviewer wants strict parity instead, the fix is to keep calling the role-gated `ensureCanDispatch` from those sites with an explicit placeholder role, which is worse but smaller.",
    "A role configured with an unrecognised agent id now produces two messages in a session: a warning at activation (from `executables.errors`) and, if the user triggers that role anyway, the T06 `unknown-agent` dispatch refusal. That redundancy is intentional (fail early, fail again at the point of use) but the two wordings must agree — both must name `roles.<role>.agent` in `.baiton/config.json`. Keeping them in sync is the reason the message text is specified in step 2.",
    "T08 has not landed, so `baiton.agents.opencode.path` / `.antigravity.path` / `.codex.path` are not declared in package.json yet. `vscode.workspace.getConfiguration().get` returns `undefined` for an undeclared key, so an override for a new agent silently does nothing until T08. PATH resolution still works, so this todo is not blocked — but do not 'fix' it by editing package.json here.",
    "The resolution is a snapshot taken at activation, as it is today: changing `roles.<role>.agent` in `.baiton/config.json`, or installing the missing CLI, needs a window reload before the gate re-opens. Making the table reactive (a config/settings watcher) is a different todo; do not add one here.",
    "`ActivationState` is a publicly exported shape and this replaces two of its four fields. Nothing in src/ or test/ reads it today (verified), so the change is contained — but if a reviewer expects `executable` to survive for a future consumer, the compromise is to keep a derived `executable` getter, which would reintroduce exactly the single-agent assumption this todo removes. Prefer the clean removal.",
    "`resolveAgentExecutables` performs one PATH walk per distinct agent at activation. With four agents on a cold Windows PATH (with PATHEXT expansion) this is up to four synchronous directory scans on the activation path. In practice a default config names one agent and the de-duplication keeps it at one walk; only a genuinely mixed config pays more. If it ever matters the fix is lazy resolution behind the table, not a different algorithm — do not pre-optimise here.",
    "Importing `AGENT_BINARY`/`isAgentId` from '../adapter' into src/activation/executable.ts pulls the adapter barrel (src/adapter/index.ts re-exports all four adapters) into the activation module graph. It is type-and-const only and there is no cycle, but if the barrel import proves awkward, import from '../adapter/adapter' directly (the map's real home) and keep `isAgentId`'s logic as a local `hasOwnProperty` check — decide once and note which was chosen."
  ],
  "acceptance": [
    "src/extension.ts no longer names `claude` anywhere: the `CLAUDE_AGENT`/`CLAUDE_EXECUTABLE` consts are gone and the agent set comes from `ROLES.map((role) => config.roles[role].agent)`.",
    "Executable resolution runs once per *distinct* configured agent id, mapping each id to its binary through `AGENT_BINARY` (`antigravity` -> `agy`), with one warning surfaced per distinct failing agent rather than one per role.",
    "`resolveExecutable`'s signature and behaviour are unchanged and the six existing `resolveExecutable` cases in test/activation.gating.test.ts pass untouched.",
    "`ExecutableError` gains an `unknown-agent` variant whose message names the role's config key and the supported agent ids, and `resolveAgentExecutables` returns it for an id `isAgentId` rejects.",
    "`ActivationState` and `CommandActivation` carry an `AgentExecutables` table in place of `executable` + `canDispatch`; no `canDispatch` identifier remains in src/ or test/.",
    "Stage dispatch is gated per role: `runStage` gates on `STAGE_ROLE[stage]`, Submit PR on `'pr-writer'`, and View on `STAGE_ROLE[entry.stage]` after the journal entry is read — so a missing binary for one agent blocks only the roles configured with that agent.",
    "A blocked dispatch warns with a message naming both the role and the agent/binary at fault, not the hard-coded 'Claude executable' text.",
    "Restricted Mode still blocks every write/dispatch path exactly as before (Req 22.1, 22.2); only the executable half of the old flag became per-role.",
    "With the default all-claude config, behaviour is unchanged: one PATH lookup for `claude`, and a missing `claude` still blocks plan/execute/review/submit-PR.",
    "No change to package.json (T08) and no adapter `shellPath` now carries a resolved absolute path; `npm run compile`, `npm run lint` and `npm test` all pass, including the new `resolveAgentExecutables` cases."
  ]
}
```
