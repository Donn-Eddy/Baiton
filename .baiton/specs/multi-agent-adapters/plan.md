# plan result

```json
{
  "steps": [
    {
      "title": "Re-read the three anchor files before writing anything (read-only)",
      "detail": "What exists today. test/adapter.claude.test.ts is the family shape: plain mocha + node `assert`, a module-level doc comment naming the pinned contracts, a local `req(overrides)` builder returning a full `LaunchRequest` ({role:'executor', model:'sonnet', prompt, runId:'run-123', resume:false, sessionId:'session-abc'}), a local `findPair(args, flag, value)` returning the index of an ADJACENT flag/value pair or -1, and one `describe` per contract group. test/adapter.opencode.test.ts (added by T09, reviewed pass) is the immediate precedent for a non-claude adapter test and should be matched for structure, comment style and naming — but NOT copied blindly, because opencode and agy differ in arg shape. src/adapter/antigravity.ts is the unit under test: it exports `AntigravityAdapter` (constructor takes NO arguments), the constants `ANTIGRAVITY_PLAN_MODE = 'plan'` and `ANTIGRAVITY_ACCEPT_EDITS_MODE = 'accept-edits'`, and the helper `antigravityModeFlags(role)`.\n\nThe exact behaviour to pin, re-verified in the source before assertions are written:\n- `launch()` builds `[...(resume ? (resumeSessionId?.length ? ['--conversation', id] : ['-c']) : []), '--model', model, ...(effort ? ['--effort', effort] : []), ...antigravityModeFlags(role), ...runDirGrant(runId), '--prompt-interactive', prompt]`.\n- `attach()` builds exactly `['--conversation', sessionId, ...antigravityModeFlags(role), ...runDirGrant(runId)]` — six elements, no prompt.\n- Three facts separate agy from both siblings: (a) there is NO leading subcommand, so unlike opencode's `run` the resume discriminator sits at index 0, exactly like the claude test — copy the claude test's index arithmetic here, not opencode's shifted-by-one version; (b) agy DOES emit `--add-dir` via `runDirGrant`, so the run-dir grant is asserted PRESENT (the opposite of the opencode test's absence block); (c) `req.sessionId` is dropped on a fresh launch, same degrade as opencode.\nNo source file is modified by this todo.",
      "files": [
        "test/adapter.claude.test.ts",
        "test/adapter.opencode.test.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/adapter.ts",
        "src/adapter/permissions.ts"
      ]
    },
    {
      "title": "Create test/adapter.antigravity.test.ts with the mirrored scaffolding",
      "detail": "New file, one only. Imports: `import * as assert from 'assert';`, `import { AntigravityAdapter, ANTIGRAVITY_PLAN_MODE, ANTIGRAVITY_ACCEPT_EDITS_MODE, antigravityModeFlags } from '../src/adapter/antigravity';`, `import type { LaunchRequest } from '../src/adapter/adapter';`, `import { AGENT_BINARY } from '../src/adapter/adapter';`, `import { isReadOnlyRole } from '../src/adapter/permissions';`, `import { ROLES } from '../src/model/role';`. Import nothing else — `npm run lint` runs eslint over `test` as well as `src`, so an unused import fails the build.\n\nModule doc comment: state that the file mirrors test/adapter.claude.test.ts for the antigravity (`agy`) CLI and pins (1) the probe `{version, ok, reason?}` contract including the non-empty reason on failure (Req 14.2-14.4), (2) the fresh vs `--conversation <id>` vs `-c` launch branches (Req 3.1, 3.2, 13.2, 13.3), (3) attach()'s no-prompt reopen (Req 3.3, 3.4), (4) the `--mode plan|accept-edits` per-role permission mapping and the per-run `--add-dir` grant that agy — unlike opencode — does support (Req 15.1-15.4), and (5) the one documented degrade: `req.sessionId` ignored on a fresh launch.\n\nCopy `req()` and `findPair()` verbatim from the claude test, keeping the claude-shaped bare model id `'sonnet'` (agy takes a bare `--model <m>`, not opencode's `provider/model`); the adapter passes it through unchanged either way, so the point of the assertion is passthrough, not shape. Instantiate as `const adapter = new AntigravityAdapter();` once per describe block — passing a `PermissionMode` is a type error, the flip is claude-specific.",
      "files": [
        "test/adapter.antigravity.test.ts",
        "test/adapter.claude.test.ts",
        "src/adapter/antigravity.ts"
      ]
    },
    {
      "title": "Probe success/failure cases (Req 14.2, 14.3, 14.4)",
      "detail": "`describe('AntigravityAdapter probe shape (Req 14.2, 14.3, 14.4)')`, two cases adapted from the claude/opencode tests:\n\n1. `it('reports ok:false with a non-empty reason and empty version when the CLI is missing')` — save `process.env.PATH`, set it to `''`, `await new AntigravityAdapter().probe()` inside `try`, restore in `finally` (`process.env.PATH = savedPath;`, exact, including the `undefined` case). Assert `typeof result.version === 'string'`, `typeof result.ok === 'boolean'`, `result.ok === false`, `result.version === ''`, `typeof result.reason === 'string'`, `(result.reason as string).length > 0`, and — the one assertion beyond the claude mirror, carried over from the opencode test — `(result.reason as string).includes(AGENT_BINARY.antigravity)`. This last one earns its place here more than anywhere: `describeProbeError()` interpolates `ANTIGRAVITY_BIN`, which is `agy`, NOT the agent id `antigravity`, so deriving the expectation from `AGENT_BINARY.antigravity` both catches a copy-paste slip that left `claude`/`opencode` in the message and pins the id -> binary indirection.\n2. `it('returns a value conforming to the ProbeResult shape regardless of outcome')` — no PATH manipulation; assert the shape and branch on `result.ok` (ok: `version.length > 0` and `reason === undefined`; not ok: non-empty string reason). Must stay outcome-agnostic: agy may or may not be installed on the machine running the suite.\n\nDo not stub `child_process.execFile` and do not add a mocking dependency; the suite has none and the sibling tests drive the real failure path through PATH. Do not write a case around `agy -v` not being a version alias — that is a property of the installed CLI, not of this adapter, and the adapter only ever emits `--version`.",
      "files": [
        "test/adapter.antigravity.test.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/adapter.ts"
      ]
    },
    {
      "title": "Fresh vs resume launch args, including the ignored session id",
      "detail": "`describe('AntigravityAdapter launch session branches (Req 3.1, 3.2, 13.2, 13.3)')`:\n\n1. Fresh launch: `adapter.launch(req({resume: false, sessionId: 'session-xyz'}))`. Assert `spec.shellPath === AGENT_BINARY.antigravity` (i.e. `agy`, not `antigravity` — worth an inline comment); `spec.shellArgs[0] === '--model'` (no leading subcommand, no resume flag); no `-c`, no `--conversation`; and the degrade — `!spec.shellArgs.includes('session-xyz')` plus `!spec.shellArgs.includes('--session-id')` — pinning that agy mints its own conversation id and `req.sessionId` is dropped rather than smuggled in under another flag.\n2. Resume with a prior id: `req({resume: true, resumeSessionId: 'prior-session'})` → `assert.deepStrictEqual(spec.shellArgs.slice(0, 2), ['--conversation', 'prior-session'])` and `!includes('-c')`.\n3. Resume with no prior id: `req({resume: true, resumeSessionId: undefined})` → `spec.shellArgs[0] === '-c'`, exactly one `-c` (`filter(a => a === '-c').length === 1`), no `--conversation`.\n4. Empty-string prior id behaves as \"no prior id\": `req({resume: true, resumeSessionId: ''})` → `shellArgs[0] === '-c'`, no `--conversation`. This pins the second half of the adapter's `!== undefined && .length > 0` guard.\n5. Branch equivalence: fresh vs no-id resume differ only by the prepended `-c` — `assert.deepStrictEqual(fresh.shellArgs, resumed.shellArgs.slice(1))`. Note this is NOT the opencode `slice(1)/slice(2)` pair and NOT the claude `slice(2)/slice(1)` pair: agy's fresh branch prepends nothing at all, so the fresh array is compared whole.\n6. Model/effort tail: with `effort: 'high'`, assert `findPair(args, '--model', 'sonnet') >= 0` and `findPair(args, '--effort', 'high') >= 0`; with `effort` omitted, `!args.includes('--effort')`. Add one case with `effort: ''` asserting `!args.includes('--effort')`, matching the adapter's `!== undefined && .length > 0` guard.\n7. Prompt tail: `assert.strictEqual(args[args.length - 1], request.prompt)`, `assert.strictEqual(args[args.length - 2], '--prompt-interactive')`, and `assert.ok(!args.includes('--'))` — the prompt is the flag's value, not a positional behind a `--` separator (agy's shape differs from both claude's `-- <prompt>` and opencode's bare trailing positional, so this is a real contract, not padding).",
      "files": [
        "test/adapter.antigravity.test.ts",
        "src/adapter/antigravity.ts"
      ]
    },
    {
      "title": "attach() as an exact array (Req 3.3, 3.4)",
      "detail": "`describe('AntigravityAdapter attach() (Req 3.3, 3.4)')`:\n\n1. `adapter.attach({role: 'executor', runId: 'run-9', sessionId: 'session-42'})` — the output is short and fully determined, so assert the whole array: `assert.deepStrictEqual(spec.shellArgs, ['--conversation', 'session-42', '--mode', ANTIGRAVITY_ACCEPT_EDITS_MODE, '--add-dir', '.baiton/runs/run-9/'])`, plus `spec.shellPath === AGENT_BINARY.antigravity`. Then the negative contracts: `spec.shellArgs.length === 6`, no prompt is appended (`!includes('--prompt-interactive')`, `!includes('--model')`, `!includes('-c')`).\n2. A read-only role: `attach({role: 'planner', runId: 'run-1', sessionId: 'session-1'})` → `findPair(args, '--mode', ANTIGRAVITY_PLAN_MODE) >= 0` and `findPair(args, '--add-dir', '.baiton/runs/run-1/') >= 0`.\n\nWrite the run-dir path as the literal `.baiton/runs/<runId>/` rather than calling `runDirGrant(runId)` to build the expectation: the claude test uses the literal for the same reason, and a test that derives its expectation from the same helper the adapter calls cannot detect a change in that helper's output.",
      "files": [
        "test/adapter.antigravity.test.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/permissions.ts"
      ]
    },
    {
      "title": "The --mode permission mapping and the per-run --add-dir grant, for every role",
      "detail": "`describe('AntigravityAdapter role -> --mode permission mapping (Req 15.1-15.3)')` — this block is T10's analogue of the claude test's permission-table block and the opencode test's `--agent` block.\n\n1. Pin the constants once so the rest of the file can reference them without going vacuous: `assert.strictEqual(ANTIGRAVITY_PLAN_MODE, 'plan')` and `assert.strictEqual(ANTIGRAVITY_ACCEPT_EDITS_MODE, 'accept-edits')`. Add an explicit `assert.notStrictEqual(ANTIGRAVITY_ACCEPT_EDITS_MODE, 'acceptEdits')`-style guard with a comment: agy's hyphenated `accept-edits` is a DIFFERENT string from claude's camelCase `acceptEdits` (`ACCEPT_EDITS_MODE` in permissions.ts), and mixing them up is the single most plausible copy-paste defect in this file.\n2. Loop `for (const role of ROLES)` and assert, for both `launch(req({role}))` and `attach({role, runId: 'run-1', sessionId: 's-1'})`, that `findPair(args, '--mode', isReadOnlyRole(role) ? ANTIGRAVITY_PLAN_MODE : ANTIGRAVITY_ACCEPT_EDITS_MODE) >= 0`, and that exactly one `--mode` appears. Driving the expectation from `isReadOnlyRole` rather than a hand-written role list means a role later added to `READ_ONLY_ROLES` is covered automatically.\n3. Exercise the exported helper directly: `assert.deepStrictEqual(antigravityModeFlags('planner'), ['--mode', ANTIGRAVITY_PLAN_MODE])` and the `'executor'` equivalent.\n4. Run-dir grant, for every role, on both launch and attach: `findPair(args, '--add-dir', '.baiton/runs/run-777/') >= 0` with `req({role, runId: 'run-777'})`. State in a comment that this is the deliberate difference from opencode — agy supports `--add-dir`, so Requirement 15.4's per-run write scoping IS emitted here and must be asserted present, not absent.\n5. Forbidden vocabulary, for every role and both call paths: the args contain none of `'--dangerously-skip-permissions'` (the OVERVIEW rules it out explicitly, so a future edit reaching for it fails a test rather than passes review), `'--allowedTools'`, `'--permission-mode'`, `'--agent'`, `'--auto'`, `'--sandbox'`. Include the note that agy has no scoped allow-list, so read-only roles rely on `--mode plan` refusing edits outright — there is no `readOnlyFallbackToAcceptEdits` flip to test because `plan` is agy's only non-edit mode.",
      "files": [
        "test/adapter.antigravity.test.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/permissions.ts",
        "src/model/role.ts"
      ]
    },
    {
      "title": "Verify: run the new file, then lint, compile and the full suite",
      "detail": "1. `npx mocha test/adapter.antigravity.test.ts` — note the finding recorded in T09's execute result: `.mocharc.json`'s `spec` glob (`test/**/*.test.ts`) overrides a file argument on the CLI, so this runs the WHOLE suite either way; read the new describe blocks in the output to confirm the new cases pass. To actually isolate the file, use `npx mocha --spec 'test/adapter.antigravity.test.ts' --config /dev/null --require ts-node/register` or simply `npx mocha --grep AntigravityAdapter`; `--grep` is the cheap reliable option.\n2. `npm run lint` — eslint covers `test`; unused imports and unused vars are the realistic failures in a new test file.\n3. `npm run compile` (`tsc -p ./`) — type-checks the test tree; `req({role})` inside `for (const role of ROLES)` types `role` as `Role`, which is what `LaunchRequest.role` needs.\n4. `npm test` — the full suite must stay green (T09's baseline was 449 passing / 1 pending). Be aware of the pre-existing flake T09 recorded: `planner context confinement (property)` in test/plannerContext.confinement.property.test.ts failed once on a random counterexample and passed on every rerun. If it fails, rerun once and record it as pre-existing rather than chasing it.\n5. Scope discipline: create exactly one new file. Do not modify src/adapter/antigravity.ts even if a test exposes something arguable (record it in the execution summary instead); do not touch test/adapter.claude.test.ts, test/adapter.opencode.test.ts or test/adapter.launch.property.test.ts; do not write the codex test (T11) or start the cross-cutting sweep (T12).",
      "files": [
        "test/adapter.antigravity.test.ts",
        ".mocharc.json",
        "package.json"
      ]
    }
  ],
  "risks": [
    "The likeliest defect is importing the wrong sibling's index arithmetic. agy has NO leading subcommand: the fresh launch starts at `--model` (index 0) and the resume discriminator is at index 0, exactly like claude and one position left of opencode's `run`-prefixed args. Copying test/adapter.opencode.test.ts's `slice(0, 3)` / `slice(1)` vs `slice(2)` shapes yields assertions that fail for the wrong reason or pass vacuously.",
    "The second likeliest defect is conflating agy's `accept-edits` with claude's `acceptEdits`. Both live in the repo (`ANTIGRAVITY_ACCEPT_EDITS_MODE` vs permissions.ts's `ACCEPT_EDITS_MODE`), differ only by a hyphen, and a wrong literal would silently weaken the mapping assertions — hence the explicit constant-value pins and the notStrictEqual guard.",
    "Asserting `--add-dir` PRESENT here is the inverse of the opencode test's absence block. An executor working from the T09 file as a template may carry the absence assertions across, which would fail against agy's real behaviour and might tempt an out-of-scope edit to src/adapter/antigravity.ts. The adapter is correct: agy supports `--add-dir` and the grant is not a degrade.",
    "The probe failure case depends on `PATH=''` producing ENOENT. On some POSIX systems an empty PATH falls back to a confstr default, so if `agy` happens to be installed there the case could fail. The claude and opencode tests carry the same assumption, so mirroring it is the consistent choice; if it proves flaky the fix is to point PATH at an empty temp directory, not to weaken the assertions.",
    "Symmetrically, the suite's host may or may not have `agy` installed, so no case may assume the probe succeeds — only the outcome-agnostic shape case runs without PATH manipulation, and it must branch on `result.ok`.",
    "`AntigravityAdapter`'s constructor takes no arguments; there is no `readOnlyFallbackToAcceptEdits` flip because `plan` is agy's only non-edit mode. Mirroring the claude test's `new ClaudeAdapter(fallbackMode)` blocks would be a type error and conceptually wrong — the analogue is the `--mode plan|accept-edits` table.",
    "attach()'s exact-array assertion is the tightest in the file and breaks on any benign reordering of agy's flags. That is intended for a six-element deterministic list, but the launch-side assertions should stay `findPair`/positional so that adding a future flag does not break unrelated cases.",
    "Scope creep toward T11/T12 is easy: a shared helper module for the three new adapter tests, or a parameterised suite over all adapters, would pull codex into this todo. Keep the file standalone and duplicate the small `req`/`findPair` helpers, exactly as the claude and opencode tests do."
  ],
  "acceptance": [
    "test/adapter.antigravity.test.ts exists, is picked up by the `test/**/*.test.ts` spec glob in .mocharc.json, and uses only mocha + node `assert` with no new dependency.",
    "Probe coverage: one case forces `ok:false` with `version === ''` and a non-empty reason containing `AGENT_BINARY.antigravity` (i.e. `agy`), and one case asserts the `{version, ok, reason?}` shape holds regardless of whether the CLI is installed; PATH is restored in a `finally`.",
    "Launch coverage: a fresh launch has no leading subcommand and starts at `--model`, carries no `-c`/`--conversation`, and provably drops `req.sessionId`; resume with a prior id leads `['--conversation', <id>]`; resume with an absent OR empty prior id leads with exactly one `-c`; and `fresh.shellArgs` deep-equals `resumed.shellArgs.slice(1)`.",
    "The model passes through verbatim as `--model <m>`, `--effort <e>` appears exactly when `req.effort` is a non-empty string (absent for both `undefined` and `''`), and the prompt is the value of a trailing `--prompt-interactive` flag with no `--` separator anywhere in the args.",
    "attach() is asserted as the exact array `['--conversation', <id>, '--mode', <mode>, '--add-dir', '.baiton/runs/<runId>/']` with `shellPath === AGENT_BINARY.antigravity`, no prompt flag and no model flag.",
    "The `--mode` mapping is asserted for every role in ROLES, for both launch and attach, with the expected value derived from `isReadOnlyRole` rather than a hand-written list; `antigravityModeFlags` is exercised directly; and the constants are pinned as `'plan'` / `'accept-edits'` with an explicit guard against claude's camelCase `acceptEdits`.",
    "For every role, on both launch and attach, the per-run `--add-dir .baiton/runs/<runId>/` grant is asserted PRESENT (agy's difference from opencode), and the args are asserted to contain no `--dangerously-skip-permissions`, `--allowedTools`, `--permission-mode`, `--agent`, `--auto` or `--sandbox`.",
    "`npm run lint`, `npm run compile` and `npm test` pass (allowing one rerun for the pre-existing plannerContext.confinement property flake, which must be reported if it recurs); no file under src/ is modified and no other test file is touched.",
    "The codex adapter test (T11) and the cross-cutting suite sweep (T12) are not started."
  ]
}
```
