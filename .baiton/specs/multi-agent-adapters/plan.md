# plan result

```json
{
  "steps": [
    {
      "title": "Re-verify the codex CLI surface before writing any args",
      "detail": "codex is third-party and self-updating, so re-run the probes first. Verified during planning against the installed binary at /home/maindric/.local/bin/codex: `codex --version` prints the PREFIXED string `codex-cli 0.154.0` (not bare like agy/opencode) and exits 0; `-V` is the short alias. `codex --help` confirms `Usage: codex [OPTIONS] [PROMPT]` with the interactive TUI as the default when no subcommand is given, and confirms every flag this task needs on the top-level command: `-m/--model <MODEL>`, `-s/--sandbox <read-only|workspace-write|danger-full-access>`, `-a/--ask-for-approval <on-request|never>`, `--add-dir <DIR>` ('Additional directories that should be writable alongside the primary workspace'), and `-c/--config <key=value>` ('Override a configuration value that would otherwise be loaded from ~/.codex/config.toml ... The value portion is parsed as TOML. If it fails to parse as TOML, the raw string is used as a literal'). `codex resume --help` confirms `Usage: codex resume [OPTIONS] [SESSION_ID] [PROMPT]` and that resume carries the SAME `-m`, `-s`, `-a`, `--add-dir` and `-c` options, plus `--last` ('Continue the most recent session without showing the picker'). There is NO `--effort` / reasoning-effort flag anywhere (`codex --help | grep -ci effort` == 0), which is why the effort degrade in the next steps goes through `-c`. Three short-flag collisions to keep straight because they differ from claude/agy/opencode: codex's `-c` is `--config` (NOT continue), codex's `-i` is `--image` (NOT interactive), codex's `-s` is `--sandbox` (NOT session). Prefer the long form of every flag in the emitted args so this can never be misread. Deliberately out of scope: `codex exec` (non-interactive one-shot that exits — the wrong shape for a terminal the user watches and interjects in), `--dangerously-bypass-approvals-and-sandbox`, `--dangerously-bypass-hook-trust`, `--approve-for-me`, `--worktree`, `-C/--cd`, `--search`, `--oss`, `-p/--profile`.",
      "files": [
        "src/adapter/adapter.ts"
      ]
    },
    {
      "title": "Create src/adapter/codex.ts scaffolded on the antigravity/opencode/claude shape",
      "detail": "Add exactly one new file `src/adapter/codex.ts` exporting `export class CodexAdapter implements Adapter` with `readonly id = 'codex' as const`. Mirror the layout T02/T03 landed in src/adapter/opencode.ts and src/adapter/antigravity.ts so all four adapters read identically: imports (`execFile` from 'child_process'; `import type { Adapter, LaunchRequest, LaunchSpec, ProbeResult } from './adapter'`; `import { AGENT_BINARY } from './adapter'`; `import type { Role } from '../model/role'`; `import { isReadOnlyRole, runDirGrant } from './permissions'`), then `const CODEX_BIN = AGENT_BINARY.codex;` (resolves to 'codex' — do NOT hard-code the literal; T01's map is the single source of truth), `const PROBE_TIMEOUT_MS = 10_000;`, the exported sandbox/approval constants and helper (next step), the class, and a module-private `describeProbeError` helper at the bottom. Constructor takes no arguments, matching OpencodeAdapter and AntigravityAdapter: claude's `PermissionMode` / `readOnlyFallbackToAcceptEdits` flip has no codex analogue and accepting the parameter would advertise a knob that does nothing. T05's registry will construct it with `new CodexAdapter()`.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/opencode.ts",
        "src/adapter/claude.ts",
        "src/adapter/adapter.ts"
      ]
    },
    {
      "title": "Add the role -> sandbox/approval mapping as named exports",
      "detail": "Add `export const CODEX_READ_ONLY_SANDBOX = 'read-only';`, `export const CODEX_WORKSPACE_WRITE_SANDBOX = 'workspace-write';`, `export const CODEX_ASK_FOR_APPROVAL = 'on-request';` and `export function codexPermissionFlags(role: Role): string[]` returning `['--sandbox', isReadOnlyRole(role) ? CODEX_READ_ONLY_SANDBOX : CODEX_WORKSPACE_WRITE_SANDBOX, '--ask-for-approval', CODEX_ASK_FOR_APPROVAL]`. Derive the classification from `isReadOnlyRole` in './permissions' rather than re-listing roles, so READ_ONLY_ROLES stays the one place that decides (spec-writer, planner, plan-reviewer, pr-writer -> read-only; executor, reviewer -> workspace-write). `--ask-for-approval on-request` is used for EVERY role — it is what lets command-approval prompts surface in the terminal for the user to answer, which is the codex analogue of claude's acceptEdits-mode terminal prompts (Requirement 15.3); `never` is deliberately never emitted because it silently auto-runs, and `danger-full-access` is never emitted at all. Exporting the constants and the helper lets T11 assert by name instead of by string literal, exactly as adapter.claude.test.ts imports ACCEPT_EDITS_MODE / READ_ONLY_ALLOWED_TOOLS and as opencode.ts/antigravity.ts export their own helpers. Note these are codex's own hyphenated spellings and are NOT the same strings as permissions.ts's claude-facing `ACCEPT_EDITS_MODE = 'acceptEdits'` or antigravity.ts's `'accept-edits'` — define codex's own constants here and do not import or reuse either.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/permissions.ts",
        "src/adapter/antigravity.ts"
      ]
    },
    {
      "title": "Add the effort degrade as a named export",
      "detail": "codex exposes no `--effort` flag, so map `req.effort` through codex's generic config override: add `export const CODEX_EFFORT_CONFIG_KEY = 'model_reasoning_effort';` and `export function codexEffortFlags(effort: string | undefined): string[]` returning `[]` when `effort` is undefined or empty, else `['--config', `${CODEX_EFFORT_CONFIG_KEY}=${effort}`]` (the long `--config` form, not the `-c` short alias, so it is never confused with a continue flag). The key name was verified during planning against the user's live `~/.codex/config.toml`, which contains `model_reasoning_effort = \"medium\"` at the top level — it is a real top-level config key, not an inferred one. Emit the value UNQUOTED (`model_reasoning_effort=high`): codex parses the value portion as TOML and falls back to the raw string literal when that fails, which is the documented behavior and yields the string `high`. Do not validate or remap the effort value — that is the role config's job, same as claude's `--effort`.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/adapter.ts"
      ]
    },
    {
      "title": "Document the deliberate degrades and the permission mapping in the class doc comment",
      "detail": "Give the class a doc comment in the same register as AntigravityAdapter's, recording the tested version (`codex-cli` v0.154.0) and: (1) codex assigns its own session UUID at start and exposes no flag to pre-assign one, so `launch()` deliberately ignores `req.sessionId` when `req.resume` is false — the journal's recorded session id will not match codex's actual session for a fresh launch (same degrade as opencode and agy). (2) The interactive form `codex [OPTIONS] [PROMPT]` is used, NOT the `codex exec` subcommand, because `exec` is a non-interactive one-shot that exits — the wrong shape for a terminal the user watches and interjects in. (3) There is no reasoning-effort flag; effort is degraded onto `--config model_reasoning_effort=<effort>`, codex's generic config override. (4) Resume with no known prior id uses `codex resume --last` and DROPS the prompt: verified against v0.154.0, `codex resume --last <text>` binds `<text>` to the SESSION_ID positional, not PROMPT (clap's own error usage line renders as `codex resume --last <SESSION_ID> [PROMPT]`), so passing the prompt there would silently be read as a session name and the resume would target a session that does not exist. (5) Read-only roles get `--sandbox read-only`, which is a whole-session sandbox rather than claude's scoped `Write(.baiton/runs/**)` allow-list, so the Requirement 15.4 run-dir grant is emitted via `--add-dir` but may not actually make the run dir writable under `read-only` — see the risks; the documented fallback if that proves true is to widen read-only roles to `--sandbox workspace-write`, which is a one-constant change here, not a plumbing rewrite. (6) `--dangerously-bypass-approvals-and-sandbox`, `--dangerously-bypass-hook-trust`, `--approve-for-me`, `--sandbox danger-full-access` and `--ask-for-approval never` are deliberately never emitted.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/opencode.ts"
      ]
    },
    {
      "title": "Implement probe() against `codex --version`",
      "detail": "Copy the probe/runVersion pair from antigravity.ts verbatim in shape, substituting CODEX_BIN: a promise-wrapped private `runVersion()` calling `execFile(CODEX_BIN, ['--version'], { timeout: PROBE_TIMEOUT_MS, windowsHide: true }, (error, stdout) => ...)` that rejects on `error` and resolves `stdout`; `probe()` trims the stdout, returns `{ version: '', ok: false, reason: `${CODEX_BIN} --version produced no version output` }` when the trimmed string is empty, else `{ version: trimmed, ok: true }`, and on throw returns `{ version: '', ok: false, reason: describeProbeError(e) }`. `describeProbeError(e)` maps an ENOENT `code` to `` `${CODEX_BIN} was not found on PATH` ``, an `Error` with a non-empty message to `` `${CODEX_BIN} --version failed: ${e.message}` ``, and anything else to `` `${CODEX_BIN} --version failed` `` — satisfying Requirements 14.2-14.4 (non-empty reason whenever `ok` is false). Keep the whole trimmed stdout as the version string rather than parsing out a number: codex prints the prefixed `codex-cli 0.154.0`, and the OVERVIEW explicitly calls for the same tolerant handling the claude adapter already uses. Use the long `--version` form (codex also accepts `-V`, but the long form keeps the four adapters identical).",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/claude.ts"
      ]
    },
    {
      "title": "Implement launch() with the fresh / resume-with-id / resume-without-id branches",
      "detail": "Unlike the other three adapters, codex's resume is a SUBCOMMAND, not a leading flag, so the three branches differ in their leading argv entries rather than only in one pushed pair. Build `args: string[] = []` and branch first: if `req.resume` and `req.resumeSessionId` is a non-empty string, push `'resume', req.resumeSessionId`; else if `req.resume`, push `'resume', '--last'`; else push nothing (fresh launch — `req.sessionId` is deliberately dropped, see the degrade doc comment). Then the shared options, in the same order as the sibling adapters: `args.push('--model', req.model)` passing `req.model` through byte-for-byte unchanged; `args.push(...codexEffortFlags(req.effort))`; `args.push(...codexPermissionFlags(req.role))`; `args.push(...runDirGrant(req.runId))` — reuse permissions.ts's existing helper unchanged, codex's `--add-dir <DIR>` matches the emitted `['--add-dir', '.baiton/runs/<run-id>/']` shape exactly and is accepted on both the top-level command and `resume`. Finally the prompt positional: push `'--', req.prompt` EXCEPT on the resume-without-id branch, where the prompt must be omitted entirely (pushing it there would bind it to SESSION_ID, verified in planning — see the doc comment and risks). The `--` end-of-options marker is required for the same reason the claude adapter uses one: the prompt is a trailing positional after variadic-looking options and may itself start with `-`; codex is clap-parsed and its own error output tips `use '-- --nope'`, confirming `--` is honored. Return `{ shellPath: CODEX_BIN, shellArgs: args }` and set no `env`.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/adapter.ts",
        "src/adapter/permissions.ts",
        "src/adapter/claude.ts"
      ]
    },
    {
      "title": "Implement attach() as a no-prompt `codex resume <id>`",
      "detail": "`attach(req: { role: Role; runId: string; sessionId: string }): LaunchSpec` returns `{ shellPath: CODEX_BIN, shellArgs: ['resume', req.sessionId, ...codexPermissionFlags(req.role), ...runDirGrant(req.runId)] }` — the resume subcommand with the session id as its SESSION_ID positional, the same role-derived sandbox/approval flags, the same run-dir grant, no `--model` (attach reopens an existing session and takes no model), no effort override, and crucially NO trailing prompt and no `--` marker, so the session reopens with no new message (Requirements 3.3, 3.4). PROMPT is optional on `codex resume`, so omitting it is valid. This mirrors ClaudeAdapter.attach's `['--resume', id, ...permissionFlags, ...runDirGrant]` and AntigravityAdapter.attach's `['--conversation', id, ...modeFlags, ...runDirGrant]` one-for-one. Every field of `req` is used here, so `noUnusedParameters` (enabled in tsconfig.json) is satisfied without the deliberate-unused note opencode.ts needed.",
      "files": [
        "src/adapter/codex.ts",
        "src/adapter/adapter.ts",
        "src/adapter/antigravity.ts"
      ]
    },
    {
      "title": "Touch nothing else — index.ts, the registry, the engine, package.json and the test are later tasks",
      "detail": "T04's deliverable is exactly one new file and no edit to any existing file, the same discipline T02 and T03 followed and their reviews passed on. Do NOT add `export * from './codex'` to src/adapter/index.ts (that is T05, together with the agent-id -> Adapter registry), do not touch src/activation/commands.ts, src/engine/* or src/activation/executable.ts (T06/T07), do not add the `baiton.agents.codex.path` setting to package.json (T08), and do not add test/adapter.codex.test.ts (T11). Keeping the blast radius to one unimported file is what makes the claude-only default path provably unchanged. In particular, do NOT extract the now-quadruplicated probe/runVersion/describeProbeError block into a shared helper in this task — that refactor touches claude.ts, opencode.ts and antigravity.ts and belongs to T12 (see risks).",
      "files": [
        "src/adapter/index.ts",
        "src/adapter/codex.ts"
      ]
    },
    {
      "title": "Type-check, lint and run the existing suite",
      "detail": "Run `npm run compile` (tsc), `npm test`, and `npx eslint src/adapter/codex.ts --ext .ts` — the exact three commands T02 and T03 ran. Expect zero behavior change and zero new failures: nothing imports codex.ts yet, so the only realistic failure is a compile error inside the new file (an `Adapter` interface mismatch, an unused import tripping `noUnusedLocals`, or an unused parameter tripping `noUnusedParameters` — both are on in tsconfig.json). The baseline to match is T03's recorded result (T02's was 409 passing, 1 pending; the pending test is pre-existing and unrelated — re-read .baiton/specs/multi-agent-adapters/review-1.md or re-run on the pre-change tree if the number has moved). Do not invoke the real `codex` binary from the build or tests; probe failure behavior is exercised by T11 with an emptied `process.env.PATH`, the way adapter.claude.test.ts already does.",
      "files": [
        "src/adapter/codex.ts",
        "test/adapter.claude.test.ts"
      ]
    }
  ],
  "risks": [
    "VERIFIED PARSER HAZARD: `codex resume --last <text>` binds `<text>` to the SESSION_ID positional, not PROMPT — confirmed in planning, where appending an unknown flag made clap print `Usage: codex resume --last <SESSION_ID> [PROMPT]` after it had already consumed the text as SESSION_ID. There is therefore NO way to pass a prompt on the resume-without-known-id branch, and this plan drops the prompt there. That branch is the codex analogue of claude's `-c` fallback, and dropping the prompt means the stage reopens a session with no new instruction — the agent will sit idle rather than do the stage's work. The branch is only reached when `resume` is true and the journal has no prior Session_Id, which is already a degenerate state, but the engine must not treat it as a working resume. The alternative (fall back to a fresh interactive launch that keeps the prompt but loses session continuity) is a defensible different choice; raise it at T06/T12 rather than deciding it inside this adapter.",
    "`--sandbox read-only` for read-only roles is a whole-session sandbox, not claude's scoped `Write(.baiton/runs/**)` allow-list. Every stage must write its result.json into `.baiton/runs/<run-id>/`, and it is UNVERIFIED whether `--add-dir .baiton/runs/<run-id>/` actually makes that directory writable while the sandbox is `read-only` (codex's help describes `--sandbox` as governing 'model-generated shell commands' and `--add-dir` as directories 'writable alongside the primary workspace', which reads as a workspace-write-mode concept). If read-only blocks the result write, EVERY codex read-only stage (spec-writer, planner, plan-reviewer, pr-writer) fails to produce its result file. This is the single most consequential unknown in T04 and must be manually verified during T11 or T12 by launching a planner role under codex and confirming it can write result.json. The fix, if needed, is to widen read-only roles to `--sandbox workspace-write` — a one-constant change to CODEX_READ_ONLY_SANDBOX's use site — not an ad-hoc escape inside the adapter. This is the exact same tension T03 flagged for agy's `--mode plan` + `--add-dir`.",
    "`req.sessionId` is dropped on a fresh launch because codex mints its own session UUID and has no pre-assign flag. The engine records the generated Session_Id in the journal start record (Requirement 3.1) and later uses it for resume and attach; for codex roles that recorded id will not match codex's real session, so a later `codex resume <id>` or `attach()` targets a session that does not exist. The adapter's silence is the honest behavior here — the reconciliation (capturing the CLI-minted id, or marking the journal id unusable for non-claude agents) belongs to T06/T07 and must be raised there. This degrade is now shared by three of the four adapters (opencode, antigravity, codex) and deserves one cross-cutting decision rather than three separate ones.",
    "The effort mapping `--config model_reasoning_effort=<effort>` is a generic config override, not a dedicated flag, so codex will NOT reject an invalid or misspelled effort value the way a typed `--effort low|medium|high` would — a bad role-config effort silently produces a config key with a nonsense value, or (if codex validates the key's value at load) an error whose message points at config.toml rather than at Baiton's role config. The key itself was confirmed real (it is present in the live ~/.codex/config.toml), but its accepted value set was not enumerated from codex's own docs; worth a `codex doctor` / config-docs check at T11 or T12.",
    "Short-flag collisions across the four CLIs are a live source of copy-paste bugs: codex's `-c` is `--config` (claude's and agy's `-c` is continue), codex's `-i` is `--image` (opencode's and agy's `-i` is interactive), codex's `-s` is `--sandbox` (opencode's `-s` is session). This plan emits only long forms, which removes the hazard from the shipped args — but a reviewer skimming the four adapters side by side, or a future edit that reaches for a short flag, can still reintroduce it.",
    "Findings were taken against `codex-cli` v0.154.0 and re-verified during planning via `codex --version`, `codex --help` and `codex resume --help` (all of `--model`, `--sandbox`, `--ask-for-approval`, `--add-dir`, `--config` and `--last` confirmed present on both the top-level command and the resume subcommand, and no reasoning-effort flag confirmed absent). codex is third-party and self-updating; the implementer should re-run all three commands before writing the args and adjust if the surface has changed.",
    "The probe / runVersion / describeProbeError block is now quadruplicated across claude.ts, opencode.ts, antigravity.ts and codex.ts. Duplicating it once more is still the right call for T04 — it keeps this task to one new file and disturbs no existing test — but with all four adapters landed, the extraction of a shared `probeVersion(bin)` helper into adapter.ts is now plainly warranted and should be raised as a T12 cleanup.",
    "`codex resume` accepts a session NAME as well as a UUID ('UUIDs take precedence if it parses'), so a malformed or non-UUID Session_Id reaching `attach()` or the resume branch will be interpreted as a session name and fail with a name-lookup error rather than an obviously-wrong-id error — a confusing failure mode if the T06/T07 session-id reconciliation above is not resolved."
  ],
  "acceptance": [
    "src/adapter/codex.ts exists and exports `CodexAdapter implements Adapter` with `id === 'codex'`; `npm run compile` passes with no edit to any other file in the repo.",
    "`shellPath` is `AGENT_BINARY.codex` (resolving to 'codex'), sourced from the T01 canonical map rather than a hard-coded string literal.",
    "probe() runs `codex --version`, returns the trimmed stdout as `version` with `ok: true` on success (the whole prefixed string `codex-cli <v>`, not a parsed-out number), and returns `{ version: '', ok: false }` with a non-empty `reason` on every failure path (verifiable by probing with an emptied `process.env.PATH`, as adapter.claude.test.ts does).",
    "A fresh launch (`resume: false`) produces `['--model', <model>, (…'--config', 'model_reasoning_effort=<effort>'), '--sandbox', <read-only|workspace-write>, '--ask-for-approval', 'on-request', '--add-dir', '.baiton/runs/<runId>/', '--', <prompt>]` — with no `resume` subcommand, no `--last`, and `req.sessionId` appearing nowhere in the args.",
    "A resume launch with a known prior id leads with `['resume', <resumeSessionId>]` and still ends with `['--', <prompt>]`; a resume with no prior id leads with `['resume', '--last']` and contains NO prompt text and no `--` marker anywhere in the args.",
    "`--config model_reasoning_effort=<effort>` is present exactly when `req.effort` is a non-empty string and absent otherwise; the long `--config` form is used, never the `-c` alias; and `req.model` reaches `--model` byte-for-byte unchanged.",
    "Read-only roles (spec-writer, planner, plan-reviewer, pr-writer) get `--sandbox read-only`; executor and reviewer get `--sandbox workspace-write`; every role gets `--ask-for-approval on-request`; the classification comes from `isReadOnlyRole` in src/adapter/permissions.ts and the strings are codex's own constants, distinct from permissions.ts's 'acceptEdits' and antigravity.ts's 'accept-edits'.",
    "Every launch and attach emits the run-dir grant by calling the existing `runDirGrant(req.runId)` from permissions.ts — unchanged and unduplicated — and `codex exec`, `--dangerously-bypass-approvals-and-sandbox`, `--dangerously-bypass-hook-trust`, `--approve-for-me`, `--sandbox danger-full-access`, `--ask-for-approval never`, `--worktree`, `-C/--cd` and `--search` are never emitted.",
    "attach() returns `['resume', <sessionId>, '--sandbox', <read-only|workspace-write>, '--ask-for-approval', 'on-request', '--add-dir', '.baiton/runs/<runId>/']` with no prompt text, no `--` marker, no `--model` and no effort override anywhere in the args.",
    "The class doc comment explicitly records: the ignored `req.sessionId` on fresh launches, the interactive-form-not-`codex exec` choice and why, the `--config model_reasoning_effort` effort degrade, the verified `resume --last` prompt-binding hazard and the resulting dropped prompt, the `--sandbox read-only` vs scoped-allow-list gap with its one-constant fallback, and the never-emitted dangerous flags — plus the tested version `codex-cli 0.154.0`.",
    "`npm test` matches the T03 baseline with no new failures, and `npx eslint src/adapter/codex.ts --ext .ts` is clean.",
    "src/adapter/index.ts, src/adapter/claude.ts, src/adapter/opencode.ts, src/adapter/antigravity.ts, src/adapter/permissions.ts, src/adapter/adapter.ts, the engine, src/activation/* and package.json are all untouched by this task, and no test file is added."
  ]
}
```
