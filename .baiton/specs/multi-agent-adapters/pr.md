# pr result

```json
{
  "title": "Add multi-agent adapters: opencode, antigravity, codex",
  "body": "## Summary\n\nExtends Baiton's adapter layer to support the opencode, antigravity (agy), and codex CLIs in addition to claude, with no changes to claude's behavior or default config. Roles can now be individually configured with different agents via their `agent` id.\n\n## What Changed\n\n**Adapter implementations:**\n- Added `src/adapter/opencode.ts` — OpenCode CLI adapter with probe, launch (fresh and resume), and attach support\n- Added `src/adapter/antigravity.ts` — Antigravity (agy) CLI adapter with `--mode` permission mapping\n- Added `src/adapter/codex.ts` — Codex CLI adapter with `codex resume` and config-override effort degradation\n\n**Engine and activation wiring:**\n- Widened `AgentId` union in `src/adapter/adapter.ts` to include claude, opencode, antigravity, codex\n- Added canonical `AGENT_BINARY` map (agent id → CLI binary name) shared by adapters and executable resolution\n- Created `AdapterRegistry` seam in `src/adapter/index.ts` for per-role adapter lookup\n- Replaced single injected adapter in `RunQueue`, `SpecDraft`, `SubmitPr` with per-role `adapterForRole(role)` resolver\n- Extended executable resolution to resolve one binary per distinct agent id and gate dispatch per role\n- Updated `src/activation/commands.ts` to route through `adapterForRole` instead of sharing a single `ClaudeAdapter` instance\n\n**Config and package.json:**\n- Added `baiton.agents.opencode.path`, `baiton.agents.antigravity.path`, `baiton.agents.codex.path` settings for executable overrides\n\n**Test coverage:**\n- Added new block in `test/adapter.claude.test.ts` confirming default config still selects claude for all roles and that registry-routed argv is byte-identical to direct construction\n- Added `test/adapter.registry.test.ts` with 13 tests covering `createAdapterRegistry`, `isAgentId`, instance stability, and `PermissionMode` isolation\n- Added unknown-agent branch tests in `test/engine.specDraft.test.ts` and `test/submitPr.test.ts`\n\n## Why\n\nUnlocking multi-agent support lets users run different stages under different CLI agents (plan with the lighter opencode, execute with claude, review with codex). The split preserves existing behavior: default config still uses claude for all six roles; mixed-agent configs work transparently when multiple binaries are present.\n\n## How It Was Verified\n\n- Baseline: npm compile, lint, and test suite (515 passing, 1 pending) already fully green at commit a0f2a1c\n- No src/ breakage fixed (tree was already clean)\n- Added 19 new tests across 4 files covering:\n  - Default claude-only behavior unchanged\n  - Adapter registry id validation and instance stability  \n  - Unknown-agent refusal branches with no probe/terminal creation\n- Re-ran full suite: 534 passing (515 baseline + 19 new), 1 pending (pre-existing), zero failures\n- npm run compile, lint, test, test:unit, test:property all exit 0\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01NQ2g2UQWjaoNfCGPZPHsCc"
}
```
