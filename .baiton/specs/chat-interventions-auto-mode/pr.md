# pr result

```json
{
  "title": "Add inline intervention cards and auto mode for human-in-the-loop asks",
  "body": "## Summary\n\nExtends the Baiton Chat_View to surface every human-in-the-loop decision as an inline, interactive intervention card—orchestrator questions, confirmations, and sub-agent harness permission asks—and adds an Auto mode toggle for deterministic auto-approval of safe asks with auditable escalations.\n\n## What changed\n\n### Intervention core (T01–T03)\n\nA new host-free `Intervention` model in `src/orchestrator/interventions.ts` unifies three kinds of human asks: `question` (options or free text), `confirm` (yes/no), and `permission` (harness ask). A pure pending-ask registry creates, resolves, and rejects asks; an `InterventionSeam` returns a promise that settles when the user answers. The existing `ConfirmSeam` is re-cast as a backwards-compatible adapter over the intervention seam, so `approve_spec`, `draft_spec`, and `submit_pr` keep compiling unchanged.\n\nIntervention records are persisted to the session transcript (`TranscriptRecord.intervention`) so resolved cards remain inline and survive conversation switching, reload, and re-render.\n\n### Protocol & webview (T02, T04–T06)\n\n`webviewProtocol.ts` gains `RenderRecord.intervention` and host-to-webview messages `showIntervention` / `resolveIntervention` / `setAutoMode`, with a pure reducer exhaustively tested. A shared-fixture parity test (`test/webviewProtocol.mirror.test.ts`) pins TypeScript and JavaScript `reduce` to identical output.\n\nThe chat webview renders intervention cards with buttons/radio for option questions, a single-line text input for open answers, Approve/Decline for confirms and permissions, and a settled state showing the decision and rationale. The Auto mode toggle sits immediately left of Stop in the composer control row, staying enabled while busy.\n\n### Orchestrator asks (T07–T08)\n\nChatController now presents orchestrator confirmations as inline intervention cards and pauses the tool loop until the user answers, rejecting all pending asks when Stop is pressed. A new `ask_user` control tool lets the model ask an option or free-text question through the seam and receive the answer as its tool result; system prompt guidance directs the model when to use it.\n\n### Auto mode (T09–T11)\n\nAuto mode evaluates asks in two stages:\n\n1. **Allow-list gate**: A deterministic per-agent list (derived from Claude's `--allowedTools`, OpenCode's permission rules, and the role profile's shell/write bits) auto-approves matching asks with no round-trip.\n2. **Model evaluation**: Everything else goes to the injected `ModelClient`, which returns `approve` with a rationale or `escalate` with a concise what/why-flagged description.\n\nEvery auto-approval and escalation is appended to the transcript as an auditable record. The toggle persists across sessions in `workspaceState` and only gates asks that arrive while it is on.\n\n### Harness relay (T12–T19)\n\nA host-free ask-relay file protocol (`src/engine/askRelay.ts`) uses `.baiton/runs/<run-id>/asks/<ask-id>.json` as the ask and `<ask-id>.response.json` as the answer. Separate todos probed Claude Code (permission hooks), OpenCode (permission ask hooks), Codex (`--ask-for-approval`), and Antigravity for native relay support; a config-driven fallback writes unhandled asks to the watched asks directory.\n\nA VSCode watcher on the run's `asks/` directory routes each ask into the chat as a `permission` card on the run's spec conversation, applies Auto mode, and writes the response back. The run stays paused until answered.\n\n### Documentation (T20)\n\nREADME.md documents inline interventions, the `ask_user` tool, Auto mode behaviour and audit trace, and a per-adapter relay capability findings table that drives which relay each adapter uses.\n\n## How it was verified\n\n- Intervention core: unit tests for the model, registry, seam, and ConfirmSeam adapter covering validation, lifecycle, and edge cases (idempotency, re-entrancy, presentation failure).\n- Protocol: reducer tests, mirror tests pinning TS and JS to identical output, and transcript serialization tests.\n- Webview: controller tests against the fake `ChatWebview` surface.\n- Auto mode: allow-list and model evaluator tests with a fake `ModelClient`.\n- Ask relay: parse/validate tests, adapter-probe findings recorded in README.\n- Integration: all 1163 existing tests passing; no regressions.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
```
