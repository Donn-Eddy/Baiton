# Execute T18

## Summary

T18 config-driven fallback ask relay implemented. Added the probe-derived ASK_RELAY_KIND table (claude = native; opencode/antigravity/codex and unknown ids = config-driven) with askRelayKind()/usesConfigDrivenAskRelay() to src/adapter/index.ts. Added the '# Asking for permission or a decision' section to src/engine/roleInstructions.ts (ASK_RELAY_SECTION_HEADING, ASK_RELAY_NO_SELF_APPROVE_INSTRUCTION, askRelayExampleAsk, askRelayInstruction) with both fenced JSON examples generated by serializeAsk/serializeResponse so they cannot drift from the parser. buildBrief now renders an optional askRelay section between # Context and # Result file, keeping schema and stop last (Req 11.3). launchStage selects the fallback per adapter: config-driven adapters get the section in their Brief, native (claude) Briefs stay byte-identical, ensureAsksDir handling unchanged, and LaunchStageOutput gained askRelayKind. Tests cover all six launcher cases, the pure ordering invariant in the brief-watcher tests, and the selection table for all four ids plus an unknown id. README's relay closing paragraph replaced with the shipped behaviour, stating plainly that the route is instruction-driven, not CLI-enforced.

## Files changed

- `src/adapter/index.ts`
- `src/engine/roleInstructions.ts`
- `src/engine/brief.ts`
- `src/engine/launcher.ts`
- `test/engine.launcher.test.ts`
- `test/engine.briefWatcher.test.ts`
- `test/adapter.index.test.ts`
- `README.md`

## Commands run

- `npm run compile`
- `npm run lint`
- `npx mocha --no-config test/engine.launcher.test.ts test/engine.briefWatcher.test.ts test/engine.askRelay.test.ts test/adapter.index.test.ts --require ts-node/register`
- `npx mocha --no-config test/adapter.claude.test.ts test/adapter.opencode.test.ts test/adapter.codex.test.ts test/adapter.antigravity.test.ts test/adapter.launch.property.test.ts test/askWatcher.routing.test.ts --require ts-node/register`
- `npm test`

## Notes

- npm test: 1156 passing, 1 pending (the pre-existing test/config.test.ts version-gating case); no new failures, no newly pending tests.
- Adapter/launcher suites pass unchanged (244 passing): no adapter argv changed.
- claude Brief with relayAsks is byte-identical to the same launch with the flag omitted (test case 4); the adapter still receives the relay descriptor and askRelayKind is 'native'.
- A relay-less launch produces exactly the previous brief text (pinned against buildBrief output) and no asks/ directory.
- The relay section's two fenced JSON blocks are extracted in tests and asserted to pass parseAsk/parseResponse, pinning the brief example to the wire format.
- No import cycle introduced: roleInstructions.ts imports only types from ../adapter and serializeAsk/serializeResponse from ./askRelay.
- The launcher test's byte-identity case compares the flag-less brief to buildBrief's composed text for the same input (path-normalisation approach was replaced because the flag legitimately adds the section).
