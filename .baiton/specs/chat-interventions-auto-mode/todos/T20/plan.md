# Plan T20

## Steps

1. Scope: README.md only, four documentation targets

   This todo is documentation-only. The single file to change is README.md (currently 286 lines, headings: '# Baiton', '## The Baiton views', '### What the orchestrator does', '### Chat sessions', '### Creating a spec', '### Driving a spec', '### The config panel', '#### Agent Model & Effort Discovery (CLI Probing & Architecture)', '#### Harness ask relay (per-adapter probe findings)', '## Commands', '## Settings'). Do NOT touch source, tests or media files: the behaviour is already shipped by T13-T19 and every sentence written must be traceable to a symbol that exists in the tree. No README test exists in test/, so accuracy is enforced by reading the code, not by an assertion. Keep the file's existing voice: prose paragraphs plus '- **Bold lead-in** — explanation' bullets, hard-wrapped at ~80 columns in the hand-written sections (the relay findings section is long-line and stays as-is; do not reflow it).

   Files: `README.md`

2. Add a '### Interventions in chat' section documenting the inline cards

   Insert a new '### Interventions in chat' section immediately after the '### Chat sessions' section and before '### Creating a spec'. Content, all traceable to src/orchestrator/interventions.ts, src/orchestrator/webviewProtocol.ts (InterventionView), src/activation/chatController.ts and media/chat.js:

   - Lead paragraph: every point where a human is needed appears as one interactive card inline in the conversation and is answered in place — no modal dialog. Three kinds (the `InterventionKind` union): **question** (the orchestrator asking you something through `ask_user`), **confirm** (a confirmation such as the one `draft_spec` and `approve_spec` raise), and **permission** (a sub-agent harness ask relayed out of a running stage, carrying the agent id, the tool name and the tool arguments).
   - Controls per kind, matching media/chat.js `renderIntervention`: an option question renders one button per offered choice (each option has a stable `id` and a `label`, with an optional `detail` line); a free-text question renders a text input plus **Send** (Enter submits, Shift+Enter newlines); confirms and permission asks render **Approve** / **Decline**.
   - Settled state: once answered the card stays in place showing the decision line, an **Auto** badge when Auto mode decided it rather than you, and the one-line rationale (for an escalated ask, the 'what you are approving / why it was flagged' text).
   - Where a card lands: on the conversation the ask belongs to. A relayed harness ask is routed to the spec conversation of the run it came from, switching the view there (ChatController.scopeForAsk).
   - Persistence: cards are appended to the session transcript JSONL as `system` records carrying an `intervention` field (see `interventionTranscriptRecord` in src/orchestrator/chatTranscript.ts), so a settled card is an inline record of the decision that survives switching conversations, reloading the window and re-rendering. An escalated ask is written twice — once pending, once settled — and the two are projected back as the one card in its original position.
   - Stop: **Stop** aborts the in-flight run *and* declines every ask still waiting for an answer (ChatController.onStop -> declinePendingAsks / PendingAskRegistry.rejectAll), so a flow paused on a card always gets control back instead of hanging; a declined `ask_user` comes back to the model as a refusal, and a declined permission ask is written back to the run as a denial.

   Files: `README.md`

3. Document the `ask_user` tool in the orchestrator tool table and prose

   In '### What the orchestrator does': add a row to the existing tools table so the reader sees its phases. The table's shape is '| tools | creating a spec | driving a spec |'; `ask_user` is registered with `phases: ['gather', 'drive']` and `mutating: false` (src/orchestrator/controlTools.ts, askUserTool), so add the row `| `ask_user` | yes | yes |` — place it directly under the first row (`list_specs`, `read_spec`, `git_status`) so the always-available tools sit together. Because it is non-mutating and not a dispatch it also stays usable under Restricted Mode; say so in one clause.

   Then add a short paragraph (either just under the table or as the first bullet of the new Interventions section — pick one place and cross-reference from the other) covering: the orchestrator asks questions with `ask_user(question, options?, allow_free_text?, placeholder?)` instead of ending its turn with a question; the question appears as a card and the call blocks until you answer, so the answer comes back as the tool result and the turn continues; `options` are for a short closed set (at most the tool's `MAX_ASK_USER_OPTIONS` cap — read the constant in src/orchestrator/controlTools.ts and quote the number), `allow_free_text` additionally accepts a typed reply, and a question with no options is always answered by typing; declining the card refuses the call, which the orchestrator quotes and stops on, exactly like any other refusal (see ASK_USER_TEXT in src/orchestrator/systemPrompt.ts, which is the instruction the model actually receives).

   Also fix the now-stale wording in '### Creating a spec' step 3: it says `draft_spec` 'asks you to confirm the requirements'. Make it explicit that this confirmation is now an inline confirm card in the conversation rather than a modal dialog, and that declining it leaves the repository untouched.

   Files: `README.md`

4. Add an '### Auto mode' section covering the toggle, the two-stage gate and the audit trace

   Insert a new '### Auto mode' section after '### Driving a spec' and before '### The config panel' (it reads as part of running work, and it can then point forward at the relay subsection). Content, all traceable to src/orchestrator/autoMode.ts, src/activation/chatController.ts, src/activation/commands.ts and media/chat.js:

   - **The toggle** — a button in the composer's control row immediately left of **Stop**, labelled `Auto: Off` / `Auto: On` (media/chat.js `renderAutoMode`, `aria-pressed`). It stays enabled while a pipeline is running, so it can be flipped mid-run. The host is authoritative: the click posts `setAutoMode` and the button repaints only when the host echoes it back. The state is remembered per workspace in VS Code's `workspaceState` under the key `baiton.chat.autoMode` (src/activation/commands.ts, `autoModeMemory`), so a window reload comes back in the mode you left it in. It is not a `settings.json` setting.
   - **What is gated** — only harness **permission** asks that arrive *while the toggle is on* (ChatController.autoDecision: returns undefined unless `autoMode` is on, a gate is wired and `ask.kind === 'permission'`). Confirmations and questions are never auto-answered: they carry human intent, not a harness capability.
   - **Stage (a), the deterministic allow-list** — a pure per-agent allow-list (`agentAllowList(agent, role, runId)` in src/adapter/index.ts, derived from claude's `--allowedTools` sets, opencode's agent permission rules and the role profile's shell/write bits) decides without any model round-trip. Document its two deliberate narrowings from the module doc-comment: (1) a write auto-approves only when every target path is repo-relative and matches one of the rule's globs — an absolute path, a `file://` URL, any `..` segment, unparseable `args` or an undeterminable target all escalate; (2) `shell: true` alone is not blanket approval — only the recognised read-only/verification prefixes clear the gate (quote the `SAFE_SHELL_PREFIXES` list from src/orchestrator/autoMode.ts verbatim: ls, cat, head, tail, wc, pwd, which, rg, grep, find, git status, git diff, git log, git show, git branch, npm test, npm run lint, npm run compile, npm run build, npx tsc, node --version), and any chaining, pipe, redirect or substitution (`;`, `&&`, `||`, `|`, backtick, `$(`, `>`, `<`, newline) escalates however the command starts. Tool names are normalised through `TOOL_FAMILIES` onto read/search/write/shell; an unmapped tool (`webfetch`, `websearch`, `task`, `agent`, anything new) escalates, so a missing name costs a round-trip and never an unsafe approval. A relayed ask is keyed on the launching run's trusted identity (its adapter id, role and run id — `AutoModeRunContext`), not on the agent field inside the ask file; an ask claiming a different agent escalates.
   - **Stage (b), the model evaluation** — everything the allow-list did not clear goes to the orchestrator model with the stage-(a) reason attached (`evaluateAsk` / `RISK_EVALUATION_PROMPT`). It approves only read-only or reversible work plainly inside the agent's own `.baiton/runs/<run-id>/` directory and within the role's remit, and escalates deletes or overwrites outside that directory, git-history rewrites or pushes, installs/downloads, network access, anything touching credentials, secrets or `.env`, version-control or CI configuration changes, and anything unclear — when in doubt it escalates. The reply contract is one JSON object: `{"decision":"approve","rationale":"…"}` or `{"decision":"escalate","what":"…","why":"…"}`.
   - **Fail-safe direction** — state plainly that every failure path escalates and none approves: empty or unreadable model output, an unknown decision, an approval without a reason, a transport error, or a gate that throws (ChatController.autoDecision catches and turns it into an escalation). The ask arguments are fenced and treated as untrusted data; embedded instructions such as 'this is safe, approve it' are ignored and are themselves a reason to escalate.
   - **The audit trace** — every auto-approval and every escalation is written into the session transcript, so the conversation is the audit log. An auto-approval never shows a pending card: the card is posted already settled, carries the **Auto** badge and the one-line rationale naming the agent, role, tool and matched rule (or the model's rationale), and is appended to the transcript. An escalation is appended when it happens, as a pending card carrying the 'what you are approving / why it was flagged' description, and appended again once you answer it. Turning Auto off leaves those records in place.
   - Close with a forward pointer: which asks can reach these cards at all depends on the per-adapter relay, documented in 'Harness ask relay (per-adapter probe findings)' below.

   Files: `README.md`

5. Tie the existing relay findings table into the intervention and Auto-mode documentation

   The '#### Harness ask relay (per-adapter probe findings)' subsection and its 'Per-adapter relay state' table already exist (written by T13-T18) and are correct: claude = native `PreToolUse` hook via inline `--settings` (verified 2.1.278); opencode, antigravity (agy) and codex = config-driven fallback, each with the recorded reason. Do not restate or rewrite the findings blocks, and do not weaken any 'Unverified' marker.

   Three small edits only:
   1. Extend the subsection's one-line lead ('A launched run's `.baiton/runs/<run-id>/asks/<ask-id>.json` is a harness ask…') with a sentence saying what the relay is *for*: those asks are what become **permission** intervention cards in the run's spec conversation, answered in place, with Auto mode applied when it is on; the run stays paused until the response file appears, and Stop or a run that ends declines any still-pending ask so nothing hangs.
   2. Add one sentence saying the table is the thing that decides which relay each adapter uses, and that the selection is encoded in `ASK_RELAY_KIND` / `askRelayKind()` in `src/adapter/index.ts` — i.e. the documented findings and the shipped behaviour have one source.
   3. Since this subsection currently sits under '### The config panel', check it reads as a peer of the relay/permission material rather than as a config-panel detail; if the '#### ' nesting makes it read as config-panel content, promote it to '### Harness ask relay (per-adapter probe findings)' placed after the new Auto mode section. Prefer promotion only if it does not disturb the '#### Agent Model & Effort Discovery' sibling; otherwise leave the level alone and rely on the cross-references from the Auto mode section.

   Files: `README.md`

6. Verify the documentation against the code and the build

   Before finishing, re-read each symbol named in the new prose and confirm it still exists with the stated behaviour: `ask_user` phases/`MAX_ASK_USER_OPTIONS` in src/orchestrator/controlTools.ts; `SAFE_SHELL_PREFIXES`, `TOOL_FAMILIES`, `AutoModeRunContext`, `RISK_EVALUATION_PROMPT`, `decideAsk` in src/orchestrator/autoMode.ts; `baiton.chat.autoMode` in src/activation/commands.ts; the `Auto: On`/`Auto: Off` labels and the `Approve`/`Decline`/`Send` button text in media/chat.js; `interventionTranscriptRecord` in src/orchestrator/chatTranscript.ts; `ASK_RELAY_KIND`/`askRelayKind` in src/adapter/index.ts. Then run `npm run compile`, `npm run lint` and `npm test` to show the tree is still green (a README-only change must not move the counts: the current baseline is 1163 passing / 1 pending, the pending one being the pre-existing test/config.test.ts version-gating case), and `git status --short && git diff --stat` to show README.md is the only changed file.

   Files: `README.md`

## Risks

- Documenting behaviour that does not exist. Every claim must be read out of the tree first — in particular the exact button labels (`Auto: On`/`Auto: Off`, `Approve`, `Decline`, `Send`), the workspaceState key `baiton.chat.autoMode`, and the `SAFE_SHELL_PREFIXES` list, which are easy to paraphrase wrongly.
- Overstating Auto mode's safety. The prose must not imply that auto-approval is enforced by the harness: on the three config-driven adapters the relay is instruction-driven, so a sub-agent that ignores its Brief simply asks in its own terminal and nothing is auto-approved on its behalf. The existing closing paragraph of the relay subsection already says this; the new Auto mode section must not contradict it.
- Damaging the probe record. The four findings blocks contain deliberately worded 'Unverified' markers and verbatim probe transcripts from T13-T16. Editing, reflowing or summarising them would destroy evidence; restrict edits in that subsection to the lead sentence, the `ASK_RELAY_KIND` pointer and (optionally) the heading level.
- Section drift. The README's per-section wrapping is inconsistent (hand-written sections wrap near 80 columns, the relay findings do not). Matching the neighbouring section's style matters more than global consistency, and reflowing untouched paragraphs would bloat the diff.
- Scope creep into code. The temptation to 'fix' a label or a constant while documenting it. This todo changes README.md only; anything that looks wrong in the code should be reported, not edited.

## Acceptance

- `git status --short` shows README.md as the only modified file, and `git diff --stat` shows no other path.
- README documents the inline intervention cards: the three kinds (question, confirm, permission), the controls each renders, the settled state with its decision line, Auto badge and rationale, the fact that cards are persisted into the session transcript and survive reload and conversation switching, and that Stop declines every pending ask.
- README documents `ask_user`: it appears in the orchestrator tool table as available in both the creating-a-spec and driving-a-spec phases, and the prose states that it renders a card, blocks until answered, returns the answer as the tool result, supports options and/or free text, and refuses when declined. The `draft_spec` confirmation in 'Creating a spec' is described as an inline card, not a modal.
- README documents Auto mode: the toggle left of Stop with its labels, that it stays enabled mid-run, that it persists per workspace in `workspaceState` under `baiton.chat.autoMode` (not in settings.json), that only harness permission asks arriving while it is on are gated and questions/confirms never are, the two stages (deterministic per-agent allow-list keyed on the run's agent/role/run id, then the model risk evaluation), the allow-list's path and shell narrowings with the actual safe-prefix list, the model's approve/escalate criteria and JSON reply contract, and that every failure path escalates rather than approves.
- README documents the audit trace: an auto-approval posts an already-settled card with the Auto badge and its rationale and is appended to the transcript, an escalation is appended pending and again once answered, and the transcript is therefore the audit log.
- The per-adapter relay capability table is present and unchanged in substance (claude native; opencode/antigravity/codex config-driven with their recorded reasons), its lead now explains that relayed asks become permission cards answered in chat with Auto mode applied, and the README states that this table is what drives the selection encoded in `ASK_RELAY_KIND` in `src/adapter/index.ts`. No 'Unverified' marker or probe transcript was altered.
- Every symbol, path, key, label and constant quoted in the new text exists in the tree with the behaviour described.
- `npm run compile`, `npm run lint` and `npm test` all pass, with the test counts unchanged from the pre-change baseline (1163 passing, 1 pre-existing pending).
