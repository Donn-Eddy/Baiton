# pr result

```json
{
  "title": "Add multi-provider inference orchestrator with Copilot, Gemini, Mistral, OpenCode, and OpenAI",
  "body": "## Summary\n\nGive the Baiton chat orchestrator first-class support for five inference backends — GitHub Copilot (via `vscode.lm`), Google AI Studio (Gemini's OpenAI-compatible endpoint), Mistral AI, OpenCode Go, and OpenAI/custom endpoints — with one API key per provider and a grouped Provider & Model dropdown in the Chat view.\n\n## What changed\n\n### Core provider infrastructure\n\n- **Provider catalog** (`src/orchestrator/providers.ts`): Host-free, single source of truth with provider IDs, display labels, dropdown order, default base URLs, SecretStorage key names, and built-in model lists (Gemini 2.5 family, Mistral, OpenCode, plus runtime enumeration for Copilot)\n- **Wire dialects** (`src/orchestrator/modelClient.ts`): Injected dialect hooks to fix Gemini tool chaining (no empty assistant content, strict tool-message shape, JSON-validated arguments) and OpenCode session tracking\n- **Session threading** (`src/orchestrator/toolLoop.ts`, `src/activation/chatController.ts`): Pass chat session ID through the tool loop so providers can key per-conversation state\n- **Copilot client** (`src/orchestrator/copilotClient.ts`): Native `vscode.lm` implementation mapping VS Code message types, tool calling, streaming, and error handling to the orchestrator's ModelClient interface\n\n### Host-side routing and key management\n\n- **ProviderRouter** (`src/activation/providerRouter.ts`): Owns one client per provider, reads keys from SecretStorage, enumerates available models, persists active selection to workspaceState, and routes completion requests to the active provider\n- **Per-provider key management** (`src/activation/setApiKey.ts`, `src/activation/commands.ts`): New `baiton.setProviderApiKey` command with quick-pick provider selection and masked input; one-time migration of legacy `baiton.orchestrator.apiKey` secret into the OpenAI slot; legacy command retained as alias\n\n### Webview protocol and UI\n\n- **Protocol extensions** (`src/orchestrator/webviewProtocol.ts`, `media/protocol.js`): Added `setProviders` (host→webview: provider groups with enabled flag, disabled reason, models, and active selection) and `selectModel` (webview→host: user's provider/model pick)\n- **Provider dropdown** (`media/chat.html`, `media/chat.js`): Grouped `<select>` with one `<optgroup>` per provider in catalog order; disabled groups show disabled reason and \"Set API key…\" affordance; empty state shows provider + model instead of endpoint + model\n- **Chat integration** (`src/activation/chatController.ts`): Post setProviders on refresh and router change, handle selectModel without touching transcript, show provider/model in empty state, map missing key to inline error with fix action\n\n## Why\n\n### Execution model\nToday, single hardcoded OpenAI endpoint limits flexibility. Five backends unlock:\n- **Copilot**: In-process, no API key, leverages VS Code's model selection\n- **Gemini**: Google's OpenAI-compatible endpoint (requires tool-chaining fixes in the wire dialect)\n- **Mistral**: Competitive inference speed and cost\n- **OpenCode**: Specialized code models, stable session headers for per-conversation state\n- **OpenAI/Custom**: Retains existing setup, settable base URL for enterprise/self-hosted\n\n### Design principles preserved\n- Existing seams unchanged: one ModelClient interface for all providers\n- No transcript corruption: provider switches apply to the next turn only\n- Backward compatible: legacy `baiton.orchestrator.apiKey` migrated once, old command keybindings work, endpoint/model settings retained as \"OpenAI / Custom\"\n- Host-free cores tested in isolation: catalog, dialects, Copilot client, ProviderRouter all unit-tested without VS Code host\n\n## How it was verified\n\n- **Type safety**: `tsc --noEmit` clean; no new runtime dependencies in host-free modules\n- **Catalog correctness**: 12 test groups covering all provider metadata, secret-key names, model defaults, and validator edge cases\n- **Wire dialects**: Mock-server tests for Gemini tool-chaining shape fixes (empty content omitted, tool arguments validated as JSON) and OpenCode session header stability\n- **Copilot integration**: 36 tests covering message/tool mapping, streaming, abort handling (pre- and mid-stream), model resolution, and error mapping over a faked `vscode.lm` API\n- **ProviderRouter**: 34 tests covering provider availability (key+endpoint checks, Copilot model enumeration), client routing, selection persistence, and model enumeration per provider\n- **Per-provider keys**: 10 new + 8 legacy handler tests for set, clear, migration, and error paths\n- **Webview protocol**: 6 new setProviders cases (full catalog, null selection, disabled reasons, replacement) plus mirror-parity verification\n- **Chat view**: 17 test cases for dropdown seed paint, optgroup order, disabled groups, selection matching, placeholder insertion, selectModel posting, busy state, and provider/model empty state\n- **End-to-end**: All 1,337 unit tests passing; new todos never regressed pre-existing suites\n\n## Key behaviors\n\n1. **Separate keys per provider**: Each provider's API key stored in SecretStorage under its own key (`baiton.orchestrator.key.google`, etc.); Copilot needs no key\n2. **Grouped dropdown**: Chat view shows a `<select>` with one disabled `<optgroup>` per provider lacking a key; \"Set API key…\" button appears when a group is disabled\n3. **Gemini multi-step tool calls**: Tool-chaining loop no longer stalls on Gemini; assistant tool_calls turns carry no empty content, tool messages are shape-validated and reordered\n4. **OpenCode request headers**: Every completion to OpenCode includes `User-Agent: baiton/<version>` and stable `x-opencode-session: <uuid>` headers per conversation session\n5. **Copilot in-window**: Runs natively via `vscode.lm`, inherits user's model selection and consent; tool calling works end-to-end\n6. **Switch providers without loss**: Changing the active provider applies to the next user message; transcript, history, and auto-mode decisions carry forward unchanged\n\n## Files modified\n\n**Core provider logic** (8 files, host-free):\n- `src/orchestrator/providers.ts` (new)\n- `src/orchestrator/modelClient.ts` (dialects, extra headers, sessionId)\n- `src/orchestrator/copilotClient.ts` (new)\n- `src/orchestrator/toolLoop.ts` (thread sessionId)\n- `src/orchestrator/index.ts` (re-exports)\n- `test/providers.test.ts` (new)\n- `test/modelClient.test.ts` (extended)\n- `test/copilotClient.test.ts` (new)\n\n**Host integration** (7 files):\n- `src/activation/providerRouter.ts` (new)\n- `src/activation/chatController.ts` (setProviders, selectModel, error handling)\n- `src/activation/commands.ts` (migration, router wiring)\n- `src/activation/setApiKey.ts` (per-provider prompt)\n- `test/providerRouter.test.ts` (new)\n- `test/setApiKey.test.ts` (extended)\n- `test/chatController.*.test.ts` (extended)\n\n**Webview and protocol** (5 files):\n- `src/orchestrator/webviewProtocol.ts` (setProviders, selectModel)\n- `media/protocol.js` (mirror)\n- `media/chat.html` (model dropdown)\n- `media/chat.js` (renderProviders, selectModel handler)\n- `test/chatView.providers.test.ts` (new, 17 cases)\n- `test/webviewProtocol.reducer.test.ts` (extended)\n- `test/fixtures/protocolCases.ts` (extended)\n\n**Documentation**:\n- `README.md` (provider table, key setup, Copilot mode, Gemini tool chaining, OpenCode headers)\n- `package.json` (command descriptions, streaming behavior)\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
```
