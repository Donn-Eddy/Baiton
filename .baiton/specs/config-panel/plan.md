# plan result

```json
{
  "steps": [
    {
      "title": "media/config.html — static shell: nonce CSP, theme variables, roles/limits/git sections, banners, error state",
      "detail": "New file, modelled line-for-line on media/chat.html (same header comment style, same CSP, same \"every color from a VS Code theme variable\" discipline, no framework, no new dependency).\n\nHeader comment: name the spec (\"Config Panel\" / config-panel, todo T04), state that this is the static shell the Config Panel WebviewPanel provider (T05) loads, and document the three placeholders the provider substitutes exactly as chat.html does: `${nonce}` (fresh per-load nonce on every <script> and on the <style>), `${cspSource}`, `${baseUri}` (the media/ webview URI; localResourceRoots is limited to media/). Note retainContextWhenHidden is set at registration time by the provider.\n\n<head>:\n- `<meta charset=\"UTF-8\">`, the same CSP meta verbatim from chat.html: `default-src 'none'; img-src ${cspSource} https: data:; style-src ${cspSource} 'unsafe-inline'; script-src 'nonce-${nonce}'; font-src ${cspSource};`\n- viewport meta, `<title>Baiton Config</title>`\n- `<style nonce=\"${nonce}\">` with only theme-variable colors: `--vscode-font-family`/`--vscode-font-size`/`--vscode-foreground`/`--vscode-editor-background` on body; `--vscode-input-foreground`/`--vscode-input-background`/`--vscode-input-border, var(--vscode-panel-border)` on input/select; `--vscode-button-*` (+ `.secondary` using `--vscode-button-secondary*`) copied from chat.html's button block so the two shells stay visually identical; `--vscode-descriptionForeground` for hints; `--vscode-inputValidation-errorForeground`/`--vscode-inputValidation-errorBorder`/`--vscode-inputValidation-errorBackground` for field errors; `--vscode-inputValidation-warningBackground`/`--vscode-inputValidation-warningBorder` for the banner; `--vscode-panel-border` for rules. Reuse the `:root { --baiton-gap: 8px; }` idiom.\n- Unlike chat.html the document *does* scroll (it is an editor-area panel with a long form): `html, body { margin:0; padding:0 }`, body gets `padding: calc(var(--baiton-gap) * 2)` and normal overflow; the sticky action bar (below) uses `position: sticky; top: 0` with the editor background so Save stays reachable on a long form.\n\n<body> structure (ids are the contract config.js binds to — keep them stable, T05/T07 touch config.js only):\n- `<header class=\"toolbar\">` sticky: `<h1>Baiton Configuration</h1>`, a `<span id=\"config-path\">` showing `.baiton/config.json`, and the action row: `<button id=\"save\">Save</button>`, `<button id=\"reload\" class=\"secondary\">Reload</button>`, `<button id=\"reset\" class=\"secondary\">Reset to Defaults…</button>`.\n- `<div id=\"banner\" role=\"alert\">` — the single banner slot (hidden by default via a `.visible` class exactly like chat.html's #error-banner): `<span id=\"banner-message\"></span>` plus two link-buttons `<button id=\"banner-primary\" class=\"link-button\">` and `<button id=\"banner-secondary\" class=\"link-button\">`. It carries the external-change, conflict and unsaved-changes messages; config.js sets the labels and a `data-action` on each button (same `dataset.action` idiom as chat.js's #error-fix).\n- `<div id=\"status\" aria-live=\"polite\"></div>` — the post-save outcome line (\"Saved.\" plus any `notes[]` the host reports for things that could not hot-reload).\n- `<div id=\"error-view\">` (hidden by default) — the load-failure state: `<p id=\"error-message\"></p>`, `<button id=\"error-reset\">Reset to Defaults…</button>`, `<button id=\"error-reload\" class=\"secondary\">Reload</button>`. Shown for `loadFailed`; the form is hidden while it is up.\n- `<form id=\"config-form\">` (with an `onsubmit` prevented in JS; `<form>` gives labels/fieldsets proper semantics) containing three `<fieldset>`s with `<legend>`: Roles, Limits, Git.\n  - Roles: `<table id=\"roles-table\">` with a `<thead>` (Role / Agent / Model / Effort) and an **empty** `<tbody id=\"roles-body\">`. The six rows are built in config.js from its ROLES mirror so the role list has one source of truth and never drifts from src/model/role.ts.\n  - Limits: three labelled rows, each `<label for=\"limit-plan_review_rounds\">Plan review rounds</label>`, the input, a `<span class=\"hint\">0–10</span>` and an empty `<div class=\"field-error\" data-error-for=\"limits.plan_review_rounds\"></div>`. Same for `exec_attempts` (1–10) and `stall_notice_minutes` (1–1440). Inputs are `type=\"number\"` with `min`/`max`/`step=\"1\"` written literally in the HTML (matching LIMIT_BOUNDS) as the todo asks; config.js still validates the raw string, because the browser's number input is a hint, not the authority.\n  - Git: `<input id=\"git-remote\" type=\"text\">` and `<input id=\"git-base\" type=\"text\">`, each with a label and an empty `.field-error` div (`data-error-for=\"git.remote\"` / `git.base`).\n- Scripts at the end of <body>, nonce'd, with a comment mirroring chat.html's: `<script nonce=\"${nonce}\" src=\"${baseUri}/config.js\"></script>`. No sanitizer and no markdown vendor are loaded: this view writes only `textContent` and typed input values, never HTML, so nothing needs sanitizing — say so in the comment.\n\nAccessibility: every input has a `<label for>`; field errors are wired by config.js with `aria-describedby` + `aria-invalid`; `#banner` is `role=\"alert\"` and `#status` is `aria-live=\"polite\"`.",
      "files": [
        "media/config.html",
        "media/chat.html"
      ]
    },
    {
      "title": "media/config.js (part 1) — the pure validator mirror exposed on window.baitonConfigForm",
      "detail": "New file, `// @ts-check`, wrapped in an IIFE with `'use strict';` — same skeleton as media/chat.js and media/protocol.js.\n\nThe file opens with a doc comment stating that **src/config/configPanel.ts is the source of truth** and that this file is its plain-script mirror, exactly as media/protocol.js mirrors src/orchestrator/webviewProtocol.ts; T09 adds a fixture test that runs the same forms through both and fails if they diverge, so any edit here must be made in both places.\n\nThe mirror block comes first and is self-contained (no DOM access), exposed as `window.baitonConfigForm = { ROLES, EFFORT_OPTIONS, LIMIT_BOUNDS, validateConfigForm }`:\n- `ROLES = ['spec-writer','planner','plan-reviewer','executor','reviewer','pr-writer']` — same order as src/model/role.ts.\n- `EFFORT_OPTIONS = ['low','medium','high']` — same order as configPanel.ts.\n- `LIMIT_BOUNDS = { plan_review_rounds:{min:0,max:10}, exec_attempts:{min:1,max:10}, stall_notice_minutes:{min:1,max:1440} }` — same **key order** as src/config/types.ts, because the TS validator walks `Object.keys(LIMIT_BOUNDS)` and the error order is part of what T09 compares.\n- `validateConfigForm(form, options)` — a literal transcription of the TS function, including the message strings **verbatim** (they are compared, not just counted):\n  - roles, in ROLES order: trimmed agent empty → `roles.<role>.agent` / `\"<role>\" is missing an agent.`; agent not in `options.agents` → `\"<agent>\" is not an installed agent (installed: <ids joined by \", \">).`; trimmed model empty → `\"<role>\" is missing a model.`; `effort !== '' && effort.trim() === ''` → `\"<role>\" effort must not be blank.`\n  - limits, in LIMIT_BOUNDS key order, on the trimmed raw string: `!/^-?\\d+$/.test(raw) || !Number.isInteger(Number(raw))` → `\"limits.<field>\" must be an integer.` and `continue`; else out of `[min,max]` → `\"limits.<field>\" must be between <min> and <max> (found <n>)` (note: no trailing period in the TS message — copy it exactly).\n  - git: empty trimmed remote → `Git remote must not be empty.`; empty trimmed base → `Git base branch must not be empty.`\n  Returns `[]` for a valid form and never mutates its input. Written with `var`-free ES2022-in-browser style (const/let, `function` expressions) matching protocol.js.\n\nGuard the rest of the file so the mirror is loadable outside a webview (the T09 test loads this file with a fake `window` and no `acquireVsCodeApi`): after exposing `window.baitonConfigForm`, `if (typeof acquireVsCodeApi !== 'function') { return; }` before any DOM work, with a comment explaining that this is what lets the mirror be unit-tested against the TS core.",
      "files": [
        "media/config.js",
        "src/config/configPanel.ts",
        "media/protocol.js",
        "src/model/role.ts",
        "src/config/types.ts"
      ]
    },
    {
      "title": "media/config.js (part 2) — state model, form building and rendering (inline errors, banners, error state)",
      "detail": "After the mirror and the `acquireVsCodeApi` guard, the entry script proper — the view is a pure projection of one state object, exactly like chat.js.\n\nState (module-local `let state`):\n`{ phase: 'loading' | 'ready' | 'error', form: ConfigForm | null, baseline: string (JSON of the last loaded/saved form), token: string, options: { agents: [], efforts: [] }, errors: [], failure: { kind, message, canReset } | null, banner: null | { kind: 'external' | 'conflict' | 'dirty' | 'io', message, primary?, secondary? }, status: string, busy: boolean }`.\nHelpers: `formJson(form)` (stable JSON of the form — roles serialized in ROLES order so dirtiness is order-independent), `isDirty()` = `state.form && formJson(state.form) !== state.baseline`, `readForm()` (build a ConfigForm from the DOM inputs — raw strings, no coercion, so a bad value survives to the validator), `setField(path, value)` (write into `state.form`).\n\nElement lookups at the top (`document.getElementById` into `const`s), mirroring chat.js's \"----- Elements -----\" section.\n\n`buildRoleRows()` — runs once on first render: for each role in the ROLES mirror, create a `<tr>` with `data-role`, a `<th scope=\"row\">` holding the role id, then three cells:\n- agent `<select>` (`id=\"role-<role>-agent\"`) — options from `state.options.agents`; the host already appended any out-of-set saved value via `configFormOptions`, so the select never silently drops a value. If the form's agent is still not among the options (defensive), append it as an extra option before selecting.\n- model `<input type=\"text\">` (`id=\"role-<role>-model\"`).\n- effort `<select>` (`id=\"role-<role>-effort\"`) — a first option with value `''` labelled `(default)` (the config `effort` is optional and `''` means unset), then `state.options.efforts`.\nEach cell gets an empty `<div class=\"field-error\" data-error-for=\"roles.<role>.<field>\">` and the control gets `aria-describedby` pointing at it. Rows are rebuilt only when the option sets change (keep a `renderedOptionsSignature`, the same idiom as chat.js's `renderedSessionSignature`) so typing never clobbers focus.\n\n`renderValues()` — write `state.form` into the controls, but **skip the control that currently has focus** (`document.activeElement`) so re-rendering never fights the user's cursor or, for `type=\"number\"`, blanks a partially-typed value.\n\n`renderErrors()` — clear every `.field-error`, then for each error from `validateConfigForm(state.form, { agents: state.options.agents })` set `textContent` on the node with the matching `data-error-for`, add `aria-invalid=\"true\"` and an `.invalid` class to the control, and remove both from controls with no error. Errors are rendered on every input event, not only on Save (the spec: \"validates on every input and again on Save\").\n\n`renderBanner()` / `renderStatus()` / `renderErrorView()` — show/hide via a `.visible` class exactly like chat.js's `renderError`, setting `dataset.action` on `#banner-primary` / `#banner-secondary` and hiding a button with `style.display = 'none'` when the banner has no second action. `renderErrorView()` shows `#error-view` and hides `#config-form` when `phase === 'error'`, printing `failure.message` and hiding `#error-reset` when `failure.canReset` is false.\n\n`updateEnablement()` — `save` disabled when `state.busy`, when the form is pristine, or when there are validation errors; `reload`/`reset` disabled while `state.busy`; all inputs disabled while `state.busy` or in the error/loading phase.\n\n`render()` — `renderErrorView(); buildRoleRows(); renderValues(); renderErrors(); renderBanner(); renderStatus(); updateEnablement();`, the same one-call-per-region shape as chat.js's `render()`.",
      "files": [
        "media/config.js",
        "media/chat.js",
        "media/config.html"
      ]
    },
    {
      "title": "media/config.js (part 3) — user actions, host message handling, draft persistence",
      "detail": "Wire the protocol from src/config/configPanel.ts. Webview→host messages are only `ready`, `load`, `save`, `reset`; the webview never builds a config document itself (unknown-key preservation is `applyFormToDocument` on the host).\n\nEvents:\n- Delegated `input` and `change` listeners on `#config-form`: read the target's `data-path` (set when the controls are built, e.g. `roles.executor.agent`, `limits.exec_attempts`, `git.base`), write the raw string into `state.form`, clear `state.status`, drop a stale `conflict`/`io` banner, and `render()`. Editing while an `external` banner is up leaves the banner in place (T07 refines).\n- `#save` → `postMessage({ type: 'save', form: state.form, token: state.token })` after a local `validateConfigForm` gate (Save is disabled when invalid, but re-check so a keyboard submit cannot bypass it); set `state.busy = true`. The `overwrite: true` variant is posted only from the conflict banner's primary action.\n- `#reload` / `#error-reload` / the banner's Reload action → `postMessage({ type: 'load' })`, clearing the banner and status; the host answers with `loaded`/`loadFailed`, which replaces the form and the baseline (discarding edits — that is the documented \"discard edits\" path in the protocol comment).\n- `#reset` / `#error-reset` → `postMessage({ type: 'reset' })`. The modal confirmation is the **host's** (`vscode.window.showWarningMessage({ modal: true })` in T05), so the webview posts directly and shows no confirm of its own — note this in a comment next to the handler.\n- `#config-form` `submit` → `preventDefault()` and route to the same save path, so Enter in a text field saves rather than reloading the webview.\n\nHost→webview (`window.addEventListener('message', …)`), one branch per `ConfigPanelHostToWebview` member, with an `if (!msg || typeof msg.type !== 'string') return;` head guard and a `default:` that ignores unknown types (same tolerance as protocol.js's reducer):\n- `loaded` → `phase='ready'`, store `form`, `token`, `options`, set `baseline = formJson(form)`, clear `errors`/`failure`/`banner`, `busy=false`.\n- `loadFailed` → `phase='error'`, `failure = { kind, message, canReset }`, form cleared.\n- `saved` → `token = msg.token`, `baseline = formJson(state.form)` (the saved form is now the clean state), `busy=false`, `status = 'Saved.'` plus each of `msg.notes` on its own line (these are the things T08 reports as not hot-reloadable), banner cleared.\n- `saveFailed` → `busy=false`; `reason === 'invalid'` → render `msg.errors` as the field errors (host-side re-validation is authoritative) plus a banner with the message; `reason === 'conflict'` → banner `{ kind:'conflict', primary:'Reload', secondary:'Overwrite' }` where Reload posts `load` and Overwrite posts `save` with `overwrite: true` and the **current** token; `reason === 'io'` → plain message banner.\n- `externalChange` → if `!isDirty()`, adopt the new token and post `load` (silent reload of a pristine form); if dirty, banner `{ kind:'external', primary:'Reload (discard edits)', secondary:'Keep editing' }` — Keep editing just dismisses the banner and keeps the stale token so a later Save surfaces the conflict path.\n\nDraft persistence (mirrors chat.js's Req 16.5 handling under `retainContextWhenHidden`): on every edit, `vscode.setState({ form: state.form, token: state.token })`; on load, if the persisted state exists **and its token equals the token in the incoming `loaded`**, restore the persisted form over the freshly loaded one (and leave the baseline at the on-disk form so it correctly reads as dirty); if the tokens differ, drop the draft — restoring edits against a document that has since changed is exactly the conflict this panel exists to avoid.\n\nFinish with `vscode.postMessage({ type: 'ready' })` followed by `render()` (the loading phase), matching chat.js's closing \"initial paint, host pushes the first real state\" comment.",
      "files": [
        "media/config.js",
        "src/config/configPanel.ts",
        "media/chat.js"
      ]
    },
    {
      "title": "Verify: mirror parity smoke check, lint/compile/test unchanged, nothing outside media/ touched",
      "detail": "No TypeScript changes and no new test file in this todo (T09 owns the mirror-sync test and the README section; T05 owns the provider that renders the shell), so the gates are mostly negative:\n\n1. `node --check media/config.js` — the file must parse as a plain script (it is never bundled or transpiled).\n2. Throwaway parity smoke check in the scratchpad (not committed): a small Node script that sets `global.window = {}`, `require`s media/config.js via `vm`/`fs.readFileSync` + `new Function`, and asserts `window.baitonConfigForm.validateConfigForm` returns exactly the same `path`/`message` list as the compiled `validateConfigForm` from src/config/configPanel.ts for: the default form, a form with every role's agent blanked, an agent not in `createAdapterRegistry().ids`, each limit at min-1/min/max/max+1 and a non-integer, and empty git fields. This is T09's test in miniature and is the cheapest way to catch a transcription slip now; note in the execute summary that T09 turns it into a committed fixture test.\n3. Confirm the guard works: with no `acquireVsCodeApi` defined, loading the file must not throw and must not touch `document`.\n4. `npm run lint` and `npm run compile` still pass (eslint runs only over `src`/`test` and `.eslintrc.json` ignores `**/*.js`, so media/ is not linted — do not add media/ to the lint globs in this todo), and `npm test` is unchanged from its pre-change baseline.\n5. `git status` shows exactly two new files, `media/config.html` and `media/config.js`, and no modifications anywhere else.\n\nAlso check by eye that every `data-error-for` path in the HTML/JS matches a `path` string `validateConfigForm` can emit (`roles.<role>.<agent|model|effort>`, `limits.<three keys>`, `git.remote`, `git.base`) — an error with no matching slot would be silently invisible.",
      "files": [
        "media/config.js",
        "media/config.html",
        "package.json",
        ".eslintrc.json"
      ]
    }
  ],
  "risks": [
    "The validator in media/config.js is a hand transcription of src/config/configPanel.ts, so it can drift — silently, since nothing asserts parity until T09. Mitigations: keep the message strings byte-identical (including the missing trailing period in the limits range message), keep the LIMIT_BOUNDS and ROLES key order identical because error ordering is compared, and run the scratchpad parity check in step 5 before calling the todo done.",
    "T09's sync test has to load media/config.js outside a webview. If the entry code is not guarded behind `typeof acquireVsCodeApi !== 'function'`, that test will need jsdom or a fake DOM. Putting the mirror first and returning early is the cheap decision that keeps T09 dependency-free — it is worth not skipping.",
    "`input[type=number]` reports `''` for unparseable text in Chromium, so a user typing `abc` into a limit sees the field blank rather than an \"must be an integer\" message on their own text. The plan keeps type=number (the todo asks for min/max) and compensates by never re-rendering a focused control; if this proves confusing in T05's manual pass, switching to `type=\"text\" inputmode=\"numeric\"` with the bounds in the hint is a one-line change and the validator does not care either way.",
    "Element ids are an implicit contract with T05 (shell rendering) and T07 (watcher-driven banners). They are listed explicitly in step 1 so later todos can rely on them; renaming them later means touching config.js in two more todos.",
    "The unsaved-draft restore under retainContextWhenHidden can resurrect edits against a document that changed on disk. Gating the restore on token equality is what keeps it safe; dropping that check would turn a hide/show into a silent overwrite.",
    "The webview is untrusted by design: it posts a form, never a document. If a later change has the webview compute JSON, unknown-key preservation (`pr`, `git.verify`, unknown roles) breaks — the spec's core promise. Keep `applyFormToDocument` host-side only.",
    "Effort `''` means unset and must render as an explicit `(default)` option rather than an empty-looking select, otherwise a user cannot tell 'unset' from 'not yet loaded' and cannot clear an effort once set."
  ],
  "acceptance": [
    "Exactly two new files exist — media/config.html and media/config.js — and `git status` shows no other file added or modified (no src/, no test/, no package.json change in this todo).",
    "`node --check media/config.js` passes; loading media/config.js with a bare `global.window = {}` and no `acquireVsCodeApi` defines `window.baitonConfigForm` with `ROLES`, `EFFORT_OPTIONS`, `LIMIT_BOUNDS` and `validateConfigForm`, and touches no DOM API.",
    "For the default form, a form with blank/unknown agents, a missing model, a blank-but-non-empty effort, each limit at min-1/min/max/max+1 and a non-integer, and empty git.remote/git.base, `window.baitonConfigForm.validateConfigForm` returns the same array of `{path, message}` in the same order as `validateConfigForm` from src/config/configPanel.ts.",
    "media/config.html carries the same CSP as media/chat.html (`default-src 'none'; … script-src 'nonce-${nonce}';`), uses the `${nonce}`/`${cspSource}`/`${baseUri}` placeholders, loads only `${baseUri}/config.js` with the nonce on the tag, and contains no hardcoded color — every color resolves through a `--vscode-*` variable.",
    "The shell renders the three sections the todo names: a roles table whose six rows config.js builds from the ROLES mirror with an agent dropdown, a model text input and an effort dropdown carrying a `(default)` option for the unset case; three numeric limit inputs with literal min/max matching LIMIT_BOUNDS; and git remote/base text inputs — every control with a `<label for>` and an empty `.field-error` slot whose `data-error-for` equals a path `validateConfigForm` can emit.",
    "Save / Reload / Reset to Defaults buttons exist; Save is disabled while the form is pristine, while validation errors are present, or while a save is in flight, and the buttons post exactly `save` (with `form` and `token`, plus `overwrite: true` only from the conflict banner), `load` and `reset` — the webview never constructs a config document.",
    "Every `ConfigPanelHostToWebview` member is handled — `loaded`, `loadFailed`, `saved`, `saveFailed` (invalid / conflict / io), `externalChange` — with an unknown-type message ignored rather than thrown on; `loadFailed` shows the error view with the message and a Reset button gated on `canReset`, `saved` shows the outcome including any `notes`, `saveFailed` with `conflict` offers Reload / Overwrite, and `externalChange` reloads a pristine form silently but banners a dirty one with Reload / Keep editing.",
    "A `ready` message is posted on load and the view paints a loading state before the first host reply; unsaved edits are persisted through `vscode.setState` and restored on show only when the persisted token matches the token of the incoming `loaded`.",
    "`npm run lint`, `npm run compile` and `npm test` all pass with results identical to the pre-change baseline (media/ is outside the lint and compile inputs; no test is added or changed here)."
  ]
}
```
