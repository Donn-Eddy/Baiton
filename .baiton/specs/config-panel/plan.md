# plan result

```json
{
  "steps": [
    {
      "title": "test/configPanel.test.ts — host-free core: form mapping, validation, unknown-key preservation",
      "detail": "New mocha/ts-node test file (no `vscode` import, no `fs` — this file must stay pure), following the house style of test/config.test.ts: a file-header doc comment naming the spec (\"Config Panel\" / config-panel, todo T03) and listing what is covered, then one top-level `describe('config panel core (config-panel T03)')` with nested describes per export.\n\nImports: `assert` from 'assert'; `ConfigForm, EFFORT_OPTIONS, applyFormToDocument, configFormOptions, formFromConfig, formFromDocument, validateConfigForm` from '../src/config/configPanel'; `defaultConfig, defaultConfigJson` from '../src/config/defaultConfig'; `LIMIT_BOUNDS, Limits, SUPPORTED_VERSION` from '../src/config/types'; `ROLES` from '../src/model'; `createAdapterRegistry` from '../src/adapter'.\n\nShared helpers at the top of the describe:\n- `const AGENTS = createAdapterRegistry().ids;` and `const OPTIONS = { agents: AGENTS };` — never hard-code 'claude'; the whole point of the rule is that the validator and the dropdown share the registry as one source of truth.\n- `function validForm(): ConfigForm { return formFromConfig(defaultConfig()); }` — a fresh valid form per test.\n- `function clone<T>(v: T): T { return JSON.parse(JSON.stringify(v)) as T; }` — for mutate-a-copy cases and for the \"rawDoc is not mutated\" assertion.\n- `function paths(errors: ConfigFieldError[]): string[] { return errors.map((e) => e.path); }` — assert on `path` lists (stable contract) and only use `.includes(...)` substring checks on `message`, so the tests do not become a copy of the wording.\n\nCases:\n\n1. `formFromConfig`\n   - `assert.deepStrictEqual(Object.keys(form.roles), [...ROLES])` — key order comes from ROLES, not Object.keys of the config, so round-trip diffs are stable.\n   - limits are the decimal text of the numbers (`'1'`, `'3'`, `'10'` for the default config, derived from `defaultConfig().limits` rather than literals).\n   - `effort` is `''` when the config entry has no `effort`, and the verbatim string when it has one (build a config with `delete roles['planner'].effort` on a clone).\n   - `git.remote`/`git.base` copied verbatim.\n\n2. `formFromDocument` (the lenient path for a semantically invalid document)\n   - `formFromDocument({})` yields every ROLES key with `{ agent: '', model: '', effort: '' }`, all three limits `''`, both git fields `''`.\n   - non-object inputs — `null`, `[]`, `3`, `'x'`, `undefined` — do not throw and yield that same empty form.\n   - wrong-typed leaves coerce to `''`: `roles.planner.agent = 42`, `limits.exec_attempts = '3'` (a string), `limits.plan_review_rounds = NaN`/`Infinity`, `git.base = null`.\n   - a finite number limit becomes its decimal text (including a non-integer like `2.5` → `'2.5'`, which validation then rejects — that is the designed hand-off, so pin it).\n   - unknown role keys in the document (`roles['custom']`) do not appear in the form; a document whose `roles` is an array or a string is treated as empty.\n\n3. `validateConfigForm`\n   - `validateConfigForm(validForm(), OPTIONS)` returns `[]`; and a loop asserting that for every id in `AGENTS`, a form using that id validates clean (the dropdown/validator single-source-of-truth rule).\n   - roles: empty and whitespace-only `agent` → one error at `roles.<role>.agent`; an agent not in the registry (`'nope'`) → `roles.<role>.agent` whose message includes `'nope'` and the joined installed list; empty/whitespace `model` → `roles.<role>.model`.\n   - effort semantics (subtle, pin it explicitly with a comment): `effort: ''` is valid (unset), `effort: '   '` is an error at `roles.<role>.effort`, and an effort outside `EFFORT_OPTIONS` (`'xhigh'`) is *valid* — the out-of-set round-trip rule from the spec.\n   - limits, driven by `(Object.keys(LIMIT_BOUNDS) as (keyof Limits)[])` so a new limit field forces the test to cover it: for each field, `min` and `max` validate clean; `min - 1` and `max + 1` produce one error at `limits.<field>` whose message includes both bounds and the offending number; `''`, `'abc'`, `'1.5'`, `'1e3'`, `'0x2'`, `'  '` produce the \"must be an integer\" error and *no* second range error (the validator `continue`s — assert `errors.length === 1`); `' 2 '` (padded but integer, within range for exec_attempts) validates clean.\n   - git: empty and whitespace-only `remote`/`base` → `git.remote` / `git.base`.\n   - aggregation and order: a form broken in all three sections reports every error, and `paths(errors)` is ordered roles (in ROLES order) → limits (in LIMIT_BOUNDS key order) → git — assert the full array, since T04/T09's webview renders in that order.\n\n4. `configFormOptions`\n   - with no form: `{ agents: [...AGENTS], efforts: [...EFFORT_OPTIONS] }`, and mutating the result does not disturb the registry (`createAdapterRegistry().ids` unchanged) — it returns copies.\n   - with a form carrying an unknown agent and an unknown effort in two different roles: each is appended once, after the known values, in ROLES order; installed ids and in-set efforts are never duplicated; `''` values are ignored.\n\n5. `applyFormToDocument` — preservation is the headline case\n   - Build `raw = JSON.parse(defaultConfigJson())` and add the keys the form does not manage: `raw.pr = { tool: 'gh' }`, `raw.git.verify = 'npm test'`, `raw.roles.executor.notes = 'keep me'`, `raw.roles.custom = { agent: 'x' }`, `raw.experimental = { x: 1 }`. Take `const before = clone(raw)`.\n   - Edit a form clone (new agent from `AGENTS`, a new model, effort cleared on one role and set on another, new limits, new git remote/base, with surrounding whitespace on several fields), apply, then assert: every preserved key is byte-equal in the output; the edited fields are present and **trimmed**; cleared effort means the `effort` key is *absent* (`assert.ok(!('effort' in out.roles[role]))`, not `undefined`); limits are `number`s, not strings; `raw` deep-equals `before` (no mutation).\n   - `version`: absent / wrong-typed / `SUPPORTED_VERSION + 1` are all rewritten to `SUPPORTED_VERSION`; an already-correct version is left as is. Add a comment noting this is deliberate (the panel only ever writes the supported shape) — see risks.\n   - `applyFormToDocument(null, validForm())` and `applyFormToDocument([], validForm())` produce a complete document with all six roles, three limits and git.\n\n6. Round trips\n   - identity: `applyFormToDocument(JSON.parse(defaultConfigJson()), formFromConfig(defaultConfig()))` deep-equals `JSON.parse(defaultConfigJson())` — editing nothing changes nothing.\n   - `formFromDocument(applyFormToDocument(raw, form))` deep-equals the trimmed form (build the expected by trimming each field), which is the invariant T04's webview relies on after a save.",
      "files": [
        "test/configPanel.test.ts",
        "src/config/configPanel.ts",
        "src/adapter/index.ts",
        "src/config/defaultConfig.ts",
        "test/config.test.ts"
      ]
    },
    {
      "title": "test/configPanel.document.test.ts — document I/O: classification, tokens, conflict refusal, serialization pin",
      "detail": "New test file exercising src/config/configDocument.ts against throwaway directories, structured exactly like test/config.test.ts: a `const dirs: string[] = []` list, a `newDir()` helper using `fs.mkdtempSync(path.join(os.tmpdir(), 'baiton-config-doc-'))`, a `writeConfig(dir, text)` helper, and an `afterEach` that `fs.rmSync(dir, { recursive: true, force: true })` every allocated directory. The temp dir plays the role of `.baiton/`, so the config path under test is always `path.join(dir, 'config.json')` — assert against `configFilePath(dir)` rather than re-joining by hand.\n\nImports: `assert`, `fs`, `os`, `path`; `ABSENT_TOKEN, configToken, readConfigDocument, readConfigToken, writeConfigDocument` from '../src/config/configDocument'; `configFilePath, loadConfig` from '../src/config/loadConfig'; `defaultConfig, defaultConfigJson` from '../src/config/defaultConfig'; `isErr, isOk` from '../src/model/result' (the existing tests narrow results this way); plus the configPanel exports for the integration case. Every assertion goes through `isOk`/`isErr` first so a failure prints the result rather than a type error.\n\nCases:\n\n1. `readConfigDocument` classification\n   - missing file → `isErr`, `kind === 'absent'`, `error.path === configFilePath(dir)`, message includes the path and `'Baiton: Initialize'`.\n   - `'{ bad json'` → `kind === 'unparseable'`, message includes the path.\n   - a parsed non-object — `'[]'`, `'null'`, `'42'`, `'\"text\"'` — each → `kind === 'unparseable'` with the `'must be a JSON object'` wording. Note in a comment that this deliberately diverges from `loadConfig`, which reports a non-object root as `missing-section: root` (T02's documented decision), so T05 does not expect `missing-section` here.\n   - a valid `defaultConfigJson()` file → `isOk`, `value.text` byte-identical to the file read back with `fs.readFileSync(p, 'utf8')`, `value.doc` deep-equals `JSON.parse(defaultConfigJson())`, `value.path === configFilePath(dir)`, and `value.token` matches `/^[0-9a-f]{64}$/` and equals `configToken(value.text)`.\n   - token stability/sensitivity: two reads of an unchanged file give the same token; rewriting the file with a whitespace-only difference (an extra trailing newline) gives a different token — the documented strictness.\n   - `readConfigDocument` accepts a Uri-like `DirLike` (`{ fsPath: dir }`) as well as a string, since the activation layer (T05) passes a `vscode.Uri`.\n   - I/O failure: `fs.mkdirSync(path.join(dir, 'config.json'))` and assert `kind === 'io'` (reading a directory yields EISDIR, not ENOENT) — see risks for the platform caveat; remove it in the test with `rmSync(..., { recursive: true })` (the shared afterEach already does this).\n\n2. `readConfigToken`\n   - missing file → `isOk` with `ABSENT_TOKEN` (and assert `ABSENT_TOKEN === ''`, since T05/T07 branch on it).\n   - present file → equals `configToken(text)` and equals the token from `readConfigDocument`.\n   - an unparseable file still yields a token (the token is over raw text, so the watcher never has to parse).\n\n3. `writeConfigDocument` — guarded write\n   - matching token: load, apply an edit, write with `expectedToken: loaded.token`; assert `isOk`, the file on disk equals the returned `text`, the returned `token` equals `configToken(fs.readFileSync(p,'utf8'))` and equals a fresh `readConfigToken`, and differs from the token written with.\n   - stale token: load, then simulate an external edit (`writeConfig(dir, otherText)`), then write with the *old* token; assert `isErr`, `kind === 'conflict'`, `error.token` equals the token of the external edit (so the webview's Reload can adopt it without a second read), `error.message` mentions the path, the file bytes are byte-for-byte the external edit's (compare the string read before and after), and `fs.readdirSync(dir)` is exactly `['config.json']` — no `.config.json.*.tmp` left behind, because the refusal happens before any write.\n   - `expectedToken` omitted → overwrites unconditionally, including right after an external edit (the Overwrite / Reset-to-defaults path).\n   - `ABSENT_TOKEN`: writing into an empty (but existing) temp dir with `expectedToken: ABSENT_TOKEN` succeeds and creates the file; with a file already present the same call returns `conflict` carrying the existing file's token. (`writeJsonAtomic` does not create the directory, so the test must always write into an existing dir — do not assert on a missing parent.)\n   - serialization pin (the test T02's comment promised): `writeConfigDocument(dir, JSON.parse(defaultConfigJson()))` writes on-disk text byte-identical to `defaultConfigJson()` — two-space indent, single trailing newline — and the returned `text` equals those on-disk bytes. This is the assertion that catches `writeJsonAtomic`'s serialization drifting from `writeConfigDocument`'s re-derived text; say so in a comment so a future editor does not delete it as redundant.\n   - the returned `doc` is a structurally equal but independent object (mutating it does not affect the value passed in).\n\n4. End-to-end through the core (the T05 path, without a VS Code host)\n   - Seed a config that carries unmanaged keys (`pr`, `git.verify`, an extra role key, an unknown top-level key). `readConfigDocument` → `formFromDocument` → edit a role's model and a limit → `validateConfigForm(form, { agents: createAdapterRegistry().ids })` returns `[]` → `applyFormToDocument(doc.doc, form)` → `writeConfigDocument(dir, next, { expectedToken: doc.token })` → re-read: the unmanaged keys are still in the raw text, the edits landed, and `await loadConfig(dir)` returns `isOk` with the new values. This pins the spec's central claim that a form passing `validateConfigForm` always produces a document `loadConfig` accepts.\n   - The negative half: a form the validator rejects (out-of-range limit) produces, via `applyFormToDocument`, a document `loadConfig` refuses — asserting the two validators agree on that direction too.",
      "files": [
        "test/configPanel.document.test.ts",
        "src/config/configDocument.ts",
        "src/config/loadConfig.ts",
        "test/config.test.ts"
      ]
    },
    {
      "title": "test/configPanel.form.property.test.ts — fast-check properties for limits and key preservation",
      "detail": "A small property file in the style of test/config.limits.property.test.ts (same header format: `Feature: config-panel, Property: <name>`, a Validates: line pointing at the spec section, and a note on what each iteration does). Keep it pure — no filesystem inside the fast-check loop — so it runs in well under the 60s mocha timeout; the fs-backed agreement check is the single `loadConfig` assertion in step 2, not a property.\n\nProperties:\n\n1. \"validateConfigForm accepts the limits iff each integer is within its closed range\". Reuse the `limitArb(field)` shape from test/config.limits.property.test.ts (samples inside, on both boundaries, just outside, and a wider window) for the three fields, build `formFromConfig(defaultConfig())` with those values stringified, and assert `validateConfigForm(form, { agents: createAdapterRegistry().ids }).length === 0` iff all three sit in range; when it fails, every reported error path is a `limits.<field>` of an out-of-range field and each out-of-range field is named exactly once. This is the form-side mirror of Property 24 and is what keeps `media/config.js` honest in T09.\n\n2. \"applyFormToDocument preserves every key the form does not manage\". Generate a record of unknown top-level keys (`fc.dictionary` of short keys, filtered to exclude `version`/`roles`/`limits`/`git`, with JSON-safe values via `fc.jsonValue()`), plus unknown keys inside `git` (excluding `remote`/`base`) and inside one role entry (excluding `agent`/`model`/`effort`). Merge them into a default document, apply an arbitrary valid form, and assert every generated key/value is still deep-equal in the output and the input document is unmutated.\n\n3. (Cheap, same file) \"formFromDocument never throws\": over `fc.jsonValue()`, `formFromDocument(value)` always returns an object with exactly the six ROLES keys and string-typed leaves. This is the guard the panel's error state depends on.\n\nUse the default `numRuns` unless the file runs slowly; no `fc.configureGlobal` (the repo does not use one).",
      "files": [
        "test/configPanel.form.property.test.ts",
        "test/config.limits.property.test.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Verify: lint, compile, and the full test suite",
      "detail": "Run, from the repo root: `npm run lint` (it lints `test` as well as `src`, so the new files must be warning-free — watch `@typescript-eslint/no-unused-vars`, `eqeqeq` and `semi`, and note `no-explicit-any` comes from the recommended set, so type the helpers instead of reaching for `any`), `npm run compile` (tsc type-checks the test tree too if it is in the tsconfig include — check `tsconfig.json` before assuming, and in any case ts-node will type-check at run time), then `npx mocha -r ts-node/register test/configPanel.test.ts test/configPanel.document.test.ts test/configPanel.form.property.test.ts` for a fast loop, and finally the full `npm test` to prove nothing regressed (the baseline recorded by T02's review is 534 passing, 1 pending; the new count should be that plus the added cases).\n\nIf a test fails because the implementation is wrong rather than the test, do not quietly change src/ — T03's scope is tests. Report the defect in the execute notes (and fix it only if it is a one-line, obviously-correct correction in src/config/, saying so explicitly), since T01/T02 have already been reviewed and passed.\n\nNo source file under src/ or media/ is modified by this todo, and no existing test file is edited; test/config.test.ts and test/config.limits.property.test.ts are read as style references only.",
      "files": [
        "package.json",
        ".mocharc.json",
        "tsconfig.json",
        ".eslintrc.json"
      ]
    }
  ],
  "risks": [
    "The `io` classification test makes `config.json` a directory so `fs.readFile` fails with EISDIR. That is reliable on Linux and macOS and generally on Windows too, but it is the one case that could be platform-dependent; if it proves flaky, assert only that the result is `isErr` with a kind other than `absent`/`unparseable`, or drop the case rather than weakening the rest.",
    "Asserting on error *messages* couples the tests to wording that T02 deliberately borrowed from loadConfig. Mitigated by asserting `kind`/`path` strictly and using substring checks (the path, 'Baiton: Initialize', 'must be a JSON object') for messages — but a future wording change will still touch these tests, by design.",
    "`applyFormToDocument` silently rewrites a `version` of 2 down to SUPPORTED_VERSION. Pinning that in a test blesses behaviour that arguably should refuse to save over a too-new config (loadConfig rejects it as `version-too-new`). The test should pin current behaviour and the execute notes should flag it for T05, where the panel decides whether it even offers to save such a document.",
    "The property file adds fast-check iterations to `npm test`; if a generator over `fc.jsonValue()` produces very large documents the preservation property could slow the suite. Keep value generators shallow (`fc.jsonValue({ maxDepth: 2 })` or similar) and the dictionary small.",
    "`validateConfigForm`'s effort rule ('' valid, whitespace-only invalid, out-of-set non-blank valid) is easy to misread as a bug and 'fix' in a later task. The tests must state the intent in a comment, not just assert it, since T09 mirrors these exact fixtures into media/config.js.",
    "The end-to-end case in step 2 asserts that any form passing `validateConfigForm` yields a document `loadConfig` accepts. That holds for the fields the form owns, but a document whose *unmanaged* parts are already invalid (e.g. a non-string `git.verify`) will still fail to load after a valid save. Scope the assertion to documents whose unmanaged keys are valid, and note the gap rather than over-claiming.",
    "Temp-directory cleanup: the `io` case creates a directory named `config.json`, so the afterEach must use `rmSync(dir, { recursive: true, force: true })` (it already does). A test that returns early on an assertion failure must still leave the dir registered for cleanup — allocate via `newDir()` before any assertion, as test/config.test.ts does.",
    "These tests exercise the core only. `src/activation/configPanel.ts` does not exist yet (T05), so nothing here covers the message channel; the spec's controller test through test/fixtures/vscodeLoader.mjs belongs to T05's todo, and this plan deliberately does not start it."
  ],
  "acceptance": [
    "`npm run lint` and `npm run compile` pass with no errors or warnings, and `npm test` passes with every pre-existing test still green (baseline 534 passing / 1 pending, plus the new cases).",
    "Three new files exist — test/configPanel.test.ts, test/configPanel.document.test.ts, test/configPanel.form.property.test.ts — and no file under src/ or media/ is modified, nor any existing test file.",
    "test/configPanel.test.ts covers, with assertions on `path` (not just message text): a clean valid form; a missing agent, an agent not in `createAdapterRegistry().ids`, and a missing model per role; effort '' valid / whitespace-only invalid / out-of-set valid; for every key of LIMIT_BOUNDS a boundary-accepted, out-of-range-rejected and non-integer-rejected case; empty git.remote and git.base; and the full ordered error list for a form broken in all three sections.",
    "Unknown-key preservation is pinned: a document carrying `pr`, `git.verify`, an extra key inside a role entry, an unknown role key and an unknown top-level key round-trips through `applyFormToDocument` with all of them byte-equal, the input document unmutated, cleared effort removed as a key, limits written as numbers, and edited strings trimmed.",
    "Round-trip is pinned in both directions: applying an unedited `formFromConfig(defaultConfig())` to `JSON.parse(defaultConfigJson())` is the identity, and `formFromDocument(applyFormToDocument(doc, form))` equals the trimmed form.",
    "test/configPanel.document.test.ts asserts `absent` for a missing file, `unparseable` for malformed JSON and for each non-object JSON root, `io` for an unreadable path, and on success the exact file text, the parsed doc, and a 64-hex-char token equal to `configToken(text)`; `readConfigToken` returns ABSENT_TOKEN ('') for a missing file and the matching token otherwise.",
    "Conflict behaviour is pinned: a write with a stale `expectedToken` returns `kind: 'conflict'` carrying the current on-disk token, leaves the file byte-for-byte unchanged and leaves no temp file in the directory; a write with a matching token succeeds and returns the new token; a write with `expectedToken` omitted overwrites unconditionally; ABSENT_TOKEN succeeds against a missing file and conflicts once one exists.",
    "A test pins `writeJsonAtomic`'s serialization to `writeConfigDocument`'s re-derived text: writing `JSON.parse(defaultConfigJson())` produces on-disk bytes identical to `defaultConfigJson()` and a returned `text` identical to those bytes.",
    "An end-to-end case drives read → formFromDocument → edit → validateConfigForm → applyFormToDocument → writeConfigDocument → `loadConfig`, showing a form that validates yields a document the loader accepts with the edits applied and the unmanaged keys intact, and that an invalid form's document is refused by the loader.",
    "The property file states its Feature/Property header in the existing style and asserts: form-side limit acceptance exactly matches the LIMIT_BOUNDS ranges (naming each out-of-range field once), unknown keys survive `applyFormToDocument` for generated documents, and `formFromDocument` never throws for arbitrary JSON.",
    "Any implementation defect the tests uncover is reported in the execute notes rather than silently patched in src/."
  ]
}
```
