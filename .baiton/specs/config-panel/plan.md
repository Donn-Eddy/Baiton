# plan result

```json
{
  "steps": [
    {
      "title": "Contribute the config panel as a webview view in the Baiton container",
      "detail": "In `package.json`, under `contributes.views.baiton`, append a second entry **after** `baiton.specExplorer` so the form renders as the bottom collapsible section under the spec list (the array order is the section order in the container): `{ \"id\": \"baiton.configPanel\", \"name\": \"Configuration\", \"type\": \"webview\", \"visibility\": \"collapsed\" }`. Use the existing `baiton` activity-bar container — do **not** add a new viewsContainer; the spec's 'a Baiton view container' already exists (`package.json:22-29`) and `baiton-chat` in the secondary sidebar is a separate thing that must stay untouched. `visibility: \"collapsed\"` matters: without it the webview resolves (and reads `.baiton/config.json`) every time the Baiton container is opened, which is a behaviour regression against today's open-on-command panel; collapsed means VS Code resolves it only when the user expands it or the reveal command focuses it. Leave the `baiton.openConfigPanel` command contribution exactly as it is — no `when` clause, not listed under `menus.commandPalette` — because `test/activation.gating.test.ts:444` pins that and the corruption-aware registration depends on it. Do not add a `when` clause to the view either: the view must resolve with an absent or unparseable config so the Reset to defaults path stays reachable.",
      "files": [
        "package.json",
        "test/activation.gating.test.ts"
      ]
    },
    {
      "title": "Rewrite ConfigPanelProvider as a WebviewViewProvider",
      "detail": "Rewrite `src/activation/configPanel.ts`'s provider class against `src/activation/chatWebview.ts` as the template — that file is the in-repo precedent for every piece of this. Changes: (a) rename the exported id constant from `CONFIG_PANEL_VIEW_TYPE` to `CONFIG_VIEW_ID = 'baiton.configPanel'` (the value changes too — it is now a contributed view id that must match `package.json` exactly, and the `.focus` command derives from it); keep the old name as nothing, i.e. delete it, and let tsc find any reader. (b) `class ConfigPanelProvider implements vscode.WebviewViewProvider, ConfigPanelWebview, vscode.Disposable`, holding `private view: vscode.WebviewView | undefined` instead of a `WebviewPanel`. (c) Delete `createOrReveal()` and the `vscode.window.createWebviewPanel` call entirely — that is the WebviewPanel-specific lifecycle T11 removes. (d) Add `public static readonly registration = { webviewOptions: { retainContextWhenHidden: true } }` copied from `ChatWebviewProvider.registration` (chatWebview.ts:75-77) and delete the `retainContextWhenHidden: true` creation option: for a view it is a *registration-time* option on `registerWebviewViewProvider`'s third argument, not a per-resolve one. Update the class doc comment accordingly — the current comment (configPanel.ts:14-23) explicitly says the opposite and must be rewritten to name T11. (e) Implement `resolveWebviewView(webviewView)` exactly like chatWebview.ts:117-151: assign `this.view`, set `webview.options = { enableScripts: true, localResourceRoots: [this.mediaUri] }`, set `webview.html = this.renderHtml(webviewView.webview)` (keep `renderHtml`/`readShell`/`makeNonce` unchanged — the nonce/CSP/baseUri substitution is identical for a view), wire `onDidReceiveMessage` and `onDidDispose` (clearing `this.view` so later posts buffer again) into `this.disposables`, then `this.flushPending()` and call a resolve handler. (f) Add `onResolve(handler: () => void)` like `ChatWebviewProvider.onResolve`, invoked at the end of every resolve so the owner can (re)start the controller — VS Code re-resolves after a window reload or after the view is disposed while hidden, and without this the reopened webview would post `ready` to a controller whose message handler is bound to a dead webview. (g) Keep `post`/`onMessage`/`pending` buffering and the `onClose`/`notifyClose` mechanism, but be deliberate about what 'close' now means: a WebviewView is disposed and re-resolved routinely, so `onDidDispose` must **not** fire the close handlers (that would tear down the watcher and controller for a view the user can simply re-expand). Close handlers fire only from `dispose()`, i.e. extension deactivation. This is the behavioural difference from the panel and the most likely place to introduce a bug.",
      "files": [
        "src/activation/configPanel.ts",
        "src/activation/chatWebview.ts",
        "src/activation/configPanelController.ts"
      ]
    },
    {
      "title": "Replace openConfigPanel() with an eager registerConfigPanel() that owns controller, watcher and provider",
      "detail": "`openConfigPanel(deps)` (configPanel.ts:205-267) is create-or-reveal keyed on a module-level `activePanels: Map<string, ConfigPanelProvider>` singleton — both the map and the create-or-reveal contract go away. Replace it with `export function registerConfigPanel(deps: RegisterConfigPanelDeps): vscode.Disposable`, called **once** at activation (step 5) rather than per command invocation. Shape: `RegisterConfigPanelDeps { extensionUri: vscode.Uri; resolveBaitonDir(): string | undefined; agentIds: readonly string[]; capabilities?: Readonly<Record<string, AgentCapabilities>>; log(message: string): void; applyConfig?(baitonDir: string, config: Config): ... }`. The key change is `resolveBaitonDir()` as a **thunk**: registration now happens before the workspace gate and before the user has necessarily opened a folder, so the `.baiton` directory cannot be resolved eagerly the way `runOpenConfigPanel` does today at command time. Body: construct the provider; `provider.onResolve(() => ensureController())`; return `vscode.Disposable.from(vscode.window.registerWebviewViewProvider(CONFIG_VIEW_ID, provider, ConfigPanelProvider.registration), provider, { dispose: () => teardown() })`. `ensureController()` is memoized on the resolved `baitonDir`: on first resolve it calls `resolveBaitonDir()`; if that returns `undefined` it posts `{ type: 'loadFailed', kind: 'invalid', message: <the same 'requires exactly one workspace folder' wording runOpenConfigPanel uses today>, canReset: false }` and returns without building a controller (so the view shows the existing error state rather than throwing); otherwise it builds the `ConfigPanelController` with `applyConfig` pre-bound to that `baitonDir`, creates the `RelativePattern`/`createFileSystemWatcher` + `EXTERNAL_CHANGE_DEBOUNCE_MS` debounce block verbatim from the current code, and calls `controller.start()`. On a *subsequent* resolve (window reload, view re-expanded after disposal) it must **not** rebuild the watcher or controller — it re-calls `controller.start()` only, which just re-registers the message handler onto the fresh webview and is idempotent (configPanelController.ts:117-125); the webview's own `ready` message drives the reload. `teardown()` clears the debounce timer, disposes the watcher subscriptions and the watcher, and disposes the controller — the same body the current `provider.onClose` callback has. Keep `EXTERNAL_CHANGE_DEBOUNCE_MS` exported; `test/configPanel.*` and T07 behaviour depend on the debounce being unchanged. The controller, `ConfigPanelControllerDeps`, the protocol and `src/config/configPanel.ts` need **no** changes in this todo — the controller was already written host-free against a two-method `ConfigPanelWebview` surface, which is exactly why this migration is confined to the glue.",
      "files": [
        "src/activation/configPanel.ts",
        "src/activation/configPanelController.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Turn baiton.openConfigPanel into a reveal-only command",
      "detail": "Add `src/activation/openConfigPanelView.ts` (or a small exported helper next to `registerConfigPanel` — prefer the separate module, mirroring `src/activation/openChat.ts`, which is the exact precedent and already carries the reasoning in its doc comment). Export `CONFIG_VIEW_FOCUS_COMMAND = 'baiton.configPanel.focus'` (VS Code auto-registers `<viewId>.focus` for every contributed view; this is the only stable way to reveal a view — do not reach for internal relocation commands), `CONFIG_VIEW_UNAVAILABLE_MESSAGE = 'Baiton: the Configuration view is not available.'`, and `async function revealConfigPanel(): Promise<void>` that awaits `vscode.commands.executeCommand(CONFIG_VIEW_FOCUS_COMMAND)` inside a try/catch and, on rejection, shows the unavailable message via `showInformationMessage` — a straight copy of `openChat()` (openChat.ts:40-46). Then in `src/activation/commands.ts`: `registerConfigPanelCommand` loses its `context`/`applyConfig` parameters and becomes `registerConfigPanelCommand(): vscode.Disposable` returning `vscode.commands.registerCommand(COMMANDS.openConfigPanel, () => revealConfigPanel())`. Delete `runOpenConfigPanel` (commands.ts:619-647), the `subscribedConfigPanels` Set (commands.ts:554) and the `openConfigPanel`/`ConfigPanelProvider` import (commands.ts:105); move the `agentIds: createAdapterRegistry().ids` / `capabilities: agentCapabilities()` wiring that lived in `runOpenConfigPanel` into the step-5 registration call. Keep `FolderScopedApplyConfig` exported from commands.ts if `extension.ts` still names it (it does, extension.ts:140) — only its call site moves. Watch for now-unused imports: tsc runs with `noUnusedLocals`, so a leftover `createAdapterRegistry`/`agentCapabilities` import in commands.ts fails the build if nothing else there uses them.",
      "files": [
        "src/activation/openConfigPanelView.ts",
        "src/activation/openChat.ts",
        "src/activation/commands.ts"
      ]
    },
    {
      "title": "Register the view provider ahead of the config gate in extension.ts",
      "detail": "In `src/extension.ts`, at the point where `registerConfigPanelCommand` is pushed today (extension.ts:156-160), push **two** things, both before `completeActivation()` so the corruption-aware, no-gate property is preserved: (1) `registerConfigPanel({ extensionUri: context.extensionUri, resolveBaitonDir: () => ..., agentIds: createAdapterRegistry().ids, capabilities: agentCapabilities(), log: (m) => surface.log(m), applyConfig: scopedApplyConfig })` and (2) `registerConfigPanelCommand()`. `resolveBaitonDir` must use the *same* rule `runOpenConfigPanel` used — `resolveCommandRoot(vscode.workspace.workspaceFolders ?? [])` then `Uri.joinPath(root, '.baiton').fsPath` — so multi-root behaviour does not change; `resolveCommandRoot` is currently module-private in commands.ts, so export it (or export a `resolveBaitonDirForCommands(): string | undefined` wrapper from commands.ts and pass that as the thunk — the wrapper keeps the resolution rule in one place and is the smaller diff). `scopedApplyConfig` already compares `baitonDir` against the activated workspace and returns `FOLDER_MISMATCH_NOTE` on a mismatch (extension.ts:140-154); that guard is unchanged and still correct, because the thunk resolves the same folder the command used to. Registering the provider eagerly is required — a `WebviewViewProvider` must be registered before VS Code tries to resolve the view, and the user can expand the section without ever running the command. Confirm `activate()`'s early return on the engine-version guard (extension.ts:112-115) still returns before this registration; that is deliberate and should stay.",
      "files": [
        "src/extension.ts",
        "src/activation/commands.ts",
        "src/activation/configPanel.ts"
      ]
    },
    {
      "title": "Adapt the webview markup and script to a narrow sidebar column",
      "detail": "The shell was written for a full-width editor tab and will be cramped in a ~300px sidebar column. In `media/config.html`: (a) rewrite the top comment — it says 'the Config Panel WebviewPanel provider (T05) loads' and 'The provider also sets retainContextWhenHidden at registration time'; the first half is now wrong, name T11 and the WebviewView provider. (b) `body { background-color: var(--vscode-editor-background) }` becomes `var(--vscode-sideBar-background)` so the section matches the surrounding sidebar, and the sticky `.toolbar` background must change with it or it will paint a mismatched strip. (c) Reduce `body` padding from `calc(var(--baiton-gap) * 2)` to `var(--baiton-gap)` and drop the `<h1>Baiton Configuration</h1>` — the view's own title bar already says 'Configuration' and the heading costs a whole row of a narrow column. (d) The roles `<table>` with four columns (Role/Agent/Model/Effort) does not fit; convert each role to a stacked block: keep the same `data-path` attributes and the same `data-error-for` error divs so nothing in `config.js`'s error-rendering or `setField` plumbing changes, and switch the container from `<table>`/`<tbody>` to a `<div id=\"roles-body\">` of per-role `<fieldset>`/labelled `.field-row` groups. If you would rather not restructure the DOM, the minimum acceptable alternative is a CSS-only stack (`@media (max-width: 420px)` turning the table rows into `display: block` rows with the `<th>` as a label) — but a sidebar view is *always* narrow, so the unconditional stacked layout is the better fit and avoids a media query that never turns off. (e) Let `.toolbar` wrap (it already sets `flex-wrap: wrap`) and shorten the Reset button label to `Reset…`. (f) Keep the CSP block, the nonce placeholders and the 'no sanitizer is loaded' comment verbatim. In `media/config.js`: update the header comment to name T11; the only *code* change is in `buildRoleRows` and whatever constructs `<tr>`/`<td>` nodes — it must emit the new stacked containers instead. **Do not touch the mirror block above the `acquireVsCodeApi` guard**: `validateConfigForm` and its constants are compared verbatim against `src/config/configPanel.ts` by `test/configPanel.mirror.test.ts`, and this todo changes no validation rule. Keep the per-agent model/effort select + `Other…` + free-text machinery from T10 intact; it only changes container, not behaviour.",
      "files": [
        "media/config.html",
        "media/config.js"
      ]
    },
    {
      "title": "Update and add tests",
      "detail": "(a) `test/activation.gating.test.ts:387-400` asserts `views.baiton.map(v => v.id)` deep-equals `['baiton.specExplorer']`; update it to `['baiton.specExplorer', 'baiton.configPanel']` **in that order** (the ordering assertion is what pins 'bottom section under the spec list' — say so in the test name) and extend it to assert the new entry's `type === 'webview'` and `visibility === 'collapsed'`, and that `viewsContainers` is unchanged (still exactly `['baiton']` / `['baiton-chat']`). Leave the existing `baiton.openConfigPanel` command/commandPalette test (line 444) untouched — it still must pass verbatim. (b) New `test/configPanel.view.test.ts` in the style of `test/setApiKey.test.ts`: register the `test/fixtures/vscodeLoader.mjs` hook in `before()`, then dynamically import `../src/activation/configPanel` and `../src/activation/openConfigPanelView`. Cases: `CONFIG_VIEW_ID` equals the id contributed in `package.json` (read the file, same drift-guard shape as the gating suite) and `CONFIG_VIEW_FOCUS_COMMAND` equals `CONFIG_VIEW_ID + '.focus'`; `registerConfigPanel` calls `registerWebviewViewProvider` once with that id and with `webviewOptions.retainContextWhenHidden === true`, and never calls `createWebviewPanel`; `resolveWebviewView` sets `enableScripts` and a `localResourceRoots` limited to `media/`, and produces HTML with no `${nonce}`/`${cspSource}`/`${baseUri}` placeholders left and a `script-src 'nonce-...'` CSP; messages posted before the first resolve are flushed in order on resolve; a second resolve does not create a second watcher/controller (assert `createFileSystemWatcher` was called once); `revealConfigPanel()` executes the focus command, and when that command rejects it resolves normally and shows `CONFIG_VIEW_UNAVAILABLE_MESSAGE`; a `resolveBaitonDir` returning `undefined` posts a `loadFailed` rather than throwing. (c) `test/fixtures/vscodeFake.mjs` is currently four `window.show*` functions and must grow the surface this test needs: `window.registerWebviewViewProvider`, `commands.executeCommand`, `workspace.createFileSystemWatcher`, `RelativePattern`, `Uri.joinPath`/`Uri.file`, `Disposable.from`, and `ViewColumn` only if something still references it. Keep the existing delegate-to-`globalThis.__vscodeFake` pattern so existing suites that install their own fake keep working — add members, never change the delegation shape. (d) `test/configPanel.controller.test.ts`, `configPanel.test.ts`, `configPanel.mirror*.test.ts`, `configPanel.document.test.ts` and `configPanel.form.property.test.ts` should need **no** changes; if any of them fails, that is a signal the migration leaked into the host-free core and should be reverted rather than papered over in the test.",
      "files": [
        "test/activation.gating.test.ts",
        "test/configPanel.view.test.ts",
        "test/fixtures/vscodeFake.mjs",
        "test/fixtures/vscodeLoader.mjs",
        "test/setApiKey.test.ts",
        "test/configPanel.controller.test.ts"
      ]
    },
    {
      "title": "Update the README",
      "detail": "`README.md:148` ('### The config panel') describes the panel as 'an interactive editor form' and `README.md:212` describes the command as one that 'opens `.baiton/config.json` as a form'. Rewrite both: the configuration form is the **Configuration** section of the Baiton view in the activity bar, collapsed by default, sitting under the Spec Explorer; **Baiton: Open Config Panel** (`baiton.openConfigPanel`) *reveals and focuses* that section rather than opening an editor tab. Keep the **Managed fields**, per-agent dropdown / Other… , validation and **reset path** bullets as they are — none of that behaviour changes — but make sure the reset-path bullet still reads correctly now that it is the view, not a panel, that is registered before the configuration gate. Match the existing wording style of the **Baiton: Open Chat** bullet (README.md:210-211), which already describes a reveal-and-focus command.",
      "files": [
        "README.md"
      ]
    },
    {
      "title": "Verify",
      "detail": "`npm run compile` first — tsc with `noUnusedLocals`/`noUnusedParameters` is what catches the dead `openConfigPanel` import, the removed `context`/`applyConfig` parameters on `registerConfigPanelCommand` and any surviving reader of `CONFIG_PANEL_VIEW_TYPE`. Then `npm run lint` (note `media/**/*.js` is in eslint's `ignorePatterns`, so the webview edit is unlinted and untyped — read it carefully), `npm run test:unit`, `npm run test:property`, then the full `npm test`. While iterating run `npx mocha test/activation.gating.test.ts test/configPanel.view.test.ts test/configPanel.controller.test.ts test/configPanel.mirror.test.ts`. Then the manual pass in the Extension Development Host, which is the only coverage the render path has: (1) the Baiton activity-bar icon shows Spec Explorer with a collapsed **Configuration** section beneath it; (2) expanding it loads the form and the stacked role rows are legible at the default sidebar width and after dragging the sidebar narrow; (3) **Baiton: Open Config Panel** from the palette reveals and focuses the section from a cold window; (4) collapse the section, edit `.baiton/config.json` externally, re-expand — the form shows the new values and no stale-token conflict; (5) with `retainContextWhenHidden`, type an unsaved edit, collapse, re-expand — the edit survives and the unsaved-changes banner is still shown; (6) rename `.baiton/config.json` to make it absent, run the command — the view still resolves and offers **Reset to Defaults**, confirming the ungated registration; (7) save a valid change and confirm the hot-reload note still appears (T08 path, unchanged).",
      "files": [
        "package.json",
        "tsconfig.json",
        ".eslintrc.json",
        ".mocharc.json",
        "media/config.js"
      ]
    }
  ],
  "risks": [
    "A WebviewView is disposed and re-resolved routinely (collapsed section, window reload, container hidden), unlike a WebviewPanel which is disposed once and for good. If `onDidDispose` keeps firing the existing `onClose` handlers, the first collapse tears down the file watcher and disposes the controller, and the re-expanded view talks to a disposed controller that silently ignores every message (`ConfigPanelController.handle` returns early when `disposed`). Close handlers must fire only from `dispose()`; the resolve path must re-`start()` an existing controller rather than build a new one.",
    "Registration moves from command-invocation time to activation time, so `baitonDir` can no longer be resolved eagerly — at `activate()` there may be zero or ambiguous workspace folders. Passing `resolveBaitonDir` as a thunk evaluated on first resolve keeps the existing `resolveCommandRoot` rule and the `FOLDER_MISMATCH_NOTE` guard intact; resolving it eagerly would either crash activation or bind the panel to the wrong folder in a multi-root workspace.",
    "Without `\"visibility\": \"collapsed\"` the view resolves whenever the Baiton container is opened, so the extension reads `.baiton/config.json` and installs a file watcher for users who never asked for the config form — a behaviour regression against the open-on-command panel, and one that puts an error state in front of users with an absent config.",
    "`retainContextWhenHidden` silently stops working if it is left as a creation option instead of moving to `registerWebviewViewProvider`'s third argument. Nothing fails loudly: unsaved edits just vanish when the section is collapsed. Copy `ChatWebviewProvider.registration` and assert it in `test/configPanel.view.test.ts`.",
    "`test/fixtures/vscodeFake.mjs` is a four-function stub today; the new view test needs roughly six more members on it. Broadening a fixture that `test/setApiKey.test.ts` also loads risks breaking that suite — only add members, keep the delegate-to-`globalThis.__vscodeFake` shape, and re-run setApiKey.test.ts after touching it. If the fixture growth turns out to be disproportionate, narrow the new suite to the pure constants and the `revealConfigPanel` helper and cover the provider by reading, but say so rather than dropping the test silently.",
    "The four-column roles table is the part of the UI most likely to be unusable in a narrow column, and the webview has neither type-checking nor lint coverage — the mirror suites only exercise the validator block, not the render path. Keep the DOM restructure mechanical (same `data-path` and `data-error-for` attributes, same T10 select/Other…/free-text logic) so `config.js`'s error rendering and `setField` plumbing are untouched, and rely on the manual Extension Development Host pass.",
    "Scope discipline: the host-free core (`src/config/configPanel.ts`, `src/config/configDocument.ts`, `ConfigPanelController` and its protocol) must not change. The todo lists `src/config/configPanel.ts` among its files, but only because the webview mirrors it — a validation-rule edit here would be T10 work redone. If a `src/config/*` change seems necessary, that is a sign the vscode glue leaked into the core.",
    "Changing the view id constant from `baiton.configPanel` as a WebviewPanel *view type* to a contributed *view id* means the `package.json` string, `CONFIG_VIEW_ID` and the derived `.focus` command must agree exactly; a typo produces a command that rejects at runtime and is caught only by the unavailable-message fallback, which looks like a working feature. Pin the agreement with a test that reads `package.json`.",
    "`registerConfigPanelCommand` loses its `context` and `applyConfig` parameters and `runOpenConfigPanel` disappears; `extension.ts` is the only caller today, but the `FolderScopedApplyConfig` type and `scopedApplyConfig` must keep flowing to the new registration or the T08 hot-reload-on-save path goes quietly dead — nothing in the unit suites would notice."
  ],
  "acceptance": [
    "`package.json` contributes `baiton.configPanel` as a `\"type\": \"webview\"` view in the existing `baiton` activity-bar container, listed after `baiton.specExplorer` and collapsed by default; no new view container is added and `baiton-chat` is unchanged.",
    "`src/activation/configPanel.ts` contains no `vscode.window.createWebviewPanel`, no `createOrReveal`, no `WebviewPanel` type and no module-level panel singleton map; the provider implements `vscode.WebviewViewProvider` with a `resolveWebviewView` that locks `enableScripts` + `localResourceRoots: [media]` and renders the nonce'd CSP shell, and carries `retainContextWhenHidden` as a static registration option.",
    "The view provider is registered once during `activate()` before the workspace/config gate, so the form — and its Reset to Defaults path — resolves with an absent or unparseable `.baiton/config.json`; `baiton.openConfigPanel` remains contributed with no `when` clause and outside `menus.commandPalette`.",
    "`baiton.openConfigPanel` reveals and focuses the view through the stable auto-generated `baiton.configPanel.focus` command, and when that command is unavailable it resolves without throwing and shows a user-visible message, matching the `openChat.ts` precedent.",
    "Collapsing and re-expanding the view, or reloading the window, re-resolves the webview and re-renders the current config without creating a second controller or a second file watcher, and without losing unsaved edits.",
    "`ConfigPanelController`, `src/config/configPanel.ts`, `src/config/configDocument.ts` and the message protocol are unchanged; every existing `test/configPanel.*.test.ts` suite passes without edits.",
    "The form is legible in a narrow sidebar column: role fields are stacked rather than laid out as a four-column table, the section uses the sidebar background, and the T10 per-agent model/effort dropdowns with the `Other…` free-text escape still work.",
    "`test/activation.gating.test.ts` pins the two `baiton` views in order with the config view's `type` and `visibility`, and a new `test/configPanel.view.test.ts` covers the view id / focus-command agreement with `package.json`, the registration options, the pre-resolve message buffering, single-watcher re-resolve, the reveal helper's failure path, and the unresolvable-workspace path.",
    "The README describes the configuration form as the collapsed Configuration section under the Spec Explorer in the Baiton view and the command as revealing it, with the managed-fields, validation and reset-path bullets still accurate.",
    "`npm run compile`, `npm run lint`, `npm run test:unit`, `npm run test:property` and `npm test` all succeed with no new failures.",
    "No validation-rule, capability-catalogue or adapter change is included; the todo is confined to the vscode glue, the webview shell, the registration wiring and their tests."
  ]
}
```
