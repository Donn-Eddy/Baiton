# plan result

```json
{
  "steps": [
    {
      "title": "Make the activation state one mutable object shared with the command layer",
      "detail": "Today `activate` builds a fresh object literal for the module-level `activationState` (src/extension.ts:161) and a *second* literal for `registerCommands` (src/extension.ts:168-172). Nothing can refresh both, and `ActivationState`'s fields are `readonly` (src/extension.ts:69-83). Hot-reload needs exactly one object that the command closures already hold a reference to.\n\n  1. In src/extension.ts drop `readonly` from `config` and `executables` on `ActivationState` (keep `workspace` readonly — the resolved folder genuinely cannot change from a config save). Document on each field that it is replaced wholesale by the refresh step and must never be destructured into a long-lived local.\n  2. Build it once and pass the same reference: `const state: ActivationState = { workspace, config, executables }; activationState = state; ... registerCommands(context, state, surface)`.\n  3. `CommandActivation` (src/activation/commands.ts:133-137) already declares `config`/`executables` as non-readonly, so `ActivationState` stays structurally assignable to it with no change. Add a comment there stating the contract: the object is live and every read must go through it.\n\nThe disposable at src/extension.ts:180-183 and `deactivate` (src/extension.ts:186-188) keep clearing `activationState`; the refresher below reads it through a getter, so a disposed window simply has nothing to refresh.",
      "files": [
        "src/extension.ts",
        "src/activation/commands.ts"
      ]
    },
    {
      "title": "Make every config read inside registerCommands lazy",
      "detail": "`registerCommands` destructures `const { workspace, config } = activation` (src/activation/commands.ts:162) and that `config` const is captured by every downstream closure. Replacing `activation.config` would leave all of them pinned to the activation-time value, so this step is what actually makes the refresh visible.\n\n  1. Change the destructure to `const { workspace } = activation;` and add one accessor next to it: `const cfg = (): Config => activation.config;` with a comment saying the config object is replaced wholesale on save and must be re-read per call, never hoisted.\n  2. Rewrite each site to call it. They are all already inside per-dispatch callbacks, so this is a pure read-site change with no restructuring:\n     - the run-queue seams `modelForRole: (role) => modelForRole(cfg(), role)` and `adapterForRole: (role) => adapterForRole(cfg(), adapters, role)` (src/activation/commands.ts:200-201) — `createRunQueue` invokes both at launch time (src/engine/runQueue.ts:476, 600), so a cached queue picks up the new values on its next dispatch;\n     - `submitPrForSlug`: `cfg().pr?.tool` (src/activation/commands.ts:218), `verify: cfg().git.verify` (src/activation/commands.ts:249) and the two seams at src/activation/commands.ts:250-251 (src/engine/submitPr.ts:131, 157 read them per submission);\n     - the spec-draft runner's seams (src/activation/commands.ts:283-284; src/engine/specDraft.ts:168, 187);\n     - the View command's `adapterForRole` argument (src/activation/commands.ts:352).\n  3. `ensureExecutable` already reads `activation.config.roles[role].agent` and `activation.executables.errorFor(agent)` off the passed object (src/activation/commands.ts:877-878) and `activation` is threaded to every stage/action registration, so it becomes lazy for free once step 1 lands. Do not change it; add a one-line comment that this is deliberate.\n  4. Leave `readGitSettings` (src/activation/commands.ts:1296-1303) and `readOrchestratorConfig`/`readRoundBound`/`buildModelClient` alone — they read `vscode.workspace.getConfiguration('baiton')`, not `.baiton/config.json`, and are already live per call. Note in the step-6 tests/comments that `config.git.remote`/`config.git.base` have no consumer in the running extension at all today (the only `config.git` read is `verify`), so editing them in the panel changes the file and nothing else needs reloading.\n  5. Extend `CommandSurface` (src/activation/commands.ts:143-146) with `runningSlugs(): readonly string[]` and return it from src/activation/commands.ts:509: `runningSlugs: () => [...queues.entries()].filter(([, q]) => q.isRunning()).map(([slug]) => slug)`. The refresher uses it to name the one genuinely non-reloadable factor (a stage already launched keeps the model/agent it launched with). Include the spec-draft runner via `specDraftRunner.isRunning()` under a synthetic entry (e.g. `'(spec draft)'`) so a running draft is reported too.",
      "files": [
        "src/activation/commands.ts",
        "src/engine/runQueue.ts",
        "src/engine/submitPr.ts",
        "src/engine/specDraft.ts"
      ]
    },
    {
      "title": "Add the host-free refresher src/activation/configRefresh.ts",
      "detail": "The `ApplyConfig` seam already exists on the controller (src/activation/configPanelController.ts:66, called from `applySaved` at src/activation/configPanelController.ts:280-296 with the freshly `loadConfig`-validated document and its notes forwarded into `saved.notes`). T08 only has to supply it — and the supplier should be unit-testable, so it goes in its own module with no `vscode` import, matching `configPanelController.ts`.\n\nExport:\n```ts\nexport interface ConfigRefreshTarget { config: Config; executables: AgentExecutables; }\nexport interface ConfigRefreshDeps {\n  /** The live activation state, or undefined when activation never completed. */\n  state(): ConfigRefreshTarget | undefined;\n  /** Re-resolve one executable per distinct agent named by a config. */\n  resolveExecutables(agents: readonly string[]): AgentExecutables;\n  /** Retry the gated half of activation after a previously invalid config; resolves the notes to report. */\n  completeActivation(): Promise<readonly string[]>;\n  /** Slugs with a stage in flight, for the in-flight note. */\n  runningSlugs(): readonly string[];\n  log(message: string): void;\n}\nexport function createConfigRefresh(deps: ConfigRefreshDeps): ApplyConfig;\n```\n\nThe returned function, given the new `Config`:\n  1. `const state = deps.state();` If it is `undefined`, activation never got past its gate (a previously absent/corrupt config is exactly the case the panel exists to repair), so `return await deps.completeActivation()` — see step 4. Everything below is the already-activated path.\n  2. Replace the whole object, never field by field: `state.config = config;` This is the crux of the todo; a merge would leave a half-old/half-new document live.\n  3. Re-resolve executables from the *new* role agents: `state.executables = deps.resolveExecutables(ROLES.map((r) => config.roles[r].agent))` (mirrors src/extension.ts:145-149). Switching `roles.executor.agent` from an installed CLI to an uninstalled one must flip the dispatch gate at src/activation/commands.ts:877-878 immediately, and back again once it is installed.\n  4. Collect notes (an empty array means \"fully live, nothing to report\"):\n     - one per `state.executables.errors` entry, so a newly named but missing agent is surfaced in the panel instead of only as an activation-time warning;\n     - when `deps.runningSlugs()` is non-empty, one note naming them: a stage already dispatched keeps the model, effort and agent it launched with; the new values apply to the next run.\n  5. `deps.log(...)` a single line recording the replacement and the note count, then return the notes.\n\nExport a couple of message builders (e.g. `IN_FLIGHT_NOTE(slugs)`, `NOT_ACTIVATED_NOTE`) as named constants/functions so the tests assert against them rather than duplicating prose. Do not re-export from `src/activation/index.ts` — that barrel is the pure-cores barrel consumed by tests; a direct import keeps the dependency explicit, the same way `configPanelController` is imported directly.\n\nNote explicitly in the module doc what needs *no* refresh and why, so a later reader does not go looking: the adapter registry (src/adapter/index.ts:52-71) is keyed by agent id with stateless instances, so only the role→agent lookup had to become lazy, not the registry; the `limits` block has no consumer in the running extension yet; and `SpecExplorer` derives everything from `spec.md` plus the restricted flag and reads no config (src/activation/specExplorer.ts:126-150).",
      "files": [
        "src/activation/configRefresh.ts",
        "src/activation/configPanelController.ts",
        "src/adapter/index.ts",
        "src/activation/specExplorer.ts",
        "src/model/role.ts"
      ]
    },
    {
      "title": "Make the gated half of activate() re-runnable so a repaired config activates without a window reload",
      "detail": "`activate` returns early when `loadConfig` fails (src/extension.ts:134-137), which is the one case where the panel is most likely to be used — and where a note saying \"reload the window\" would contradict the spec's \"in no case does save require a window reload\".\n\nExtract src/extension.ts:118-179 (workspace resolution, trust read, config load, executable resolution, crash recovery, `activationState` assignment, `registerCommands`, the `baiton.activated` context key) into:\n```ts\nasync function completeActivation(context: vscode.ExtensionContext, surface: Surface): Promise<readonly string[]>\n```\nand call it from `activate` after the pre-gate command registrations, ignoring its return there (activation-time failures keep using `showErrorMessage` exactly as now). Rules:\n  - Guard with a module-level `let wired = false;` set once `registerCommands` has run; return `[]` immediately if already wired, so a save after a successful activation never re-registers a command (VS Code throws on a duplicate command id).\n  - On a failure that a config edit cannot fix — `resolveWorkspace` refusing on zero or ambiguous folders (src/extension.ts:128-132) — return a note carrying `resolved.error.message` and explaining that Baiton stays inactive in this window. This is the genuinely non-reloadable factor the todo asks to be reported.\n  - On a `loadConfig` failure return a note carrying the message: the panel wrote something the loader still rejects (the controller already guards the common case at src/activation/configPanelController.ts:283-285, but the semantic check must stay authoritative here too).\n  - On success return `[]`. Crash recovery runs on this late path as well, which is correct: it never ran.\n  - Keep the pre-gate registrations (`registerInitializeCommand`, `registerConfigPanelCommand`, the `Surface`) in `activate` itself, outside the extracted function.\n\nThen build the seam in `activate`, before `registerConfigPanelCommand`:\n```ts\nconst applyConfig = createConfigRefresh({\n  state: () => activationState,\n  resolveExecutables: (agents) => resolveAgentExecutables(agents, pathLookup, settingsOverride),\n  completeActivation: () => completeActivation(context, surface),\n  runningSlugs: () => commandSurface?.runningSlugs() ?? [],\n  log: (m) => surface.log(m),\n});\n```\nwith `let commandSurface: CommandSurface | undefined` assigned inside `completeActivation`. `pathLookup` and `settingsOverride` are already module-level consts (src/extension.ts:206-268), so re-resolution reuses the exact seams activation used.",
      "files": [
        "src/extension.ts",
        "src/activation/configRefresh.ts",
        "src/activation/executable.ts"
      ]
    },
    {
      "title": "Thread applyConfig from activate() to the panel controller, scoped to the activated workspace",
      "detail": "`runOpenConfigPanel` builds the `openConfigPanel` deps and omits `applyConfig` today (src/activation/commands.ts:585-606), so `applySaved` falls through to `ACTIVATION_VALUES_NOTE` (src/activation/configPanelController.ts:298).\n\n  1. Widen `registerConfigPanelCommand(context, surface)` (src/activation/commands.ts:536-543) to `registerConfigPanelCommand(context, surface, applyConfig?: ApplyConfig)` and forward it to `runOpenConfigPanel`, which passes it into the `openConfigPanel` deps alongside `log` — `OpenConfigPanelDeps.applyConfig` and its wiring into the controller already exist (src/activation/configPanel.ts:181-187, 231). Import the `ApplyConfig` type from `./configPanelController`.\n  2. **Scope the seam to the right folder.** `runOpenConfigPanel` resolves its own root via `resolveCommandRoot` (src/activation/commands.ts:611-626), which in a multi-root workspace can differ from the folder activation resolved. Applying a config from folder A to the extension activated against folder B would be wrong, so wrap before passing: call the real `applyConfig` only when the panel's `baitonDir` equals `getActivationState()?.workspace.baitonDir.fsPath` (compare the same `fsPath` strings `openConfigPanel` already keys its singleton map on, src/activation/configPanel.ts:196), and otherwise return a single note saying the file was saved but belongs to a folder this window is not activated against. Do the comparison in src/extension.ts where `activationState` lives, so `commands.ts` gains no import of `extension.ts` (that would be a cycle — `extension.ts` imports `commands.ts` at src/extension.ts:27-31). The cleanest shape is for `activate` to hand `registerConfigPanelCommand` a `(baitonDir: string, config: Config) => ...` wrapper, or to keep `ApplyConfig` and have `openConfigPanel`'s caller close over the already-resolved `baitonDir`; pick one and say so in a comment.\n  3. Update the `ACTIVATION_VALUES_NOTE` doc comment (src/activation/configPanelController.ts:39-41) — it is labelled \"T05 before T08\". It is now only reachable when no seam is injected, i.e. in unit tests and any future caller that opts out; keep the constant and the fallback branch, and say that in the comment. The `notes`-rendering path in the webview is already in place from T04 (`media/config.js:591`, `white-space: pre-line` on `#status` at media/config.html:191-196), so **no webview change is needed for this todo** — verify rather than edit, and leave the mirrored validator block alone for T09's parity test.",
      "files": [
        "src/activation/commands.ts",
        "src/extension.ts",
        "src/activation/configPanel.ts",
        "src/activation/configPanelController.ts",
        "media/config.js"
      ]
    },
    {
      "title": "Unit-test the refresher, and extend the controller suite for the wired seam",
      "detail": "Add `test/configRefresh.test.ts` in the style of `test/configPanel.controller.test.ts` — a static import with no `test/fixtures/vscodeLoader.mjs` hook, which also proves the new module stays host-free. Drive `createConfigRefresh` with a hand-built target object and fake seams:\n\n  - **The config object is replaced as a whole.** Start from `defaultConfig()`, apply a config with a different `roles.executor.model` and a different `limits.exec_attempts`, and assert `target.config` is the *same reference* that was passed in (not a merge of old and new).\n  - **Executables are re-resolved from the new role agents.** Assert `resolveExecutables` was called once with the agent ids of the new config in `ROLES` order, and that `target.executables` is the returned table. Add one case where the new agent resolves and one where it fails, asserting the failure's message appears in the returned notes — that is the dispatch gate at src/activation/commands.ts:877-878 flipping.\n  - **No notes on a clean refresh.** All agents resolve, nothing running → `[]`.\n  - **In-flight run reported.** `runningSlugs()` returning `['alpha']` → exactly one note naming `alpha`; `[]` → no such note.\n  - **Not-yet-activated path.** `state()` returning `undefined` → `completeActivation` called exactly once and its notes returned verbatim; assert the target is untouched.\n  - **Late activation still blocked.** `completeActivation` resolving a workspace-resolution note → that note is what the caller gets (this is the reported non-reloadable factor).\n\nExtend the existing `applyConfig` block in `test/configPanel.controller.test.ts` (coverage item 7 at test/configPanel.controller.test.ts:32-36) with one end-to-end case wiring a real `createConfigRefresh` over a fake target: load → edit a role model → save → assert `saved.notes` is absent/empty **and** the fake target's `config.roles.<role>.model` holds the edited value, i.e. the save reached the running state without a window reload. Update that file's numbered header comment, and add `test/activation.gating.test.ts` assertions only if the `CommandSurface.runningSlugs` shape needs pinning there (it reads `package.json`, so probably not).",
      "files": [
        "test/configRefresh.test.ts",
        "test/configPanel.controller.test.ts",
        "src/activation/configRefresh.ts"
      ]
    },
    {
      "title": "Compile, lint, and run the suite",
      "detail": "Run `npm run compile`, `npm run lint`, `node --check media/config.js` (unchanged, but cheap insurance), `npx mocha test/configRefresh.test.ts test/configPanel.controller.test.ts test/configPanel.document.test.ts`, then the full `npm test`.\n\nExpect exactly the two documented pre-existing failures recorded in .baiton/specs/config-panel/review-1.md and review-2.md — the packaging native-module scan in test/activation.gating.test.ts tripping over `node_modules/keytar` pulled in by `@vscode/vsce`, and test/setApiKey.test.ts's `before all` hook failing to resolve the bare `vscode` specifier under the CommonJS mocha run. The baseline is 649 passing / 1 pending / 2 failing; anything else is a regression from this todo. Do not fix the two known ones here.\n\nPay particular attention to lint on the extracted `completeActivation`: moving ~60 lines of `activate` into a new function will surface unused-import and `no-floating-promises` complaints if the `void`-prefixed calls are moved carelessly.",
      "files": [
        "package.json",
        "src/extension.ts",
        "test/configRefresh.test.ts"
      ]
    }
  ],
  "risks": [
    "The whole todo hinges on one invariant: nothing may hold a long-lived reference to a `Config`. `registerCommands` destructures `config` at src/activation/commands.ts:162 and eight closures capture it; miss one and that code path silently keeps the activation-time value while everything else reloads — a bug that no compile error and no existing test catches. Grep for `config\\.` in commands.ts after the change and confirm every hit goes through the accessor or through `activation.config`.",
    "`ActivationState` is assigned as a fresh literal in two places today (src/extension.ts:161 and the separate literal at src/extension.ts:168-172). If the refresh mutates one object while `registerCommands` closed over the other, every read stays stale and every test that pokes the state directly still passes.",
    "Re-running the gated half of activation risks duplicate command registration: `vscode.commands.registerCommand` throws on an id that already exists, and `registerInitializeCommand`/`registerConfigPanelCommand` are registered before the gate (src/extension.ts:111-116). The `wired` flag must be set the moment `registerCommands` returns, and the pre-gate registrations must stay outside the extracted function.",
    "Late activation also re-runs crash recovery (src/extension.ts:155-158). That is correct — it never ran — but it dispatches git commits and state writes from inside a config save, which is a surprising place for them. It is skipped under Restricted Mode, as at activation.",
    "Re-resolving executables re-walks PATH once per distinct agent id on every save. That is a handful of `statSync` calls and is fine, but it must not throw: `resolveAgentExecutables` already reports a lookup failure as not-found rather than raising, and the refresher must not add its own I/O.",
    "A config save cannot fix a workspace-resolution refusal (zero folders, or an ambiguous multi-root). Reporting that honestly as a note is required; silently returning no notes would tell the user the save went live when the extension is still inactive.",
    "In a multi-root workspace `runOpenConfigPanel` resolves its own root (src/activation/commands.ts:611-626) and may open a panel for a folder the extension is not activated against. Applying that folder's config to the running state would corrupt the live configuration; the seam must compare `baitonDir` against the activated workspace and decline with a note.",
    "`commands.ts` must not import `extension.ts` to reach `getActivationState` — `extension.ts` already imports `commands.ts` (src/extension.ts:27-31), so that closes a cycle. The seam has to be injected downward.",
    "A stage already dispatched keeps the model, effort and agent it launched with; `createRunQueue` reads the seams at launch (src/engine/runQueue.ts:476, 600), not per tick. This is genuinely non-reloadable and must be named in the save outcome rather than papered over.",
    "The `notes` array is rendered into a single `#status` string (`media/config.js:591`), so a long note list becomes a wall of text under the buttons. Keep notes short and few; do not emit one per role.",
    "`config.git.remote` and `config.git.base` have no consumer in the running extension — approve/PR read the `baiton.git.*` VS Code settings via `readGitSettings` (src/activation/commands.ts:1296-1303), and the only `config.git` read is `verify` at src/activation/commands.ts:249. Do not 'hot-reload' them into `gitSettings`; that would be a behaviour change this todo did not ask for, and it belongs in its own todo if it is wanted at all.",
    "The `limits` block is validated but read nowhere in `src/` outside the config module, so there is nothing to reload for it. Claiming otherwise in a note or a comment would be misleading."
  ],
  "acceptance": [
    "A successful save from the Config Panel replaces `activationState.config` with the freshly parsed and `loadConfig`-validated document as a single whole object; no field-by-field merge is performed anywhere.",
    "After a save that changes `roles.<role>.model` or `.effort`, the next stage dispatch, spec draft and PR submission launch with the new values, with no window reload — `modelForRole`/`adapterForRole` are re-read per dispatch through the live state rather than a captured const.",
    "After a save that changes `roles.<role>.agent`, executables are re-resolved for the new agent set and the per-role dispatch gate in `ensureExecutable` reflects it immediately: switching to an uninstalled agent blocks that role's dispatch and names it, switching back unblocks it.",
    "A save that names an agent whose executable is missing still succeeds and reports the resolution failure as a note in the panel, instead of an activation-time-only warning.",
    "When a stage is in flight at save time, the save outcome names the affected spec(s) and states that the running stage keeps the values it launched with; when nothing is running, no such note appears.",
    "Saving a repaired config while activation had bailed out on an absent/corrupt file completes the gated half of activation in place — commands, tree view, chat and the `baiton.activated` context key come up — with no window reload and no duplicate command registration.",
    "A failure a config edit cannot fix (zero or ambiguous workspace folders, or a document the loader still rejects) is reported as an explicit note naming the reason; the panel never tells the user to reload the window.",
    "A clean save against an already-activated, fully-resolved workspace returns an empty `notes` array and the panel shows a bare 'Saved.'",
    "A panel opened against a `.baiton/` folder other than the activated one does not mutate the running state and says so in a note.",
    "`src/activation/configRefresh.ts` carries no `vscode` import and is covered by `test/configRefresh.test.ts`, which imports it statically with no `vscodeLoader` hook: whole-object replacement, executable re-resolution (success and failure), in-flight note, empty notes on a clean refresh, and the not-yet-activated path delegating to `completeActivation`.",
    "test/configPanel.controller.test.ts still passes and gains an end-to-end case proving a save reaches a wired refresh target, with its numbered header comment updated.",
    "The adapter registry, the `limits` block and `SpecExplorer` are documented in the refresher as needing no re-initialization, with the reason for each; no speculative refresh wiring is added for them.",
    "`npm run compile` and `npm run lint` pass clean; `npm test` shows no new failures beyond the two documented pre-existing ones (the keytar native-module scan and the setApiKey `vscode` module resolution).",
    "Manual check in an Extension Development Host: open the panel, change `roles.executor.model`, Save, and confirm the next Execute dispatch launches the new model without reloading the window; separately, delete `.baiton/config.json`, reload the window so activation bails, then Reset to defaults from the panel and confirm the tree view and commands appear without a reload."
  ]
}
```
