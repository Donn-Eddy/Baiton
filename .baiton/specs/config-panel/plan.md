# plan result

```json
{
  "steps": [
    {
      "title": "Expose the reusable pieces of loadConfig.ts (path helper + atomic write)",
      "detail": "In src/config/loadConfig.ts, stop keeping the two pieces configDocument needs private:\n\n1. Add `export function configFilePath(baitonDir: DirLike): string { return path.join(toDir(baitonDir), 'config.json'); }` with a short doc comment saying it is the single place that spells the `config.json` filename, and rewrite the first line of `loadConfig` to `const configPath = configFilePath(baitonDir);` so there is exactly one definition of the path. Keep `toDir` private (it stays an implementation detail; `DirLike` is already exported).\n2. Change `async function writeJsonAtomic(...)` (currently at the bottom of the file, ~line 428) to `export async function writeJsonAtomic(...)`, and extend its doc comment to note that it is also the Config Panel's write path: it serializes with `JSON.stringify(value, null, 2) + '\\n'`, matching `defaultConfigJson()` exactly, and renames a sibling temp file over the target so a failed write never truncates the existing config. Do not change its behaviour or signature — `migrateAndPersist` must keep working unchanged.\n\nNothing else in loadConfig.ts changes; in particular the ConfigError union in types.ts is left alone (configDocument gets its own error type, see step 2).",
      "files": [
        "src/config/loadConfig.ts"
      ]
    },
    {
      "title": "Add src/config/configDocument.ts: raw read with a content-hash conflict token",
      "detail": "New host-free module (imports only `fs/promises`, `crypto`, `../model` for Result/ok/err, and `./loadConfig` for `DirLike`/`configFilePath`/`writeJsonAtomic`; no `vscode` import), with a file-header comment in the style of loadConfig.ts/configPanel.ts naming the spec (\"Config Panel\" / config-panel) and stating that the panel reads the raw document itself — rather than going through `loadConfig` — so unknown keys and the exact on-disk text survive a round trip.\n\nExports:\n\n- `export const ABSENT_TOKEN = '';` — the conflict token of a file that does not exist. Documented as the sentinel that lets a save after Reset-to-defaults use the same token check as any other save (\"I expect no file to be there\").\n- `export function configToken(text: string): string` — hex SHA-256 of the UTF-8 text, using `createHash('sha256').update(text, 'utf8').digest('hex')`, the same convention as the private `sha256` in src/model/hash.ts. Deliberately a local helper rather than an export added to hash.ts, because hash.ts is about spec-content digests (approvalHash/computeInputRev) and has no business knowing about config files. Note in the comment that the token is over the raw text, so a whitespace-only external edit still counts as a conflict.\n- `export interface ConfigDocument { path: string; text: string; doc: Record<string, unknown>; token: string; }` — the raw document as loaded: absolute file path, exact file text, the parsed JSON object, and the token to echo back on save.\n- `export type ConfigDocumentError = { kind: 'absent'; path: string; message: string } | { kind: 'unparseable'; path: string; message: string } | { kind: 'conflict'; path: string; token: string; message: string } | { kind: 'io'; path: string; message: string };` — deliberately a separate union from `ConfigError`: `absent`/`unparseable` mirror the loader's classification (and reuse the same message wording so the panel's error state reads like the rest of the extension), while `conflict`/`io` are write-side outcomes that have no loader equivalent. The `conflict` error carries the current on-disk `token` so the webview's Reload action can adopt it without a second read.\n- `export async function readConfigDocument(baitonDir: DirLike): Promise<Result<ConfigDocument, ConfigDocumentError>>` — `configFilePath(baitonDir)`, then `fs.readFile(p, 'utf8')`: ENOENT → `absent` with the loader's wording (`No configuration found at <path>. Run \"Baiton: Initialize\" first.`), any other read error → `io`. Then `JSON.parse`; a throw → `unparseable` with the loader's wording (`Configuration at <path> is not valid JSON: <message>.`). A parse that yields a non-object (null, array, number, string) → `unparseable` as well, with `Configuration at <path> must be a JSON object.`, because the panel cannot merge a form into a non-object; keep a local `isObject` helper (same three-line shape as loadConfig.ts and configPanel.ts). On success return `{ path, text, doc, token: configToken(text) }`. Never throws for an expected failure — same contract as `loadConfig`.\n- `export async function readConfigToken(baitonDir: DirLike): Promise<Result<string, ConfigDocumentError>>` — reads just the text and returns its token, or `ABSENT_TOKEN` for ENOENT; any other read error is `io`. This is the primitive the write path and (later) the file watcher use, so neither has to parse to learn whether the file moved under it.\n- `export interface WriteConfigOptions { expectedToken?: string; }` and `export async function writeConfigDocument(baitonDir: DirLike, value: unknown, options?: WriteConfigOptions): Promise<Result<ConfigDocument, ConfigDocumentError>>` — the guarded write:\n  1. When `options.expectedToken` is a string, call `readConfigToken`; propagate an `io` error, and when the current token differs from `expectedToken` return `err({ kind: 'conflict', path, token: current, message: '<path> changed on disk since it was loaded. Reload to pick up the new contents, or overwrite to save your edits anyway.' })` — nothing is written. `expectedToken === ABSENT_TOKEN` therefore correctly refuses when a file has appeared since the panel decided there was none.\n  2. When `expectedToken` is `undefined`, skip the check entirely: this is the Overwrite and the Reset-to-defaults path (T05/T07 pass no token there deliberately).\n  3. Write with `writeJsonAtomic(path, value)`; a throw → `err({ kind: 'io', ... })` with a `Could not write configuration at <path>: <message>.` message.\n  4. On success recompute the text exactly as the writer did — `JSON.stringify(value, null, 2) + '\\n'` — and return `ok({ path, text, doc, token: configToken(text) })`, so the caller gets the post-write token without re-reading the file. Note in the comment that the serialization is duplicated with `writeJsonAtomic` on purpose to avoid widening that function's return type, and that a test pins the two together (T03).\n\nTOCTOU is not solved and is not meant to be: document that the token check narrows, not closes, the window between reading the token and the rename, and that the file watcher in T07 is the other half of the story.\n\nStyle notes to follow: `Result`/`ok`/`err` from `../model` (never throw for expected failures), private `describe(e)` helper for error text exactly as in loadConfig.ts, JSDoc on every export (the codebase is consistent about this), and no unused locals/params (tsconfig has `noUnusedLocals`/`noUnusedParameters`).",
      "files": [
        "src/config/configDocument.ts",
        "src/config/loadConfig.ts",
        "src/model/hash.ts",
        "src/config/defaultConfig.ts"
      ]
    },
    {
      "title": "Re-export from the config barrel",
      "detail": "Add `export * from './configDocument';` to src/config/index.ts, after `./configPanel` so the barrel order still reads types → loader → defaults → scaffolding → panel core → panel document I/O. Update the module doc comment's last sentence to mention the panel's document I/O alongside the form protocol. Verify no name collides with an existing export (`ABSENT_TOKEN`, `configToken`, `ConfigDocument`, `ConfigDocumentError`, `readConfigDocument`, `readConfigToken`, `writeConfigDocument`, `WriteConfigOptions`, `configFilePath`, `writeJsonAtomic` are all new) — `tsc` will catch a duplicate, but a quick `grep -rn` over src/config is cheaper.",
      "files": [
        "src/config/index.ts"
      ]
    },
    {
      "title": "Verify: compile, lint, and a manual round-trip smoke check",
      "detail": "Run `npm run compile` and `npm run lint` (the T01 gate). Because the unit tests for this module are T03's todo, do a throwaway smoke check against the compiled output the way T01 did — a short node script under the scratchpad dir, not a file added to the repo — over a temp dir: (a) `readConfigDocument` on a missing file → `absent`; on `{ bad json` → `unparseable`; on `[]` → `unparseable`; on a `defaultConfigJson()` file → ok with a stable token and `doc.roles` present; (b) `writeConfigDocument` with the loaded token succeeds and returns a different token; (c) writing again with the now-stale token returns `conflict` and leaves the file byte-identical; (d) writing with no `expectedToken` overwrites; (e) `writeConfigDocument(dir, JSON.parse(defaultConfigJson()))` produces text byte-identical to `defaultConfigJson()`. Do not run `npm test` expecting new coverage here — no test file is added by this todo.",
      "files": []
    }
  ],
  "risks": [
    "Exporting `writeJsonAtomic` widens loadConfig.ts's public surface; the risk is a later caller passing a non-config path or a value that is not a plain object. Mitigated by leaving the signature and behaviour untouched and documenting it as the config write path only.",
    "The token is a hash of the raw file text, so any external reformat (even whitespace-only) reads as a conflict. That is the intended strictness — the panel cannot merge text it did not parse — but it means a prettier-on-save setup on `.baiton/config.json` will produce conflict banners in T07; worth calling out in the T09 README note.",
    "Token-check-then-write is not atomic: a write that lands between `readConfigToken` and the rename is still lost. Accepted (documented in the module); closing it would need file locking, which the design does not ask for.",
    "`writeConfigDocument` re-derives the written text with its own `JSON.stringify(value, null, 2) + '\\n'`. If `writeJsonAtomic`'s serialization ever changes, the returned token drifts from the file's real hash and the next save spuriously conflicts. Mitigated by a T03 test asserting the returned text equals the file contents read back.",
    "Reusing `loadConfig`'s exact message wording for `absent`/`unparseable` couples two modules by string. Acceptable (the user sees one voice), but if the loader's wording changes the panel's should too; the duplication is noted in the comment.",
    "Classifying a parsed non-object as `unparseable` diverges from `loadConfig`, which reports it as `missing-section: root`. Intentional — the panel's error state only distinguishes \"can we offer Reset to defaults\" — but it must be stated in the doc comment so T05 does not expect a `missing-section` here.",
    "`readConfigDocument` returning a bare object means the panel trusts nothing about the shape; T05 must still run `formFromDocument`/`validateConfigForm` before using it. Not this todo's job, but a real integration hazard."
  ],
  "acceptance": [
    "`npm run compile` and `npm run lint` both pass with no errors or warnings.",
    "src/config/configDocument.ts imports no `vscode` module and is loadable under plain node from `out/`, so T03 can unit-test it without a VS Code host.",
    "`readConfigDocument` returns `absent` for a missing file (message naming the path and pointing at \"Baiton: Initialize\"), `unparseable` for malformed JSON and for a parsed non-object, `io` for any other read failure, and on success returns the exact file text, the parsed object, and a token that is the hex SHA-256 of that text.",
    "`writeConfigDocument` with a matching `expectedToken` writes the file atomically and returns the new token; with a stale `expectedToken` it returns `kind: 'conflict'` carrying the current on-disk token and leaves the file byte-for-byte unchanged; with `expectedToken` omitted it overwrites unconditionally.",
    "`writeConfigDocument(dir, JSON.parse(defaultConfigJson()))` writes text byte-identical to `defaultConfigJson()` (two-space indent, single trailing newline), and the returned `text`/`token` match what a fresh read of the file yields.",
    "`ABSENT_TOKEN` as `expectedToken` succeeds when no file exists and conflicts when one has appeared.",
    "`configFilePath` is the only place `'config.json'` is joined in loadConfig.ts, and `loadConfig`'s existing behaviour — including migration persistence through `writeJsonAtomic` — is unchanged (the existing test/config.test.ts, config.version.property.test.ts and config.limits.property.test.ts still pass).",
    "Every new export is reachable from `src/config/index.ts` and carries a JSDoc comment; no source file outside src/config/ is modified."
  ]
}
```
