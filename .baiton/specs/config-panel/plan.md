# plan result

```json
{
  "steps": [
    {
      "title": "Add the host-free ConfigPanelController in src/activation/configPanelController.ts",
      "detail": "The todo asks for a provider 'plus a host-free ConfigPanelController', and the spec says the host side is 'structured like chatWebview.ts + chatController.ts: thin vscode glue around a host-free controller' — that pair is two files, so this is two files. The controller carries no `vscode` import (chatController.ts imports only `path`, `fs/promises` and host-free cores; match that), which is what makes the whole of T05's behaviour unit-testable without the vscodeLoader hook.\n\nHeader comment in house style (see chatController.ts): name the spec ('Config Panel', config-panel, todo T05), state that this is the host-free half driven by the ConfigPanelHostToWebview / ConfigPanelWebviewToHost union already defined in src/config/configPanel.ts (T01), and that every host capability — modal confirm and live-config apply — arrives as an injected seam.\n\nSurface:\n- `export interface ConfigPanelWebview { post(msg: ConfigPanelHostToWebview): void; onMessage(handler: (msg: ConfigPanelWebviewToHost) => void): void; }` — deliberately the same two-method shape as `ChatWebview` (chatController.ts:87), so the provider implements it and tests use a recording fake.\n- `export type ApplyConfig = (config: Config) => Promise<readonly string[]> | readonly string[];` — the T08 hot-reload seam. It receives the freshly loaded config as a replaced whole (the spec's wording) and returns human-readable notes naming anything that could not be reloaded. T05 defines and calls it; T08 supplies the implementation.\n- `export interface ConfigPanelControllerDeps { webview: ConfigPanelWebview; baitonDir: string; agentIds: readonly string[]; confirmReset(message: string): Promise<boolean>; applyConfig?: ApplyConfig; log(message: string): void; }` — `agentIds` is injected (not imported from ../adapter) so the validator and the dropdown share one source of truth chosen by the caller, exactly as the spec's 'installed agent ids from createAdapterRegistry().ids' requires without hardwiring it here.\n- `export class ConfigPanelController { constructor(deps: ConfigPanelControllerDeps); start(): void; }` — `start()` only registers `deps.webview.onMessage(...)`. It must not push an unsolicited first message: media/config.js posts `{ type: 'ready' }` itself at the end of its IIFE (config.js:607), and that is what triggers the first load.\n\nPrivate state, only what a round trip needs: `doc: Record<string, unknown> | undefined` (the last raw parsed document, kept so applyFormToDocument merges onto the real unknown keys), `token: string | undefined` (the token that document was read with), and `options: ConfigFormOptions` (recomputed on every load via `configFormOptions(deps.agentIds, form)`; initialise to `configFormOptions(deps.agentIds)` so save-side validation has an agent set even before a load).\n\nDispatch: a single `private async handle(msg: ConfigPanelWebviewToHost): Promise<void>` switching on `msg.type` over exactly the four cases the union and media/config.js use — `ready`, `load`, `save`, `reset`. An unrecognised type is passed to `deps.log` and ignored, never thrown. `start()` registers a synchronous handler that calls `void this.handle(msg).catch(...)` (the vscode message channel is sync and ignores returned promises), and that catch logs through `deps.log` and posts `{ type: 'saveFailed', reason: 'io', message }` so the webview's `busy` flag can never latch on an unexpected throw.",
      "files": [
        "src/activation/configPanelController.ts",
        "src/activation/chatController.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Implement load: raw read, tolerant form mapping, dropdown options, error state",
      "detail": "One `private async load(): Promise<void>` serves both `ready` and `load`. media/config.js posts `{type:'load'}` for the Reload button, for Reload-after-conflict, for Reload-from-the-error-state, and automatically after an `externalChange` on a pristine form (config.js:493, :589), so load must be idempotent and must always terminate in exactly one `loaded` or `loadFailed`.\n\n1. `const read = await readConfigDocument(this.deps.baitonDir)` (src/config/configDocument.ts). This is the raw read, not `loadConfig`, precisely so unknown keys survive — see that module's header.\n2. On `isErr`: clear `this.doc` and `this.token`, then post `loadFailed` mapping the `ConfigDocumentError.kind` onto the protocol's narrower `'absent' | 'unparseable' | 'invalid'`:\n   - `absent` → `{ kind: 'absent', message: error.message, canReset: true }`\n   - `unparseable` → `{ kind: 'unparseable', message: error.message, canReset: true }`\n   - `io` → `{ kind: 'invalid', message: error.message, canReset: false }`. Add a one-line comment saying why: `loadFailed` has no `io` kind, and an unreadable file is not something Reset can fix (a reset would write over a file we could not even read). `conflict` cannot occur on a read; handle it in the same default branch as `io`.\n3. On `isOk`: `const form = formFromDocument(read.doc)` — the tolerant mapper, not `formFromConfig`. A parseable-but-semantically-invalid document must still open in the form so the user can fix the bad field (the spec's error-state story is only about `absent`/`unparseable`). Then `this.doc = read.doc; this.token = read.token; this.options = configFormOptions(this.deps.agentIds, form);` and post `{ type: 'loaded', form, token: read.token, options: this.options }`.\n\nPassing `form` into `configFormOptions` is load-bearing, not decoration: it appends an out-of-set agent or effort already present in the document so the dropdown keeps that value instead of silently rewriting it on the next save — the spec's 'a saved value outside that set is shown as an extra option so an existing config still round-trips'. Note it in the header comment.",
      "files": [
        "src/activation/configPanelController.ts",
        "src/config/configDocument.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Implement save: host-side re-validation, merge base selection, conflict refusal, atomic write",
      "detail": "`private async save(msg: { form: ConfigForm; token: string; overwrite?: boolean }): Promise<void>`. The spec is explicit that the webview is untrusted and the host re-validates; media/config.js does validate before posting (config.js:433), so the host check is a second gate, never the only one.\n\n1. `const errors = validateConfigForm(msg.form, { agents: this.options.agents })`. If non-empty, post `{ type: 'saveFailed', reason: 'invalid', message: <n> + ' field(s) are invalid.', errors }` and return without writing. The `errors` array must be exactly what `validateConfigForm` returns: media/config.js stores it as `errorsFromServer` and renders each message against its `path`, so the dotted paths (`roles.<role>.agent`, `limits.exec_attempts`, `git.base`) are the contract.\n2. Choose the merge base:\n   - normal save (`overwrite !== true`): use `this.doc` when `this.token === msg.token`; otherwise re-read with `readConfigDocument` and use its `doc` (the in-memory copy is stale or absent).\n   - `overwrite === true`: **always** re-read first and merge onto what is on disk now. Overwrite means 'my form fields win', not 'silently revert the unknown keys somebody just added'. If that re-read fails `absent`/`unparseable`, fall back to `this.doc ?? {}` so overwrite still succeeds against a file that was deleted or corrupted underneath; an `io` failure is reported as `saveFailed { reason: 'io' }`.\n3. `const next = applyFormToDocument(base, msg.form)` — it clones rather than mutates and preserves `version`, `pr`, `git.verify` and unknown role keys (configPanel.ts), so nothing extra is needed here.\n4. `const written = await writeConfigDocument(this.deps.baitonDir, next, msg.overwrite === true ? undefined : { expectedToken: msg.token })`. Pass the **webview's** token, not `this.token`: the conflict question is 'did the file change since the form the user is looking at was built', and that is the token the panel echoes back.\n5. On `isErr`: `conflict` → `{ type: 'saveFailed', reason: 'conflict', message: error.message }`; everything else → `{ type: 'saveFailed', reason: 'io', message: error.message }`. Do not adopt the conflict error's `token` into `this.token` — the webview's Reload comes back as a plain `load`, which re-reads anyway, and adopting it would let a later save silently overwrite an unread change.\n6. On success: `this.doc = written.doc; this.token = written.token;`, run the hot-reload step (next step), then post `{ type: 'saved', token: written.token, notes }`, omitting `notes` when the array is empty. `saved` is posted only after `applyConfig` resolves, because config.js renders the notes as part of the save status line (config.js:566).",
      "files": [
        "src/activation/configPanelController.ts",
        "src/config/configDocument.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Implement the post-save hot-reload step and its notes",
      "detail": "`private async applySaved(): Promise<string[]>`, called from `save` after a successful write. This is the T05 half of the spec's 'Live config reload': define the seam, call it, and report honestly. The refactors it will eventually drive are T08.\n\n1. `const loaded = await loadConfig(this.deps.baitonDir)` — the authoritative semantic check against the bytes now on disk, and the source of the `Config` the running extension should adopt. This is what the spec means by 'reuses loadConfig for the semantic check'; note in a comment that it runs *after* the write, on the merged document, which is why the panel can preserve unknown keys and still end up with a `Config` the loader accepts.\n2. On `isErr`: this should be unreachable after `validateConfigForm` passed, but it is reachable through sections the form does not manage (a hand-broken `version`, a `pr.tool` the loader refuses). Return a single note: 'Saved, but the running extension kept its previous configuration: ' + error.message, and do not call `applyConfig`.\n3. On `isOk`, when `deps.applyConfig` is set: `return [...(await deps.applyConfig(loaded.value))]`.\n4. When it is not set (T05 lands before T08 wires it): return one note — 'New values were written to .baiton/config.json; components that read the configuration at activation keep their current values until the window is reloaded.' The panel must not imply a hot-reload that did not happen, and the spec removed the Reload Window notification action, so this is a note in the panel, never a `showInformationMessage` with an action.\n5. Wrap the `applyConfig` call in try/catch: a throw is logged through `deps.log` and turned into a note. A failed hot-reload must not turn a successful write into `saveFailed` — the bytes are on disk either way.\n\nWhen T08 injects a real `applyConfig`, nothing else in this module changes; that is the point of the seam.",
      "files": [
        "src/activation/configPanelController.ts",
        "src/config/loadConfig.ts",
        "src/config/defaultConfig.ts"
      ]
    },
    {
      "title": "Implement reset-to-defaults with a modal confirmation",
      "detail": "`private async reset(): Promise<void>`. media/config.js posts `{type:'reset'}` with no dialog of its own and an explicit comment that the confirmation is the host's (config.js:499), so the modal lives here.\n\n1. `const ok = await this.deps.confirmReset('Reset .baiton/config.json to the Baiton defaults? The current contents, including any keys the panel does not manage, are discarded.')`.\n2. On `false`: write nothing, and call `this.load()` so the webview re-renders its current state and clears the `busy`/status it set when the button was pressed. Declining must be observably a no-op on disk.\n3. On `true`, create the parent directory first: `await mkdir(path.dirname(configFilePath(this.deps.baitonDir)), { recursive: true })`. `writeJsonAtomic` (loadConfig.ts:441) writes a sibling temp file and renames; it does **not** create the directory, so a reset in a workspace with no `.baiton/` would fail ENOENT. This matters because the spec routes the reset path through the `absent` error state, which is exactly the case where the folder may not exist. Treat an mkdir failure as `saveFailed { reason: 'io' }`.\n4. `await writeConfigDocument(this.deps.baitonDir, defaultConfig(), undefined)` — no `expectedToken`: reset is intentionally unconditional (configDocument.ts documents the omitted-token case as the Overwrite/Reset path). Write the object, not `defaultConfigJson()`: `writeJsonAtomic` serialises as `JSON.stringify(value, null, 2) + '\\n'`, byte-identical to `defaultConfigJson()`, and going through one serializer keeps the two from drifting. Say so in a comment and let the new test pin the equality.\n5. On write failure post `{ type: 'saveFailed', reason: 'io', message }`. On success call `this.load()` so the panel gets a fresh `loaded` built from the document actually on disk, with a token that matches it, rather than a synthesised one.\n\nDo not call `initialize()` here — the panel resets one file, it does not scaffold a workspace.",
      "files": [
        "src/activation/configPanelController.ts",
        "src/config/defaultConfig.ts",
        "src/config/loadConfig.ts",
        "src/config/configDocument.ts"
      ]
    },
    {
      "title": "Add the ConfigPanelProvider (vscode glue) in src/activation/configPanel.ts",
      "detail": "New file modelled on src/activation/chatWebview.ts, with the same header shape (what it owns, and what it deliberately does not) and the same four responsibilities: nonce'd shell, nonce-only CSP, `localResourceRoots` limited to `media/`, two-way message plumbing. media/config.html already documents the three placeholders it expects and states that the provider sets `retainContextWhenHidden`.\n\n- `export const CONFIG_PANEL_VIEW_TYPE = 'baiton.configPanel';` and a module-private `const CONFIG_HTML = 'config.html';`\n- `export class ConfigPanelProvider implements ConfigPanelWebview, vscode.Disposable`, constructed with the extension `vscode.Uri`, holding `panel: vscode.WebviewPanel | undefined`, the `pending: ConfigPanelHostToWebview[]` buffer, the single `messageHandler`, a `closeHandler`, and a `disposables` array.\n- `public createOrReveal(): void`, the create-or-reveal singleton the todo names. If a panel exists, `this.panel.reveal(this.panel.viewColumn ?? vscode.ViewColumn.Active, false)` and return — do **not** re-render the HTML: `retainContextWhenHidden` keeps the DOM, the unsaved edits and the `vscode.setState` draft alive (config.js persists a draft), and a rebuild would discard them silently. Otherwise `vscode.window.createWebviewPanel(CONFIG_PANEL_VIEW_TYPE, 'Baiton Config', vscode.ViewColumn.Active, { enableScripts: true, retainContextWhenHidden: true, localResourceRoots: [this.mediaUri] })`, set `panel.webview.html = this.renderHtml(panel.webview)`, wire `onDidReceiveMessage` to `this.messageHandler`, wire `onDidDispose` to clear `this.panel`, drop the pending buffer and invoke `closeHandler`, then `flushPending()`. Note the difference from the chat provider in the header: a `WebviewPanel` is created rather than resolved, so `retainContextWhenHidden` is a creation option here, not the registration-time `webviewOptions` the chat provider exposes as a static.\n- `post` / `onMessage` implement `ConfigPanelWebview` exactly as ChatWebviewProvider implements `ChatWebview`, including the buffer-until-ready behaviour (the controller can post before the DOM is up).\n- `public onClose(handler: () => void): void` and `dispose()` (dispose the listeners, and the panel if still open).\n- Private `mediaUri` getter, `renderHtml(webview)` (the same three global regex replacements chatWebview.ts uses for `${nonce}`/`${cspSource}`/`${baseUri}`), `readShell()` (`fs.readFileSync` of `media/config.html`), and a module-private `makeNonce()` (16 random bytes as hex, CSP-safe). chatWebview.ts keeps its own copy of that helper and does not export it; duplicate the three lines here with a comment pointing at the original rather than touching a file this todo lists only as a reference.\n- No `WebviewPanelSerializer` is registered: nothing in the spec asks for the panel to come back after a window reload, and registering one would need its own state contract. Say so explicitly in the header so the omission reads as a decision.",
      "files": [
        "src/activation/configPanel.ts",
        "src/activation/chatWebview.ts",
        "media/config.html",
        "media/config.js"
      ]
    },
    {
      "title": "Export a small openConfigPanel entry point so T06 stays a pure registration change",
      "detail": "Also in src/activation/configPanel.ts:\n\n`export interface OpenConfigPanelDeps { extensionUri: vscode.Uri; baitonDir: string; agentIds: readonly string[]; log(message: string): void; applyConfig?: ApplyConfig; }`\n\n`export function openConfigPanel(deps: OpenConfigPanelDeps): ConfigPanelProvider` — a module-level singleton: if a provider already exists for the same `baitonDir`, `createOrReveal()` it and return it; otherwise construct the provider, construct a `ConfigPanelController` with `confirmReset` implemented as `async (message) => (await vscode.window.showWarningMessage(message, { modal: true }, 'Reset')) === 'Reset'` (the same modal idiom as the `confirmDelete` seam in extension.ts:390 and the submit-PR confirm in commands.ts:873), `start()` it, `createOrReveal()` the panel, and clear the module singleton from `provider.onClose(...)`.\n\nDo **not** add an export to src/activation/index.ts: that barrel exports only the host-free activation cores (engineVersion, workspace, executable) and neither chatWebview nor chatController appears there; extension.ts and commands.ts import glue modules by path. Follow that. Likewise do not touch package.json, src/extension.ts or commands.ts here — the command contribution, workspace resolution and registration before the config gate are T06's scope, and half-registering them now would leave an intermediate commit with a contributed command and no handler.",
      "files": [
        "src/activation/configPanel.ts",
        "src/activation/commands.ts",
        "src/extension.ts"
      ]
    },
    {
      "title": "Add test/configPanel.controller.test.ts",
      "detail": "Mocha + ts-node (.mocharc picks up `test/**/*.test.ts`), in the style of test/configPanel.document.test.ts: a header naming the spec and todo and enumerating coverage, and throwaway `fs.mkdtempSync(path.join(os.tmpdir(), 'baiton-config-panel-'))` directories playing `.baiton/`, cleaned up in `after`.\n\nBecause the controller imports no `vscode`, this suite needs **no** `test/fixtures/vscodeLoader.mjs` hook: import it statically at the top of the file, the way the document suite imports src/config/*. That static import is itself the assertion that the controller stayed host-free — if someone adds a `vscode` import the suite fails to load. Note that in the header (the spec's testing section mentions the loader fake; the split makes it unnecessary here, and T06's command test is where the fake would earn its place).\n\nHarness: a `recordingWebview` implementing `ConfigPanelWebview` that appends every posted message to an array and exposes `send(msg)` to drive the webview→host direction; a `confirmReset` stub with a settable answer and a call counter; an `applyConfig` spy recording the `Config` it received and returning canned notes; `log` collecting into an array. Await the promise the handler returns (or a `flush()` helper over it) so assertions do not race the async dispatch.\n\nCases:\n1. `ready` against a valid file → exactly one `loaded`, whose `token` equals `configToken(<file text>)`, whose `form` matches `formFromConfig` of the loaded config, and whose `options.agents` equals the injected ids.\n2. `ready` with no file → `loadFailed { kind: 'absent', canReset: true }`; with `{ not json` → `{ kind: 'unparseable', canReset: true }`.\n3. load → change a role model → `save` with the loaded token → `saved`; the file parses to the edited value, still carries an unknown top-level key and `pr`, and is two-space indented with a trailing newline.\n4. `save` with a blank model → `saveFailed { reason: 'invalid' }` carrying the `roles.<role>.model` path, and the file byte-for-byte unchanged.\n5. `save` with a stale token after an external edit → `saveFailed { reason: 'conflict' }` with the external edit still on disk; replaying the same save with `overwrite: true` succeeds **and preserves an unknown key the external edit introduced** — the re-read-before-overwrite rule.\n6. `reset` declined → no write, file unchanged, `confirmReset` called once; accepted → file text exactly `defaultConfigJson()` and a fresh `loaded` follows. Plus a reset into a directory that does not exist yet, covering the mkdir path.\n7. Hot-reload: after a successful save `applyConfig` is called once with a `Config` carrying the new values and its notes appear in `saved.notes`; with no `applyConfig` injected, `saved.notes` is the single activation-values note; an `applyConfig` that throws still yields `saved` (never `saveFailed`) with a note and a logged line.\n\nRun `npx mocha test/configPanel.controller.test.ts`, then `npm run lint` and `npm run compile` before handing off.",
      "files": [
        "test/configPanel.controller.test.ts",
        "test/configPanel.document.test.ts",
        "test/setApiKey.test.ts",
        "test/fixtures/vscodeLoader.mjs"
      ]
    }
  ],
  "risks": [
    "The todo's file list names src/activation/configPanel.ts, but the plan adds src/activation/configPanelController.ts beside it. This follows the todo's own wording ('plus a host-free ConfigPanelController') and the spec's 'structured like chatWebview.ts + chatController.ts'. Folding both into one file would force the controller tests through test/fixtures/vscodeLoader.mjs, whose fake currently exposes only window.show* and would need Uri, ViewColumn, Disposable and createWebviewPanel added — a bigger change to shared test scaffolding than the extra file.",
    "Overwrite semantics are a real decision, not a detail. Merging the form onto the stale in-memory document would silently revert unknown keys an external editor just added. The plan re-reads before an overwrite and pins it with a test; if the reviewer prefers the cheaper stale merge, it must be a deliberate call.",
    "writeJsonAtomic does not create the parent directory. Every other write path in the codebase runs after initialize(), so this is easy to miss — but reset-to-defaults is reachable exactly when .baiton/config.json is absent, which is also when .baiton/ may be absent.",
    "ConfigPanelHostToWebview['loadFailed'] has kinds absent | unparseable | invalid only, so an I/O read failure is folded into 'invalid' with canReset:false. If that reads badly in the UI, the fix belongs in the protocol in src/config/configPanel.ts (T01 territory), not an ad-hoc fourth kind here.",
    "The conflict token narrows but does not close the TOCTOU window between reading the token and the atomic rename — configDocument.ts says so explicitly and names T07's file watcher as the other half. T05 inherits that; do not try to solve it here.",
    "applyConfig is a seam with a fallback note. If T08 slips, the panel still tells the truth about what did not reload, but the spec's promise that no save requires a window reload is not yet met. Keep the note wording honest rather than aspirational, and do not reintroduce a Reload Window notification action — the spec removed it.",
    "The panel is an editor-area WebviewPanel with no serializer, so it does not survive a window reload. That is a deliberate omission; if it is wrong it is a visible behavioural gap to raise, not something to add silently.",
    "makeNonce/renderHtml/readShell are duplicated from chatWebview.ts. Accepted for three small helpers (chatWebview.ts is a reference file for this todo, not an edit target); if a third webview appears, factor them into a shared module then.",
    "The controller must answer every webview message, including failures, or media/config.js leaves its busy flag latched and the panel looks frozen. The catch-all around the async dispatch is what prevents that, not an afterthought."
  ],
  "acceptance": [
    "`npm run compile` and `npm run lint` pass with no new errors or warnings.",
    "`npx mocha test/configPanel.controller.test.ts` passes and the full `npm test` is still green.",
    "test/configPanel.controller.test.ts imports the controller with a plain static import and never registers the vscodeLoader hook, proving the controller carries no `vscode` import.",
    "Opening the panel twice reveals the same panel rather than creating a second one, and does not re-render the HTML or lose unsaved form edits.",
    "The webview's `ready` is answered by exactly one `loaded` or `loadFailed`, for a valid, an absent and an unparseable config; `absent` and `unparseable` carry canReset:true.",
    "A save posted with an invalid form is refused host-side with `saveFailed { reason: 'invalid' }` carrying per-field paths from validateConfigForm, and leaves the file byte-for-byte unchanged, even though the webview validates too.",
    "A save with a stale token is refused with `saveFailed { reason: 'conflict' }` and writes nothing; the same save with `overwrite: true` succeeds and preserves unknown keys present in the current on-disk document.",
    "A successful save writes two-space-indented JSON with a trailing newline and preserves `version`, `pr`, `git.verify` and unknown top-level keys.",
    "Reset-to-defaults asks for a modal confirmation, writes nothing when declined, and on confirmation writes exactly `defaultConfigJson()` — including when `.baiton/` did not exist — then re-posts `loaded`.",
    "After a successful save the injected applyConfig seam is called once with the freshly loaded Config, its notes are reported in `saved.notes`, and a throwing or absent seam still yields `saved` with an honest note rather than a failure or a Reload Window prompt.",
    "No changes to package.json, src/extension.ts, src/activation/commands.ts or src/activation/index.ts (T06's scope), and none to media/config.html or media/config.js (T04's scope)."
  ]
}
```
