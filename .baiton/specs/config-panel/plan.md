# plan result

```json
{
  "steps": [
    {
      "title": "Add the per-agent capability constants to each adapter module",
      "detail": "Keep each CLI's knowledge in its own adapter file, next to the flags that already encode it, and export it as plain constants (module-level exports, not new members of the `Adapter` interface, so the Requirement 14.1 'an adapter owns only launch args, the probe and the continue flag' boundary is untouched — this is exactly how `CODEX_EFFORT_CONFIG_KEY`, `OPENCODE_PLAN_AGENT` and `ANTIGRAVITY_PLAN_MODE` already live). Add, with a short doc comment each: `src/adapter/claude.ts` — `export const CLAUDE_MODELS: readonly string[]` (curated; MUST contain `'claude-sonnet-5'`, the model `defaultConfig()` writes, so a fresh config's value is in-table) and `export const CLAUDE_EFFORTS = ['low','medium','high'] as const` (the `--effort` values `launch()` emits). `src/adapter/antigravity.ts` — `ANTIGRAVITY_MODELS` (curated) and `ANTIGRAVITY_EFFORTS = ['low','medium','high']` (agy's `--effort`). `src/adapter/codex.ts` — `CODEX_MODELS` (curated) and `CODEX_EFFORTS` — the values codex accepts for `--config model_reasoning_effort=<e>` (`codexEffortFlags`); list every documented level rather than the smallest plausible set, because this list is range-checked (see step 3) and an over-narrow list rejects a legal value. `src/adapter/opencode.ts` — `export const OPENCODE_MODELS: readonly string[] = []`, `export const OPENCODE_EFFORTS: readonly string[] = []` and `export const OPENCODE_MODEL_DOC_URL = 'https://opencode.ai/docs/models/'`, with a comment stating the deviance the spec calls out: opencode models are `provider/model` ids and `--variant` maps to user-configured values, so both are free text and neither may be rejected. Every list is declared `readonly string[]`/`as const` and never mutated; the curated model lists are explicitly documented as a starting point that the \"Other…\" escape (step 4) makes non-blocking when a new model ships. Do not invent values you have not seen: seed each list from the CLI's own help/docs during step 8's investigation and keep it short.",
      "files": [
        "src/adapter/claude.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/codex.ts",
        "src/adapter/opencode.ts",
        "src/config/defaultConfig.ts"
      ]
    },
    {
      "title": "Add the AgentCapabilities type and agentCapabilities() to the adapter seam",
      "detail": "In `src/adapter/adapter.ts`, next to `AgentId`/`AGENT_BINARY`, add `export interface AgentCapabilities { readonly models: readonly string[]; readonly efforts: readonly string[]; readonly modelLink?: string }` — the exact shape the spec names. Document the one rule the whole feature hangs on: **an empty list means free text** (no dropdown, no membership check), a non-empty list means an enumerated set rendered as a dropdown *plus* an always-present \"Other…\" entry. `modelLink` is an optional documentation URL rendered next to a free-text model field. In `src/adapter/index.ts` add `export function agentCapabilities(): Record<AgentId, AgentCapabilities>` assembling the four adapter constants: `claude`/`antigravity`/`codex` get their enumerated models+efforts and no `modelLink`; `opencode` gets `{ models: [], efforts: [], modelLink: OPENCODE_MODEL_DOC_URL }`. Return a **fresh object with copied arrays on every call** (`[...CLAUDE_MODELS]` etc.), matching `defaultConfig()`'s factory convention and `configFormOptions`' existing copy-don't-alias behaviour, so a caller that appends an \"Other…\" value cannot corrupt the table for the next call. Keys are built from the same `AgentId` union as `createAdapterRegistry`, so the catalogue and the registry ids cannot drift (assert this in step 6). Put `agentCapabilities` beside `createAdapterRegistry`, and note in its doc comment that this table — not the panel, not the webview — is the single source of truth for model and effort options and for the host-side validator.",
      "files": [
        "src/adapter/adapter.ts",
        "src/adapter/index.ts",
        "src/adapter/claude.ts",
        "src/adapter/opencode.ts"
      ]
    },
    {
      "title": "Thread the catalogue through ConfigFormOptions and configFormOptions()",
      "detail": "In `src/config/configPanel.ts` (which must stay import-light: `src/model` + `./types` only, no `src/adapter` import, because `src/adapter` pulls `child_process` and this module is the one mirrored into the browser), change the option set from one global effort list to a per-agent table. Replace `ConfigFormOptions` with `{ agents: readonly string[]; byAgent: Readonly<Record<string, AgentFormCapability>> }` where `AgentFormCapability = { models: readonly string[]; efforts: readonly string[]; modelLink?: string }` — structurally identical to `AgentCapabilities` but declared locally so the module keeps no adapter import. **Drop the top-level `efforts` field**: keeping it would re-create the second source of truth this todo exists to remove (update every consumer — see steps 5 and 6). Keep `EFFORT_OPTIONS` exported only if something outside the panel still uses it; otherwise delete it and let the catalogue supply efforts (grep first — today only `configFormOptions` and tests read it). New signature: `configFormOptions(agentIds: readonly string[], capabilities: Readonly<Record<string, AgentFormCapability>>, form?: ConfigForm): ConfigFormOptions`. Behaviour: (a) `agents` is `[...agentIds]` plus any non-empty `entry.agent` in `form` that is not already listed, appended once in `ROLES` order — unchanged; (b) `byAgent` starts as a per-agent copy of `capabilities` (copy the arrays); (c) the round-trip rule, which is the load-bearing part: for each role in `ROLES`, if the role's trimmed agent has a `byAgent` entry whose `models` list is **non-empty** and the form's `model` is not in it, append that model; same for `efforts` with the form's `effort` (skipping `''`). Agents with an empty list stay empty (free text — nothing to append to), and an agent with no `byAgent` entry at all (an out-of-set agent id) is given `{ models: [], efforts: [] }`, i.e. free text, so an unrecognised agent never causes a spurious model/effort error on top of the agent error it already produces. This is precisely the existing `configFormOptions` agent-round-trip rule extended per the spec, and it is what makes step 4's closed-set effort check safe for values already on disk.",
      "files": [
        "src/config/configPanel.ts",
        "src/adapter/index.ts",
        "src/model/role.ts"
      ]
    },
    {
      "title": "Extend validateConfigForm with the per-agent effort rule (and deliberately not a model rule)",
      "detail": "Change the signature to `validateConfigForm(form: ConfigForm, options: { agents: readonly string[]; byAgent: Readonly<Record<string, AgentFormCapability>> })` and keep the error ordering exactly as it is (roles in `ROLES` order → limits in `Object.keys(LIMIT_BOUNDS)` order → git), because three test suites pin it. Per role, after the existing agent and model checks, in the same emit order agent → model → effort: (a) agent rules unchanged (missing / not in `options.agents`, verbatim messages unchanged); (b) **model: no membership check at all** — blank/whitespace stays the only model error. The spec is explicit that a brand-new model must work without a table update and that a hand-typed opencode model must not be rejected, and the \"Other…\" escape makes any enumerated model list advisory. Say so in a comment so a later reader does not \"tighten\" it; (c) **effort: closed-set check only where the catalogue is closed.** Keep the existing `entry.effort !== '' && entry.effort.trim().length === 0` blank error first. Then, when `entry.effort` is non-empty and non-blank, look up `options.byAgent[entry.agent.trim()]`; if that entry exists and its `efforts` list is non-empty and does not include the trimmed effort, push `{ path: 'roles.<role>.effort', message: '\"<role>\" effort \"<effort>\" is not supported by <agent> (supported: <efforts joined with \", \">).' }`. Emit at most one effort error per role (blank OR unsupported, never both). Nothing changes for opencode (empty list ⇒ skipped) or for an unknown agent (no entry ⇒ skipped). Because step 3's `configFormOptions` appends the value already present in the loaded document, an existing config with an exotic effort still loads, still saves and is never silently rewritten; only a value the user newly types for a closed-set agent is rejected. Limits and git rules are untouched. Also update `applyFormToDocument`'s doc comment only if its precondition wording needs it — its behaviour does not change.",
      "files": [
        "src/config/configPanel.ts",
        "src/config/types.ts"
      ]
    },
    {
      "title": "Mirror the new validator and render per-agent controls in the webview",
      "detail": "`media/config.js`: (1) inside the mirror block above the `acquireVsCodeApi` guard, port the new `validateConfigForm` rule for rule and message for message (the parity suite compares message strings verbatim), keeping the block self-contained — no DOM access, no new globals — and update the header comment to name T10. `EFFORT_OPTIONS` in the mirror is now dead once efforts come from the options payload: delete it from the mirror and from the `window.baitonConfigForm` export only if step 3 deleted the TS constant, otherwise keep both in lockstep — whichever you choose, the two files must agree, since `test/configPanel.mirror.test.ts` asserts the exported constant set. (2) Below the guard, rebuild the role row rendering from `state.options.byAgent`. The agent `<select>` is unchanged. The model cell becomes: a `<select>` listing `byAgent[agent].models` plus a trailing `Other…` option carrying a sentinel value (`var OTHER_MODEL_VALUE = '\\u0000other'` — a value no real model id can collide with), plus a sibling text `<input>` that is shown when the sentinel is selected or when `models` is empty, and hidden otherwise; when `models` is empty the select is hidden entirely and the field is pure free text. The effort cell follows the same shape over `byAgent[agent].efforts`, keeping the leading `(default)` option for `''`; an empty `efforts` list renders a free-text input. When `modelLink` is set, render an anchor next to the model field (`textContent` only, `href` = the link, `target=\"_blank\"`) — CSP already allows navigation-free links; if VS Code blocks the anchor, post the URL as text instead rather than adding a new message type. (3) `state.form` still holds a single plain string per field — the select/Other split is view state only, so nothing in the protocol or in `ConfigForm` changes; writing the free-text input writes the same `roles.<role>.model` path through `setField`. (4) On an agent change, recompute that row's model/effort controls from `byAgent` and **keep the current value**: if it is not in the new list, select `Other…` and put it in the text input (never blank it, never substitute the first list entry) — this is the 'never silently rewrite a saved value' rule at the UI layer. (5) `buildRoleRows`' `renderedOptionsSignature` must include the per-agent lists and each row's current agent, or rows will not rebuild when an agent changes; simplest correct form is to serialize `state.options` plus the six selected agents. (6) `state.options` initialises to `{ agents: [], byAgent: {} }`. `media/config.html`: add the markup/CSS the new cells need (a wrapper class for the select+input pair and the doc link) — the per-row nodes are still built in JS, so the HTML change is the style block plus any static table header wording.",
      "files": [
        "media/config.js",
        "media/config.html",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Wire the catalogue through the host controller, provider and command",
      "detail": "`src/activation/configPanelController.ts`: add `capabilities?: Readonly<Record<string, AgentCapabilities>>` to `ConfigPanelControllerDeps` and default it in the constructor to `agentCapabilities()` imported from `../adapter` (the controller already imports Node built-ins, and `src/adapter` carries no `vscode` import, so the host-free property holds). Making it optional keeps the ~20 existing `ConfigPanelController` test constructions compiling while still exercising the real table. Pass it into both `configFormOptions` call sites (constructor and `load()`), and pass `this.options` — now carrying `byAgent` — into `validateConfigForm` in `save()`; the host-side re-validation therefore enforces the same per-agent effort rule as the webview, which is the point of the todo. `src/activation/configPanel.ts`: add the matching optional `capabilities` to its deps interface and forward it, alongside the existing `agentIds`. `src/activation/commands.ts:637`: pass `capabilities: agentCapabilities()` next to `agentIds: createAdapterRegistry().ids` so the running extension uses the real table explicitly rather than relying on the default. Check `src/activation/configRefresh.ts` for anything that re-derives panel options on a live config refresh; the catalogue is static (it depends on the installed adapters, not on the config), so nothing there needs to change — confirm rather than assume.",
      "files": [
        "src/activation/configPanelController.ts",
        "src/activation/configPanel.ts",
        "src/activation/commands.ts",
        "src/activation/configRefresh.ts"
      ]
    },
    {
      "title": "Tests: the catalogue, the option sets, the validator, the fixtures and the mirror",
      "detail": "New `test/adapter.index.test.ts` (the file the todo names), in the style of `test/adapter.registry.test.ts`: `agentCapabilities()` has exactly one entry per `createAdapterRegistry().ids` and per `AGENT_BINARY` key (drift guard); claude/antigravity/codex each expose non-empty `models` and `efforts` with no duplicates and no blank entries; opencode exposes empty `models` and `efforts` and a non-empty `modelLink` (the free-text shape the spec calls out); `defaultConfig()`'s model and effort are members of the claude lists, so a freshly initialised workspace never opens the panel on an out-of-table value; two calls return equal but non-aliased objects, and mutating the arrays of one call does not affect the next. Update `test/configPanel.test.ts`: the `configFormOptions` block now asserts `byAgent` — copies of the catalogue, the per-role append of an out-of-table model/effort for a closed-set agent, no append for a free-text agent, an out-of-set agent mapping to the free-text shape, and no mutation of the passed-in catalogue; add `validateConfigForm` cases for an unsupported effort on claude (error, verbatim message) and the same effort on opencode (no error). Update `test/fixtures/configFormCases.ts`: its `options` become the full `{ agents, byAgent }` shape (derive the default from `agentCapabilities()` next to the existing `AGENT_IDS`), and add cases for (a) a closed-set agent with an out-of-table effort — one `roles.<role>.effort` error, (b) the same case with that effort injected into `byAgent` — valid, the round-trip rule, (c) opencode with a `provider/model` model and a hand-typed variant — valid, (d) an out-of-table *model* on a closed-set agent — valid, pinning the deliberate absence of a model rule. `test/configPanel.mirror.property.test.ts`: extend the arbitrary to draw an options value with a varying `byAgent` (sometimes closed, sometimes free-text, sometimes missing the drawn agent) so the new branch is compared under generated input. `test/configPanel.mirror.test.ts` needs only its constant-parity assertions adjusted if step 5 removed `EFFORT_OPTIONS` from the mirror export. Fix up `test/configPanel.controller.test.ts` and `test/configPanel.document.test.ts` where they construct a bare `{ agents: ... }` options object for `validateConfigForm`.",
      "files": [
        "test/adapter.index.test.ts",
        "test/configPanel.test.ts",
        "test/fixtures/configFormCases.ts",
        "test/configPanel.mirror.test.ts",
        "test/configPanel.mirror.property.test.ts",
        "test/configPanel.controller.test.ts",
        "test/configPanel.document.test.ts",
        "test/adapter.registry.test.ts"
      ]
    },
    {
      "title": "Run the CLI model-listing investigation and record the findings",
      "detail": "The spec's 'CLI model listing — follow-up investigation' section belongs with this todo because it is what the curated table is a stand-in for. For each of `claude`, `agy`, `codex` and `opencode` that is actually installed on this machine, try the plausible listing surfaces (`<bin> models`, `<bin> --list-models`, `<bin> model list`, `<bin> doctor`, and the help output) and record what actually happened — including 'no such command' and 'not installed', which are results. Do not guess and do not let any shipped behaviour depend on what you find: the shipped behaviour stays 'curated table + Other…'. Use whatever you learn to sanity-check the model and effort lists seeded in step 1 (an effort list that is too narrow is the one list that can reject a legal value — widen it if the CLI documents more levels). Write the findings up as a short per-agent list in the README, next to the config-panel section, with one sentence on how a future change could fold a real listing command into the capability probe that already runs before every stage so the curated table refines itself. Keep it factual and dated in the sense of naming the CLI versions you probed.",
      "files": [
        "README.md",
        "src/adapter/claude.ts",
        "src/adapter/antigravity.ts",
        "src/adapter/codex.ts",
        "src/adapter/opencode.ts"
      ]
    },
    {
      "title": "Update the README's config panel section",
      "detail": "The existing `### The config panel` section (README.md:148) currently describes 'a model text input, and an effort level (low, medium, high, or (default) when unset)' — both sentences are now wrong. Rewrite the **Managed fields** bullet to say the model and effort options are per-agent, drawn from one catalogue in the adapter seam: for claude, antigravity (agy) and codex a curated dropdown with an always-present **Other…** entry that switches the field to free text, so a brand-new model works without an extension update; for opencode both fields are free text (its models are `provider/model` ids and effort maps to user-configured `--variant` values) with an inline link to opencode's model documentation. Add a sentence to the **Preservation of unmanaged keys** or **Inline and host-side validation** bullet stating the round-trip rule explicitly: a value already in the file that is outside the catalogue is kept and shown as the selected Other… value, never rewritten, and changing a role's agent re-computes that role's option set without discarding what is there. Note that effort is validated against the agent's supported set where the agent has one (claude/agy/codex) while a model is never rejected for being unrecognised. Then add the step-8 investigation findings as their own short subsection.",
      "files": [
        "README.md",
        "src/adapter/index.ts",
        "src/config/configPanel.ts"
      ]
    },
    {
      "title": "Verify",
      "detail": "`npm run compile` (tsc is strict with `noUnusedLocals`/`noUnusedParameters` — a deleted `EFFORT_OPTIONS` or an unused import will fail the build), `npm run lint` (covers `src` and `test`; `media/**/*.js` is in eslint's `ignorePatterns`, so the webview change is only covered by the mirror suites), `npm run test:unit` and `npm run test:property`, then the full `npm test`. Run `npx mocha test/adapter.index.test.ts test/configPanel.test.ts test/configPanel.mirror.test.ts test/configPanel.mirror.property.test.ts` while iterating. Confirm by hand once that a one-character divergence between the new effort rule in `src/config/configPanel.ts` and the one in `media/config.js` turns the parity suite red, then revert it. Finally open the panel in the Extension Development Host and check the three UI behaviours no unit test covers: switching a role to opencode turns model and effort into text inputs with the doc link; switching back to claude keeps the typed value as the selected Other… entry; and a config on disk carrying an out-of-table model loads with Other… pre-selected and saves back byte-identical when nothing is edited.",
      "files": [
        "package.json",
        ".mocharc.json",
        "tsconfig.json",
        ".eslintrc.json",
        "media/config.js"
      ]
    }
  ],
  "risks": [
    "The curated effort lists are the only newly range-checked values in this change, so an over-narrow list silently blocks a legal configuration (e.g. omitting codex's `minimal` or a `xhigh` level). Mitigation: seed the effort lists from the CLI's own help during the step-8 investigation, err on the permissive side, and rely on the fact that a value already present in the document is appended by `configFormOptions` and therefore never rejected. Models are deliberately not range-checked at all — do not 'fix' that.",
    "Dropping the top-level `ConfigFormOptions.efforts` is a breaking shape change across `src/config/configPanel.ts`, `src/activation/configPanelController.ts`, `media/config.js` and four test files. Keeping it alongside `byAgent` would be less churn but would re-create the second source of truth this todo removes; take the churn and let tsc find the call sites (the webview is not type-checked, so `media/config.js`'s `state.options` initialiser and `buildRoleRows` must be found by reading, not by the compiler).",
    "The webview is the half with no compiler and no lint coverage. The mirror parity suites only exercise `validateConfigForm`, not the new select/Other…/free-text rendering, so the render path is covered by the manual Extension Development Host pass in the Verify step and nothing else. Keep the render logic dumb and derived from `state.options` so there is little to get wrong.",
    "`buildRoleRows` currently rebuilds only when the flat agent/effort option signature changes. With per-agent lists the rows must also rebuild when a role's selected agent changes; if the signature is not widened, a user switching a role to opencode keeps a stale dropdown and can be blocked from typing a valid value.",
    "The round-trip rule and the new closed-set effort check pull in opposite directions: the check only stays safe because `configFormOptions` is given the loaded `form` and appends its out-of-table effort. The controller's constructor calls `configFormOptions` without a form and `load()` calls it with one — the save path re-validates against whatever `this.options` currently holds, so if a future edit ever validates against the constructor-built options, an existing config becomes unsaveable. Keep `save()` reading the options that `load()` produced.",
    "`src/config/configPanel.ts` must not import `src/adapter`: it is the module mirrored into a browser script and it deliberately carries no Node built-in import, while `src/adapter` pulls `child_process`. Pass the catalogue in as data (a structurally-typed local interface) rather than importing `agentCapabilities` there.",
    "Curated model ids are a fact about the outside world that this plan cannot verify. Do not ship a list from memory: seed it from the installed CLIs' own output or documentation in step 8, keep it short, and make sure `defaultConfig()`'s `claude-sonnet-5` is present or a freshly initialised workspace opens the panel showing its own default as an Other… value.",
    "Scope discipline: T11 converts the panel to a sidebar WebviewView and will touch `media/config.html`, `media/config.js` and `src/activation/configPanel.ts` again. Do not start that migration here, and keep the new markup layout-agnostic (it has to work in a narrow sidebar column soon).",
    "`agentCapabilities()` returning shared array instances would let `configFormOptions`' append step mutate the catalogue for every later call, which would leak one workspace's out-of-table value into the next panel load. Copy the arrays on every call and assert the non-aliasing in `test/adapter.index.test.ts`."
  ],
  "acceptance": [
    "`agentCapabilities(): Record<AgentId, { models, efforts, modelLink? }>` is exported from `src/adapter/index.ts`, has exactly one entry per `createAdapterRegistry().ids`, and sources each entry from constants that live in that agent's own adapter module.",
    "claude, antigravity and codex expose non-empty enumerated `models` and `efforts`; opencode exposes empty `models` and `efforts` plus a `modelLink` to opencode's model documentation, and an empty list is documented and treated everywhere as 'free text, never range-checked'.",
    "`configFormOptions` takes the catalogue and returns per-agent option sets (`byAgent`), with the existing agent round-trip rule extended: a model or effort already present in the loaded document but outside a closed catalogue list is appended to that agent's list rather than dropped.",
    "`validateConfigForm` rejects an effort that is not in the agent's supported set when that agent has one, never rejects a model for being unrecognised, and never rejects an opencode model or variant — verified by fixtures for all three.",
    "The host-side re-validation in `ConfigPanelController.save` and the webview's live validation run the same per-agent rule against the same option payload; `test/configPanel.mirror.test.ts` and `test/configPanel.mirror.property.test.ts` still pass with the extended options shape and would fail on a one-character divergence (verified by hand once).",
    "The panel renders per-agent model and effort dropdowns with an always-present Other… free-text escape, renders opencode's fields as free text with the documentation link, and re-computes a role's option set when its agent changes without rewriting the value already there.",
    "A config on disk whose model or effort is outside the catalogue loads with that value selected as Other…, and saving with no edits leaves the file byte-identical.",
    "`test/adapter.index.test.ts` exists and covers the per-agent shapes, the registry-id drift guard, the non-aliasing of returned arrays, and that `defaultConfig()`'s model and effort are members of the claude lists.",
    "The CLI model-listing investigation is recorded per agent in the README — including 'no such command' and 'not installed' outcomes — with a note on how a future change could fold a real listing command into the pre-stage capability probe, and nothing shipped depends on an unverified CLI surface.",
    "The README's config panel section no longer describes a plain model text input or a fixed low/medium/high effort list, and states the Other… escape, the opencode free-text deviance and the never-rewrite round-trip rule.",
    "`npm run compile`, `npm run lint`, `npm run test:unit`, `npm run test:property` and `npm test` all succeed with no new failures.",
    "No WebviewView / view-container migration work (T11) and no change to adapter launch-argument behaviour is included in this todo."
  ]
}
```
