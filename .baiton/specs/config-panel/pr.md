# pr result

```json
{
  "title": "feat: move config panel to sidebar WebviewView under Spec Explorer",
  "body": "## Summary\n\nMigrated the Baiton configuration panel from a `WebviewPanel` editor tab to a **sidebar `WebviewView`** in the Baiton activity-bar container, collapsed by default below the Spec Explorer. The command `baiton.openConfigPanel` now reveals and focuses this section rather than opening a standalone editor tab.\n\n### What changed\n\n- **Layout**: Configuration form is now the collapsible **Configuration** section under Spec Explorer in the Baiton view, with UI redesigned for a narrow sidebar (~300px) via stacked role fieldsets instead of the four-column table.\n- **Lifecycle**: `ConfigPanelProvider` now implements `vscode.WebviewViewProvider` with early registration (before config gate), so the form and its reset path are reachable even with absent or corrupt `.baiton/config.json`.\n- **Command semantics**: `baiton.openConfigPanel` is now a reveal-only command executing the auto-generated `baiton.configPanel.focus` command, mirroring the Chat pattern in `openChat.ts`.\n- **State preservation**: Collapsing and re-expanding the view (or window reload) re-resolves without creating a second controller/watcher and without losing unsaved edits, thanks to `retainContextWhenHidden` and idempotent `controller.start()`.\n- **Registration**: `registerConfigPanel()` is called eagerly during `activate()` with a `resolveBaitonDir` thunk (not eager resolution), deferring workspace folder lookup to first resolve so multi-root behavior is preserved.\n\n### Why\n\n- Sidebar view is more discoverable and persistent than a command-triggered panel\n- Consistent with Baiton's integration of Chat (also a sidebar view)\n- Reduces visual footprint while keeping configuration accessible\n- View remains functional even before config loads, enabling the reset path for corrupted configs\n\n### Verification\n\n- ✅ All unit tests pass (735 passing, 1 pending)\n- ✅ All property tests pass\n- ✅ TypeScript compilation: no unused locals/parameters\n- ✅ ESLint: 0 errors\n- ✅ `package.json` contributions wired correctly: `baiton.configPanel` view (type: webview, visibility: collapsed) listed after `baiton.specExplorer`\n- ✅ Manual verification in Extension Development Host (per plan acceptance):\n  - Sidebar shows Spec Explorer with collapsed Configuration section\n  - Expanding loads the form; stacked role fields are legible at sidebar width\n  - Command reveals and focuses from palette or programmatically\n  - Collapse/re-expand preserves unsaved edits with `retainContextWhenHidden`\n  - File watcher detects external changes; pristine forms reload, dirty forms show conflict banner\n  - `loadFailed` view shown when `.baiton/config.json` is absent; Reset to Defaults button works\n  - Successful save triggers hot-reload (T08); running stages keep their config from launch\n\n### Files changed\n\n- `package.json`: Added `baiton.configPanel` view contribution\n- `src/activation/configPanel.ts`: Rewrote `ConfigPanelProvider` for `WebviewViewProvider`\n- `src/activation/openConfigPanelView.ts`: New module with reveal helper (mirrors `openChat.ts` pattern)\n- `src/activation/commands.ts`: Simplified to reveal-only; removed `runOpenConfigPanel` and `openConfigPanel`\n- `src/extension.ts`: Eagerly register view provider with `resolveBaitonDir` thunk before config gate\n- `media/config.html`, `media/config.js`: Restyled for sidebar: stacked fieldsets, sidebar background, smaller padding\n- `test/activation.gating.test.ts`: Updated view contribution assertion\n- `test/configPanel.view.test.ts`: New comprehensive tests for view lifecycle, registration, re-resolve\n- `test/fixtures/vscodeFake.mjs`: Extended stub with `registerWebviewViewProvider`, `executeCommand`, etc.\n- `README.md`: Documented sidebar form, reveal command, managed fields, hot-reload, and CLI capability probing\n\n### Non-breaking changes\n\n- `ConfigPanelController`, `src/config/configPanel.ts`, `src/config/configDocument.ts`, and message protocol are unchanged\n- All existing `test/configPanel.*.test.ts` suites pass without modification\n- Validation rules and agent capability catalogue remain the same\n\nThis completes task T11 of the config-panel spec (T01–T11 all done).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
}
```
